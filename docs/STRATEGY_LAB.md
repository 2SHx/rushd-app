# Strategy Lab — Ledger

Single source of truth for the strategy loop (plan: `G2/G3 Strategy Lab`). One row per setup.
Statuses: `CANDIDATE → CODIFIED → VALIDATING → ACCEPTED | REJECTED`. Only ACCEPTED enters the
AUTO_PAPER league. Never delete a row — a REJECTED row with evidence prevents blind re-tries. Cold-start every session from this file +
docs/JOURNAL.md, never from re-exploration. Raw bars / equity curves / MC distributions live in
DB + `results/*.json`, NEVER in this file — headline metrics only.

**Promotion gate (QDR-6):** walk-forward · ≥20–30% OOS holdout · ≥100–200 trades · deflated Sharpe ·
profit plateau · MC maxDD p95 within envelope. Sharpe > 3 ⇒ `implausible`, blocks promotion.
All setups long-only cash (Sharia: no short, no margin). Every claim labeled simulated/paper.

## 2026-07-21 evidence-integrity amendment (append-only)

An independent audit proved that the following twelve legacy terminal cards replay a sleeve selected from
the 2026-07-17 SPUS fixture and terminal-date liquidity/Sharia evidence back through earlier history:
`ts-momentum-halal-basket-v4`, `bollinger-mr-long-v3`, `halal-markowitz-core`,
`halal-risk-parity-core`, `halal-momentum-risk-parity-core`,
`halal-sector-capped-risk-parity-core`, `halal-sector-capped-risk-parity-wide`,
`halal-concentrated-momentum-core`, `halal-managed-momentum-core`,
`halal-momentum-markowitz-core`, `halal-trend-rider-core`, and `halal-fast-momentum-core`.
Their raw artifacts and original **REJECTED** verdicts remain unchanged, but their reason sets are
amended with `SHARIA_UNVERIFIABLE` and `DATA_QUALITY_PIT_FAILURE`. Their CAGR, DSR, curves, and
cross-card rankings are conditional on 2026 survivors: exploratory only, **not a clean OOS frontier
and not promotable**. The same OOS slice also informed successive mechanisms, so future DSR evidence
uses the full related trial family (currently 108 trials after registering the neural family), and fresh
confirmation requires genuine PIT membership snapshots plus never-inspected or forward-paper data.
The zero-run residual and causal-TCN candidates below are the thirteenth and fourteenth guarded setups.
Terminal runs of all fourteen
now fail before loading bars, simulation, result writes, or DB
persistence until historical membership/lifecycle/Sharia coverage exists; diagnostics are marked
`unverified-diagnostic-only` and may be used for wiring only.

## 2026-07-22 residual-momentum registration (no performance run)

`halal-residual-fast-momentum-core@v1` is codified from an independent frozen brief but has **no
diagnostic or FULL result**. It removes each stock's trailing-126-session SPUS beta before ranking
63-day/skip-2 momentum, applies a decision-time 21-session dollar-volume top-60 gate, then holds the
weekly top five with inverse-volatility weights and a 25% name cap. Its 3×3 plateau is
`momentumDays {42,63,84} × betaLookbackDays {84,126,168}` and the related-family DSR count is 99.
Terminal replay remains fail-closed until complete SPUS membership, lifecycle, independent Sharia
evidence, and removed-name bars exist. Fresh confirmation must use never-inspected forward evidence
after the frozen 126-session warm-up beginning 2026-07-23.
The sealed, zero-run manifest is tracked at
`docs/quant-experiments/halal-residual-fast-momentum-core-v1.json`.

## 2026-07-22 causal-TCN registration (pipeline only; no performance run)

`halal-causal-tcn-alpha@v1` is a frozen, approximately 4.7K-parameter causal temporal convolutional
network. It scores five-session beta-residual alpha from 126 sessions of decision-time price,
volatility, drawdown, and liquidity features. The offline Python lane owns only feature contracts,
training, and hash-pinned scores; the existing TypeScript portfolio path retains the deterministic
Sharia veto, top-60 liquidity gate, long-only top-five sizing, 25% name cap, costs, fills, and risk
breakers. The governed protocol is five expanding purged folds (minimum 504 training sessions,
126-session validation/roll, five-session embargo), a 3×3 learning-rate/dropout plateau, and 108
related-family trials. Synthetic smoke tests are explicitly non-promotable. No real dataset was
materialized, no diagnostic or FULL run occurred, and no performance was inspected because local
history lacks sufficient PIT membership, lifecycle, removed-name bars, and independent Sharia
lineage. The 2026-08-06 trust-hardening pass binds data, folds, weights, artifacts, predictions,
runtime, and the exact sealed configuration hash; 24 adversarial Python tests pass, but this remains
pipeline evidence rather than return evidence. The sealed manifest is
`docs/quant-experiments/halal-causal-tcn-alpha-v1.json`.

## 2026-08-06 SPUS volatility-managed beta registration (forward only; no performance run)

`halal-spus-vol-managed-beta@v1` isolates the strongest reusable risk lesson from the rejected
historical studies without reusing their contaminated stock-selection OOS: hold only SPUS, measure
population volatility from 42 close-to-close log returns every fifth completed session, and target
`min(100%, 10% / realized annual volatility)` with the balance in non-interest-bearing cash. The
frozen 3×3 plateau is lookback `{21,42,63}` × volatility target `{8%,10%,12%}` and the related-family
trial count is 108. It has **no diagnostic, historical, or FULL result** and makes no CAGR claim.
Terminal execution is hard-blocked before data access/simulation until the exact sealed manifest has
an independent ready decision and an immutable point-in-time SPUS Sharia/fund-lifecycle evidence root
covers the forward window. Even then, one terminal evaluation cannot occur before 504 new sessions
and 100 non-overlapping five-session book returns after `2026-08-07T20:00:00Z`.

## Active catalog

| id | tier | hypothesis (one line) | params ver | status | trades | expectancy/trade (net) | dSharpe | maxDD p95 | P(day≥5%) | next action |
|---|---|---|---|---|---|---|---|---|---|---|
| gapper-orb | T1 | Micro-cap gappers (mcap 10–400M, PM>5%, day>5%, cumVol≥10M consol. / 350k IEX) continue after a 5-min opening-range breakout | v1 (consol.) · v1-iex (minCumVolume 350k) | REJECTED | 76 (G3d PIT micro-cap run; earlier 20-sym run: 0) | −4.19%/trade (net) | 0.051 | MC p95 99.80% | 0.00% (0 days) | `NEGATIVE_EXPECTANCY`, `DRAWDOWN_RISK_FAILURE`, `INSUFFICIENT_SAMPLE`, `SHARIA_UNVERIFIABLE`; long gap-continuation on real micro-cap gap days LOSES (hit 31.6%, jitter p=0.986) — the documented edge in this pattern is substantially short-side (Sharia-excluded); a v2 must change the hypothesis (Stocks-in-Play relative-volume + trend filters), never retune v1; both runs' cards below |
| stocks-in-play-orb | T1 | Zarattini SiP: relative-volume ≥3× + trend alignment on LIQUID names + 5-min ORB, long-only (NEW hypothesis after gapper-orb rejection) | v1 | REJECTED | 468 | −0.24%/trade (net) | 0.002 | MC p95 81.4% | 0.00% (5,313 days) | `NEGATIVE_EXPECTANCY`, `DRAWDOWN_RISK_FAILURE`; decisive sample (468 trades, 2y real minute bars, 11 liquid names): hit 38.9%, CAGR −45.7%, jitter p=0.999, ruin 96.1% — the published long-side ORB edge did NOT replicate on this universe/feed/cost model; card below |
| htf-trend-filter | T1 | Long entries only above the 1H 100-EMA improve every other setup's expectancy (composable filter, not standalone) | v1 | CANDIDATE | – | – | – | – | – | codify; A/B via harness |
| vwap-reclaim | T1 | Long wick + volume + no-follow-through at regular-session VWAP marks absorption; next-bar confirmation continues up | v1 (regular-only VWAP, 1.5× wick/body, 1.5× trailing-20 volume, actual-fill 2R, next-open fills) | REJECTED | 347 | −0.29%/trade (net) | 0.000 | MC p95 67.43% | 0.00% (5,313 days) | `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; hit 17%, CAGR −40.62%, OOS −44.90%, ruin 100%, jitter p=1.000 — decisive negative expectancy; do not test premarket v1.1 because the a-priori branch required non-negative v1 expectancy |
| rsi-exhaustion-long | T1 | RSI(2–14)≤10 + first reversal candle mean-reverts up (long side only) | v1 | CANDIDATE | – | – | – | – | – | codify after G1 |
| stop-hunt-reversal-long | T1 | Fake break below prior-day low + hard reclaim = stop sweep; continuation up | v1 (0.2% sweep depth, 15-min reclaim, stop=sweep low, target=max(2R,VWAP), 09:35–15:00 ET, inverse-vol sizing) | REJECTED | 887 | −0.33%/trade (net) | 0.000 OOS | MC p95 97.0% | 0.00% | `NEGATIVE_EXPECTANCY`, `DRAWDOWN_RISK_FAILURE`, `SHARIA_UNVERIFIABLE`; decisive 2y/11-name run (seed 42, gitSha ed51992, BacktestRun in results/stop-hunt-reversal-long-2024-08-05-2026-07-10.json): hit 26.9%, OOS CAGR −90.8%, ruin 100%, jitter p=1.000, walkForward earned, sharia UNSCREENED_EXECUTION_BLOCKED — 7th intraday long-only falsification on this feed/cost model |
| bagholder-bounce | T1 | Gap-down ≥20% + flush + first higher low bounces (long) | v1 (20% gap; 5% flush; first higher-low reclaim; 09:45–11:00 ET; 2R; seed 42) | REJECTED | 0 | 0.00% | **0.000 full / 0.000 OOS** | 0.00% (no trades) | 0.00% (682 symbol-days) | `INSUFFICIENT_SAMPLE`, `OOS_FAILURE`, `DSR_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; 247,565 real Alpaca-IEX bars over 62 captured days/name, but no ≥20% gap + 5% flush + first-higher-low event. FULL `cecc9d4` / `b98e0145-0ba5-4ac9-bd49-44424af8cb37`; no retune/admission. |
| time-of-day | T1 | 9:45 reversal + first-hour trend lock as entry-window constraints improve fills | v1 (A: ≥1% drop then first reclaim, 09:45–10:00; B: first-hour return ≥1%, first high/VWAP breakout 10:30–11:00; 2R; seed 42) | REJECTED | 262 | **−0.23%/trade net** | **0.020 full / 0.448 OOS** | MC **trade-path** p95 **65.45%** | 0.00% (682 symbol-days) | `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; full/OOS CAGR −7.08%/−30.28%, hit 34.35%, ruin 47.0%, permutation p=0.992. FULL `6fde71d` / `53430eca-132e-48a0-aa77-54f75e64dede`; no retune/admission. |
| coint-statarb-long-leg | T2 | Engle–Granger cointegrated halal pairs: long the undervalued leg at spread ≥ k·σ, exit at mean (short leg Sharia-vetoed, never traded) | v1 (252d formation, k=2, EG-5% ADF≤−3.34, mean-reversion exit) | REJECTED | 9 | +2.09%/trade (net, 9-trade artifact — non-informative) | 0.823 full / 0.005 OOS | 20.52% | 0.00% | `INSUFFICIENT_SAMPLE`, `SHARIA_UNVERIFIABLE`; 9 ≪ 100 trades — only ~10 halal names carry ≥252d shared real history, and the strict EG cointegration gate + k=2 entry rarely fires; results in card below; v2 is a NEW candidate, not a retune |
| bollinger-mr-long | T2 | Buy lower Bollinger band in stationary regimes (Chan ch.2–3); Kalman spread variant later | v1 (20d band, 2σ, VR≤1 gate) | REJECTED | 151 | +1.10%/trade (net, full-history artifact) | 0.954 full / **0.283 OOS** | **75.4%** | 0.00% | `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; exact results in card below; v2 is a NEW candidate, not a retune of v1 |
| bollinger-mr-long-v2 | T2 | v1's lower-band MR entry/exit family + TWO diagnosed structural fixes: inverse-vol per-name sizing (∝1/σ) and a stricter regime gate (VR≤0.85 AND close>200d SMA, uptrend-only) | v2 (20d/2σ band; VR≤0.85; 200-SMA uptrend; 60d realized-vol sizing, 0.4%/day budget, 20% cap) | REJECTED | 240 | +0.189%/trade (net) | 0.997 full / **0.931 OOS** | MC p95 14.14% | 0.00% | Corrected-measurement re-run (seed 42, params-identical): `OOS_FAILURE`(walk-forward flag only), `DSR_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. The implausible/PIT/repro codes were harness **measurement artifacts**, now removed — corrected annualized Sharpe **1.02** (was 3.06 under a trade-sequence √252 bug). The two structural fixes still WORKED on the risk axis (OOS CAGR **+5.13%** vs v1's −32.3%; maxDD 52%→**10%**; MC p95 75%→**14%**; ruin 15%→**0%**; jitter **p=0.001**); the binding miss is now OOS DSR **0.931 < 0.95** + no plateau. Do NOT tune to dodge the flag; two cards below (original run + corrected-measurement re-run) |
| bollinger-mr-long-v3 | T2 | v2's rule family and sizing unchanged, expanded to C1's verified top-100-by-dollar-volume sleeve in one shared-cash book | v3 (v2 params unchanged; max 6; center + 4 frozen v2 neighbors) | REJECTED | 625 | +0.54%/trade net | **0.373 full / 0.388 OOS** | MC **book-day** p95 **58.45%** | 0.14% (2,145 book-days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; breadth cleared the sample floor and fixed plateau instability, but tail drawdown and 5.5% ruin failed. C1 VERIFIED_COMPLIANT cleared Sharia. FULL `d4814b6` / `4b2d4085-dc96-47c2-9a33-471eae80dd48`; no retune/admission. |
| ts-momentum-halal-basket | T2 | Long-only time-series momentum (252d & 63d dual) with vol targeting on halal mega-cap tech basket (compliant NQ proxy) | v1 (252/63 dual mom, 100-EMA-break OR 63d≤0 exit) | REJECTED | 231 | +0.72%/trade (net, full-history artifact) | 0.924 full / **0.186 OOS** | **81.4%** | 0.01% | `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; exact results in card below; v2 is a NEW candidate |
| ts-momentum-halal-basket-v2 | T2 | v1's dual-momentum (252d & 63d) entry/exit FAMILY unchanged + TWO diagnosed structural fixes: inverse-vol per-name sizing (∝1/σ, reused bollinger-mr-long-v2 helper) and a market-regime kill-switch (basket equal-weight index > its OWN 200d SMA gates NEW entries; open positions exit by v1 rules) | v2 (252/63 dual mom, 100-EMA-break OR 63d≤0 exit; 60d realized-vol sizing, 0.4%/day budget, 20% cap; 200d basket-index regime gate) | REJECTED | 1334 | +0.112%/trade (net) | 0.999 full / **0.894 OOS** | MC p95 **37.25%** | 0.00% | Corrected-harness run (seed 42, a-priori params, NO sweep): `DRAWDOWN_RISK_FAILURE` (MC p95 37.25% > 30% breaker — binding merit miss), `DSR_FAILURE` (OOS 0.894 < 0.95), `OOS_FAILURE` (walk-forward flag only; OOS CAGR is **+16.65%**), `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. The two fixes WORKED as diagnosed: v1's OOS collapse is GONE (OOS CAGR −45.2%→**+16.65%**; MC p95 81.4%→**37.25%**; ruin 24.2%→**0%**; jitter p=0.108→**0.002**), and the corrected harness measures an HONEST Sharpe **0.90** (no implausible flag). Binding misses are now tail drawdown just over the breaker + OOS DSR just under 0.95. Do NOT retune to dodge flags; card below |
| ts-momentum-halal-basket-v3 | T2 | v2 signal family unchanged + portfolio-level trailing-60d basket-vol governor (15% annual target, down-only to cash) and six-position cap on the true shared book | v3 (v2 params unchanged; basket vol 60d/15%; max 6; 9 frozen validation trials) | REJECTED | 1,637 | +11.374%/closed-position trade net (OOS +13.256%) | **0.414 full / 0.375 OOS** | MC **book-day** p95 **49.91%** | 0.00% (2,140 book-days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `SHARIA_UNVERIFIABLE`; OOS CAGR +11.83% and the fixed 3×3 plateau PASS are positive but do not promote: DSR(9) misses 0.95, book-day MC p95 exceeds 30%, and AAOIFI execution evidence is absent. Final QA-safe replay: `d3d0071` / `f9e5f702-253b-4a9d-bdec-d89144038ecb`; prior `f9d788b` run is retained below but superseded. AUTO_PAPER blocked. |
| ts-momentum-halal-basket-v4 | T2 | v3 signal family unchanged + a-priori peak-to-trough exposure governor, using C1's verified top-100-by-dollar-volume sleeve | v4 (v3 params unchanged; full exposure through 8% DD, linear de-risk to cash at 20%; 9 frozen trials) | REJECTED | 1,693 | +12.402%/closed execution record net | **0.556 full / 0.428 OOS** | MC **book-day** p95 **41.49%** | 0.00% (2,145 book-days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`; realized maxDD improved to 18.31%, but MC tail risk stayed above 30% and one frozen neighbor degraded 66.2%. C1 state VERIFIED_COMPLIANT removed the parent's Sharia failure. FULL `d00c79b` / `9fd3f2d5-274c-492a-b91e-a524d2c157e3`; no retune or AUTO_PAPER. |
| silver-swing | T2 | Swing long on silver exposure — instrument fiqh-contested (ribawi/spot rules) | v1 | CANDIDATE — WAITING_SHARIA_REVIEW | – | – | – | – | – | No validation or execution until Sharia agent adjudicates the instrument charter (e.g. miners' equities) |
| dual-momentum-rotation | T2 | Antonacci dual momentum on declared sleeve {SPUS, HLAL, AAPL, MSFT, NVDA, GOOGL, AMZN}: 12−1M relative rank + 12M absolute filter, monthly, cash when absolute≤0 | v1 (252d/skip21; threshold 0; fixed 25% continuous cap; max 1; 9 trials) | REJECTED | **11 closed position episodes** (3 OOS; 147 raw sell executions audited separately) | **+16.630%/episode net** (OOS plateau center +47.727%) | **0.737 full / 0.701 OOS** | MC **book-day** p95 **23.15%** | **0.0467%** (2,140 days) | `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; 11≪100 independent episodes, OOS DSR(9) and plateau fail, AAOIFI unscreened. Final `0aef451` / `13a95084-5890-4d17-92f5-c01d913e2d5c`; AUTO_PAPER blocked; next R3-3. |
| multi-mode-book-v1 | T2 | One shared-cash book combining disjoint C1-screened momentum, mean-reversion, and dual-momentum sleeves through the existing capped allocator | v1 (seed 42 allocator: 20%/40%/40%; signal-cadence rebalance; 3 trials) | REJECTED | 428 | +2.96%/trade net | **0.888 full / 0.729 OOS** | MC **book-day** p95 **46.40%** | 0.19% (2,145 book-days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; OOS CAGR 21.46%, plateau PASS, permutation p=0.001, ruin 0.5%, and C1 verification are positive, but the two hard gates failed. FULL `455d8ea` / `8aed13a7-c560-4d92-9d18-9e5c647c2fe0`; no retune/admission. |
| multi-mode-book-v2 | T2 | v1 sleeves/allocator UNCHANGED + one a-priori book-level drawdown-responsive exposure governor (linear 1.0@≤10% book DD → 0.0@≥25% book DD, on running peak-to-trough NAV, new sizes only; de-risking sells always allowed) attacking E3's `DRAWDOWN_RISK_FAILURE` | v2 (v1 params unchanged; governor {10%,25%}; frozen 3×3 plateau {8,10,12}×{20,25,30}; 3 trials) | REJECTED | 444 | +3.17%/trade net | **0.860 full / 0.706 OOS** | MC **book-day** p95 **40.06%** | 0.10% (2,145 book-days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; the governor attacked the right axis and moved the tail the right way (MC p95 46.40%→**40.06%**, realized maxDD 22.90%→**22.01%**) but not under the 30% gate, and OOS DSR 0.729→0.706 did not clear 0.95; OOS CAGR 21.46%→**15.47%** (governor trims exposure in drawdowns), plateau **PASS**, jitter p=0.000, ruin 0.10%, C1 `VERIFIED_COMPLIANT`. FULL `a38ed59` / `60079932-1655-449b-b4ba-859aacc6dba8`; no retune/admission. |
| multi-mode-book-v3 | T2 | v2 sleeves/allocator/governor UNCHANGED + one structural substitution (zero new tunable params): idle/absolute-filter/governor-de-risked capital parks in the SPSK sukuk ETF instead of cash (fill next open, 15 bps/side, ADV-capped; SPSK sold FIRST before equity entries, exits first; pre-2019-12-31 cash fail-closed) — attacks v2's `DSR` (sukuk carry) + `DRAWDOWN` (low equity correlation) codes | v3 (v2 params UNCHANGED; SPSK ballast; frozen 3×3 {8,10,12}×{20,25,30}; 3 trials) | REJECTED | 204 (0 OOS closed) | +3.43%/equity trade net (book NAV NEGATIVE) | **0.150 full / 0.001 OOS** | MC **book-day** p95 **50.65%** | 0.00% / 0.05% (2,145 book-days) | `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`; the sukuk ballast FALSIFIED the hypothesis on BOTH binding axes — SPSK **raw-close** carries no coupon in the price data (20.10→17.92) so parking idle/de-risked cash there DRAGGED returns (OOS CAGR **+15.47%→−7.75%**, full **15.47%→−1.08%**) and the governor now de-risks into a FALLING instrument, not cash, WORSENING the tail (MC p95 **40.06%→50.65%**, realized maxDD **22.01%→39.22%**, plateau **PASS→SPIKY**). Equity sleeves themselves were fine (jitter p=0.004, +3.43%/trade). C1 `VERIFIED_COMPLIANT` (16 equity names); SPSK `FUND_LEVEL_PURIFICATION_ONLY` paper-research ballast. FULL `d562dc1` / `367faa48-6c8c-4155-8de7-febba2201fd6`; no retune/admission. |
| nvda-focus-v1 | T2 | USER DIRECTIVE single-stock focus: {NVDA} only, dual time-series momentum (252d skip-21 AND 63d, hold-both-positive, exit either ≤0), 100% single-name sizing, 10%→25% drawdown governor | v1 (252/skip21 & 63; threshold 0; governor {10%,25%}; 100% single-name; frozen 3×3 {231,252,273}×{42,63,84}; 9 trials) | REJECTED | **42 closed-position episodes** (23 OOS; 86 raw sells / 44 partial governor trims audited separately) | **−0.94%/episode OOS** (full/OOS CAGR 15.87%/24.41% carried by few big winners, hit 29.6%) | **0.856 full / 0.533 OOS** | MC **book-day** p95 **44.71%** | 0.51% (2,145 days) | `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`; one-stock focus can NOT honestly serve the daily target — 42≪100 episodes (no decisive sample), concentration drives MC p95 44.71%>30% at just 27.5% mean exposure, OOS DSR<0.95, center OOS episode expectancy −0.94%. C1 `VERIFIED_COMPLIANT`, jitter p=0.019, ruin 0.10%, reproducible. FULL `14a4501` / `08aa5aab-f7ea-4b3c-93dd-215a4a568481`; card below; no retune/admission. |
| tom-overlay | T2 | Turn-of-month: hold the SPUS research proxy only across last-4+first-3 observed trading sessions, cash otherwise | v1 (4+3; fixed 25% cap; max 1) | REJECTED | 79 episodes (full 2018–2026) | −0.12% CAGR full | 0.005 OOS DSR | MC book-day p95 13.9% | 0.00% | `NEGATIVE_EXPECTANCY`, `DSR_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; terminal FULL run (user-authorized, seed 42) + 3Y view (36 eps, −0.71%) + 1Y view (12 eps, +0.32%, non-promotable evidence view) all flat/negative — the 30-country TOM anomaly does NOT replicate on the halal sleeve net of costs; period views prove no window rescues it; results/tom-overlay-*.json |
| g6b-linear-factor | T2 | Monthly top-quartile linear cross-sectional rank on 12−1M momentum plus trailing-21-session dollar-volume proxy; seven equal target weights | v1 (252/skip21; 50/50 ranks; top 25%; 9 trials) | CODIFIED — DIAGNOSTIC ONLY | **18 active monthly book observations** (10 OOS; 63 raw closed-name executions audited separately) | +1.74%/active month (diagnostic only) | **0.303 full / 0.195 OOS** | MC **book-day** p95 **42.17%** | 0.16% (631 days) | Short 2024-01-02..2026-07-10 diagnostic only: CAGR 11.39% / 15.24%; `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; clean-replay flag false from unrelated pre-existing WIP. True turnover unavailable and proxy explicitly labeled; no artifact/run or terminal verdict; full evidence awaits explicit user authorization. |
| pead-long-liquid | T2 | Post-earnings drift, long-only liquid names | – | PARKED — EVIDENCE | – | – | – | – | – | 2024 literature: 0.04–0.14%/mo for liquid long-only (edge is illiquid+short side); Q4-2025 drift ≈ half historical — do not build without new evidence |
| g6b-linear-factor-wide | T2 | R3-4: v1's linear factor at BREADTH — any-equities universe, rankable = ≥253 bars + close ≥$5 + 21d dollar-volume proxy ≥$21M, breadth floor 100 names else cash, top decile capped at 50 equal weights | v2 a-priori (`7d391ac`; integrity fixes through `ff73b4f`) | REJECTED | **90 active book-months** (31 OOS; 1,666 fills / 827 name-exits audited separately) | +0.623%/active month net; CAGR **5.71%** / OOS **0.00%** | DSR **0.374 / OOS 0.000** | MC book-day p95 DD **54.86%** | P(day≥+5%) **0.32%** / P(day≤−5%) **0.32%** (2,164 days) | `INSUFFICIENT_SAMPLE`, `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`; the 2Y plateau PASS was regime-specific and collapsed to zero OOS expectancy across every FULL neighbor. FULL `ff73b4f` / run `fff3d286`; 2,767 symbols, 4,481,246 resolved real bars, 2 MOCK excluded. No AUTO_PAPER and no retune. |
| halal-risk-parity-core | T2 | R-RP1: NEW mechanism class (no directional signal) — monthly inverse-volatility (weight ∝ 1/σ, 252d) risk-parity weighting over the C1-verified 40-name dollar-volume sleeve, 20% per-name cap, 15-name breadth floor, testing whether genuine breadth + vol-aware sizing (not a better timing signal) fixes the recurring concentration-driven tail-drawdown failure | v1 (lookback 252d; cap 20%; floor 15; maxNames 40; 9 frozen trials) | REJECTED | **90 active book-months** (32 OOS; 1,631 raw closed-name executions / 3,414 fills audited separately) | +2.270%/trade net (permutation mean); OOS plateau center +2.727%/active month; CAGR **24.64%** / OOS **39.62%** | DSR **0.958 full / 0.898 OOS** | MC book-day p95 DD **45.90%** | P(day≥+5%) **0.37%** / P(day≤−5%) **0.37%** (2,145 days) | `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; the hypothesis partially worked — MC p95 tail drawdown fell to 45.90% (vs the 37-59% range typical of every prior 1-6 position concentrated setup) and the 3×3 plateau PASSED (plateau_confirmed, all 8 neighbors same-sign, max degradation 8.8%) — but 90 book-months (<100) and OOS DSR 0.898 (<0.95) still bind, and 45.90% remains above the 30% breaker. FULL `6e5a095` / run `214b6a94-9437-4a18-bf9d-efe42652d197`; no retune/admission. |
| halal-markowitz-core | T2 | R5: a structurally NEW mechanism (covariance-aware Markowitz tangency weighting) over the C1-verified top-40-by-dollar-volume halal sleeve, monthly rebalance, 20% per-name cap, testing whether diversification tames the tail where every prior single-signal concentrated bet (1-6 names) could not | v1 a-priori (`9e2eafe`; frozen 3×3 {231,252,273}×{0.15,0.20,0.25}; 9 trials) | REJECTED | **90 active book-months** (32 OOS; 3,414 fills / 1,644 name-exits audited separately) | +2.84%/active month net | **0.970 full / 0.899 OOS** | MC book-day p95 DD **50.78%** | P(day≥+5%) **0.56%** / P(day≤−5%) **0.70%** (2,145 days) | `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; the hypothesis is FALSIFIED on the exact axis it was designed to test — MC p95 book-day drawdown (50.78%) is WORSE than several concentrated single-signal setups it was meant to beat, because the C1-verified sleeve resolves to 40 highly-correlated mega-cap tech/growth names (NVDA/AAPL/TSLA/AMD/MSFT/GOOGL/AVGO…) that all fall together in systematic drawdowns (2022) — idiosyncratic diversification cannot offset dominant beta/factor risk. Plateau PASSED (robust 231-273d × 15-25% grid, all same-sign, within bound) — a genuine first for a multi-name T2 setup — but OOS DSR 0.899 falls just short of 0.95, and 90 active months < 100. FULL `9e2eafe` / run `354b784e-a43f-4694-9292-acf792927eaa`; no retune/admission. |
| halal-momentum-risk-parity-core | T2 | R6: COMBINES the proven dual-momentum SELECTION signal (top 50% by 12−1M relative rank among names with positive absolute momentum, `dualMomentumRotation`'s exact formula) with the proven inverse-volatility risk-parity SIZING mechanism (both reused UNCHANGED) over the C1-verified top-60-by-dollar-volume halal sleeve, testing whether layering a real directional signal on the best-yet diversification mechanism clears the gates neither single-mechanism sibling could | v1 a-priori (`fe224b9`; lookback 252d/skip21; selectionFraction 0.5; cap 20% fixed; floor 15; maxNames 60; frozen 3×3 {231,252,273}×{0.4,0.5,0.6}; 9 trials) | REJECTED | **89 active book-months** (32 OOS; 1,072 raw closed-name executions / 2,073 fills audited separately) | +2.107%/trade net (permutation observed mean); CAGR **21.46%** / OOS **51.17%** | DSR **0.836 full / 0.753 OOS** | MC book-day p95 DD **60.18%** | P(day≥+5%) **0.61%** / P(day≤−5%) **1.03%** (2,145 days) | `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; the hypothesis is FALSIFIED on BOTH axes it was built to fix — MC p95 tail drawdown (60.18%) and OOS DSR (0.753) are WORSE than both single-mechanism siblings (halal-risk-parity-core 45.90%/0.898, halal-markowitz-core 50.78%/0.899), not better; momentum selection concentrated the book into a correlated semiconductor/AI-rally cluster (NVDA/LITE/MU/KLAC/AMD dominate P&L) that crashes together, undermining rather than reinforcing risk-parity's diversification. FULL `fe224b9` / run `146d4ed7-306f-4999-9943-f81f03eb1334`; no retune/admission. |
| halal-sector-capped-risk-parity-core | T2 | R7: ISOLATED A/B — the SAME `halal-risk-parity-core` mechanism (identical 40-name C1-verified sleeve, 252d lookback, 20% per-name cap, inverse-vol weighting), with ONE new variable added: a 30% sector-exposure water-filling cap applied BEFORE the per-name cap, testing whether constraining WHAT sectors can be held (not just reweighting within the existing correlated universe) improves the MC tail/DSR where three prior reweighting-only mechanisms could not | v1 a-priori (`70c2fa6`; lookback 252d fixed structure; sectorCap 0.30; perNameCap 20% fixed; floor 15; maxNames 40; frozen 3×3 {231,252,273}×{0.25,0.30,0.35}; 9 trials) | REJECTED | **90 active book-months** (32 OOS; 1,632 raw closed-name executions / 3,417 fills audited separately) | +2.232%/trade net (permutation observed mean); OOS plateau center +2.711%/active month; CAGR **24.22%** / OOS **39.26%** | DSR **0.958 full / 0.900 OOS** | MC book-day p95 DD **44.95%** | P(day≥+5%) **0.37%** / P(day≤−5%) **0.37%** (2,145 days) | `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; the isolated sector cap moved BOTH target metrics in the hypothesized direction but only MARGINALLY — MC p95 44.95% vs baseline `halal-risk-parity-core` 45.90% (−0.95pp) and OOS DSR 0.900 vs 0.898 (+0.002) — both still fail their gates (44.95%>30% breaker, 0.900<0.95). Plateau PASSED (plateau_confirmed, all 8 neighbors same-sign, max degradation 6.3%). FULL `70c2fa6` / run `9dfa79cd-5498-4379-86d0-b3c46e0621ef`; no retune/admission. |
| halal-sector-capped-risk-parity-wide | T2 | Breadth follow-up to R7 (LAST test in this in-universe research line): the SAME sector-capped risk-parity mechanism (`capSectorExposure` + `sectorCappedInverseVolatilityWeights`, imported unchanged), same 252d lookback / 30% sector cap / 20% per-name cap, over the QDR-8 ceiling of 100 names instead of the narrow 40-name isolated A/B sleeve (which was 17/40=42.5% Semiconductors) — tests whether real breadth gives the cap genuine sector alternatives to redistribute into | v1 a-priori (`79ad4c3`/`48d360e`; lookbackDays 252 fixed structure; sectorCap 0.30; perNameCap 20% fixed; floor 15; maxNames 100; frozen 3×3 {231,252,273}×{0.25,0.30,0.35}; 9 trials) | REJECTED | **90 active book-months** (32 OOS; 4,119 raw closed-name executions / 8,716 fills audited separately) | +1.713%/trade net (permutation observed mean); OOS plateau center +1.531%/active month; CAGR **18.20%** / OOS **23.18%** | DSR **0.912 full / 0.802 OOS** | MC book-day p95 DD **42.26%** | P(day≥+5%) **0.33%** / P(day≤−5%) **0.28%** (2,145 days) | `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; breadth MEANINGFULLY improved MC p95 (42.26% vs narrow 44.95%, −2.69pp — ~3× the narrow A/B's own −0.95pp effect, and −3.64pp vs the `halal-risk-parity-core` baseline's 45.90%) but OOS DSR got WORSE, not better (0.802 vs narrow 0.900 / baseline 0.898) — the hypothesis is PARTIALLY confirmed (drawdown axis) and PARTIALLY falsified (DSR axis). FULL `48d360e` / run `1e12d743-7253-40c3-834e-fb5a32362bc3`; no retune/admission. |
| halal-concentrated-momentum-core | T2 | P1 — USER-DIRECTED PIVOT (after 5/5 diversification-focused REJECTs this session): maximize realized CAGR via genuine CONCENTRATION — fixed top-10 WEEKLY momentum selection (126d lookback/skip5, faster than the monthly family's 252/21) sized by the SAME `inverseVolatilityWeights`/`capAndRedistribute` mechanism (25% per-name cap), over the C1-verified top-60-by-dollar-volume halal sleeve; user explicitly accepts a risk profile that may not clear QDR-6 — Sharia compliance stays completely non-negotiable and the gate itself is unchanged | v1 a-priori (`aee92d1`; lookbackDays 126/skip5; topN 10; cashFloor 3; perNameCap 25%; maxNames 60; frozen 3×3 {105,126,147}×{8,10,12}; 9 trials) | REJECTED | **2,317 trades** (726 OOS; 4,828 fills audited separately; trade-level observation unit, not monthly-collapsed) | +17.26%/trade net (permutation observed mean); CAGR **33.84%** / OOS **69.66%** | DSR **0.929 full / 0.750 OOS** | MC book-day p95 DD **64.68%** | P(day≥+5%) **1.63%** / P(day≤−5%) **1.91%** (2,145 days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; the pivot hypothesis is CONFIRMED on both predicted axes — OOS CAGR **69.66%** is the HIGHEST of six T2 mechanisms this session (beats `halal-markowitz-core`'s 56.57%), but MC p95 **64.68%** is the WORST (beats `halal-momentum-risk-parity-core`'s 60.18% record) — concentration bought CAGR at exactly the tail-risk cost predicted a priori. Trade-level sample (2,317) cleared the sample floor outright, unlike every monthly-book sibling's ~90 active months. FULL `aee92d1` / run `01772bc9-7988-4f90-b7e9-3ff0ad5063d2`; no retune/admission. |
| halal-managed-momentum-core | T2 | P2 — PRE-REGISTERED tail-risk attack on `halal-concentrated-momentum-core`'s own diagnosed flaw: BYTE-IDENTICAL weekly top-10 126d/skip5 momentum selection + inverse-vol sizing (25% cap), plus THREE down-only, literature-standard momentum-crash mitigants stacked on top — (1) vol targeting (Barroso & Santa-Clara 2015, 60d lookback/20% target, engine-native), (2) a book drawdown governor (15%→35% linear to cash, engine-native), (3) a market-regime filter (Daniel & Moskowitz 2016 / Faber 2007: sleeve equal-weight index > its own 200d SMA gates new entries, else whole book holds cash — reusing `ts-momentum-halal-basket-v2`'s proven equal-weight-index/SMA logic). Tests whether the 64.68% MC p95 tail can be cut substantially while keeping most of the 69.66% OOS CAGR | v1 a-priori (`50c7cf9`; lookbackDays 126/skip5/topN 10/cashFloor 3/perNameCap 25% unchanged from baseline; realizedVolLookback 60/targetAnnualVol 20%/drawdownStart 15%/drawdownCash 35%/regimeSmaPeriod 200; frozen 3×3 {0.15,0.20,0.25}×{150,200,250}; 9 trials) | REJECTED | **5,675 trades** (2,334 OOS; trade-level observation unit, same precedent as the concentrated baseline) | +25.35%/trade net (permutation observed mean, mean/trade); CAGR **20.14%** / OOS **44.45%** | DSR **0.868 full / 0.733 OOS** | MC book-day p95 DD **50.70%** | P(day≥+5%) **0.42%** / P(day≤−5%) **0.47%** (2,145 days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`; the three down-only governors moved BOTH axes in the predicted direction but only PARTIALLY — MC p95 fell **64.68%→50.70%** (−13.98pp) while OOS CAGR fell **69.66%→44.45%** (−25.21pp, retaining ~64% of the baseline's OOS CAGR) — a real but not decisive trade, and the frozen plateau came back **SPIKY** (parameter-fragile) ⇒ `NO_PROFIT_PLATEAU_OVERFIT`. Lands WORSE on every axis than the pre-existing `halal-markowitz-core` diversification result (56.57% CAGR / 50.78% MC p95 / 0.899 DSR). Clean reproducible FULL run, gitSha `50c7cf9` / `BacktestRun ddda4001-9305-41c9-b93c-a34102f84563`; no retune/admission. |
| halal-momentum-markowitz-core | T2 | P3 — PRE-REGISTERED SYNTHESIS: the one untried corner all seven prior in-session results point to — momentum SELECTION (weekly top-20, 126d/skip5, `dualMomentumMetrics`/`selectTopNByMomentum` reused unchanged, the proven CAGR engine) combined with Markowitz covariance-aware SIZING (`markowitzFrontier` tangency weighting reused unchanged, the proven best-all-round diversified sizer) over the selected subset. Tests whether a correlation-AWARE sizer applied AFTER weekly momentum selection retains most of the concentrated engine's CAGR while taming the correlated-cluster tail risk that inverse-vol sizing (every momentum-selection sibling this session) could not | v1 a-priori (`47bc0d9`; lookbackDays 126/skip5; topN 20; cashFloor 10; perNameCap 25%; maxNames 60; markowitz seed 42/portfolios 10,000/riskFreeRate 0/tradingDaysPerYear 252; frozen 3×3 {15,20,25}×{105,126,147}; 9 trials) | REJECTED | **4,499 trades** (1,464 OOS; trade-level observation unit, same precedent as the concentrated baseline) | +1.548%/trade net (permutation observed mean); CAGR **25.65%** / OOS **52.95%** | DSR **0.884 full / 0.746 OOS** | MC book-day p95 DD **60.51%** | P(day≥+5%) **1.21%** / P(day≤−5%) **1.07%** (2,145 days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`; the synthesis is PARTIALLY confirmed — profit plateau **PASSED cleanly** (`plateau_confirmed`, all 8 neighbors same-sign, 2.29–5.16% OOS expectancy range) and OOS CAGR retains **~76%** of the concentrated baseline's (52.95% vs 69.66%), but the covariance sizer only trims MC p95 tail risk **modestly** (64.68%→**60.51%**, −4.17pp, a 6.4% relative reduction — far short of the diversified family's 42.26–50.78% range) — and the result lands **WORSE than `halal-markowitz-core` on all three axes simultaneously** (CAGR −3.62pp, MC p95 +9.73pp, DSR −0.153): the a-priori hope of beating it was NOT realized. Clean reproducible FULL run, gitSha `47bc0d9` / `BacktestRun 8869bef7-0be0-46f6-8338-ec54e487f01e`; no retune/admission. |
| halal-fast-momentum-core | T2 | P5 — ISOLATED velocity+concentration A/B testing `halal-trend-rider-core`'s REJECT finding (rotation velocity, not winner-trimming, is the edge) at its sensible max: the SAME weekly ISO-week `halal-concentrated-momentum-core` engine (`dualMomentumMetrics`/`selectTopNByMomentum`/`inverseVolatilityWeights`/`capAndRedistribute` all reused UNCHANGED), with ONLY three params changed — lookbackDays 126→63 (faster signal), skipRecentDays 5→2, topN 10→5 (tighter concentration), cashFloor scaled proportionally 3→2. Tests whether MORE rotation velocity + MORE per-rotation reallocation impact beats the concentrated baseline's 69.66% OOS CAGR ceiling | v1 a-priori (`000dea1`; lookbackDays 63/skip2; topN 5; cashFloor 2; perNameCap 25%; maxNames 60; frozen 3×3 {42,63,84}×{4,5,6}; 9 trials) | REJECTED | **1,390 trades** (410 OOS; 2,727 fills audited separately; trade-level observation unit) | +12.76%/trade net (permutation observed mean); CAGR **36.99%** / OOS **105.92%** | DSR **0.928 full / 0.874 OOS** | MC book-day p95 DD **68.40%** | P(day≥+5%) **2.33%** / P(day≤−5%) **2.19%** (2,145 days) | `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`; the rotation-velocity hypothesis is CONFIRMED — OOS CAGR **105.92%** is the HIGHEST of all ten mechanisms this session, **+36.26pp (+52.1% relative)** above `halal-concentrated-momentum-core`'s 69.66% ceiling — but MC p95 **68.40%** is also the WORST of the session, **+3.72pp** above that baseline's 64.68%: max velocity + max concentration bought materially more CAGR at a materially worse tail, exactly the accepted a-priori trade. Plateau **SPIKY** (`lookbackDays=42` neighbors degrade). FULL `000dea1` / run `218716ac-f692-4f0f-91f3-81045a159a62`; no retune/admission. |
| halal-residual-fast-momentum-core | T2 | Remove each name's SPUS common-return beta before the fast weekly rank, targeting stock-specific acceleration rather than beta-only momentum | v1 (63d/skip2 residual; beta 126; weekly PIT 21-session dollar-volume top-60 → residual top-5; 25% cap; 9 local / 99 family trials) | CODIFIED — BLOCKED, NO PERFORMANCE RUN | – | – | – | – | – | Complete historical SPUS membership, lifecycle, independent per-name Sharia evidence, and removed-name bars; then use never-inspected forward confirmation. Historical replay cannot confirm or supersede 105.92%. |
| halal-causal-tcn-alpha | T2 | A small causal TCN distinguishes persistent stock-specific momentum from crowded beta spikes using only decision-time path, volatility, drawdown, and liquidity evidence | v1 (126 sessions; 10 channels; 6 residual dilated blocks; 5-session beta-residual label; 9 local / 108 family trials) | CODIFIED — BLOCKED, PIPELINE ONLY, NO PERFORMANCE RUN | – | – | – | – | – | Build an independently trusted REAL_PIT materializer with historical membership/lifecycle/Sharia lineage and removed-name bars; then pass fresh READY_TO_RUN review before one never-inspected forward confirmation. Synthetic artifacts cannot promote or supersede 105.92%. |
| halal-spus-vol-managed-beta | T2 | Scale fixed SPUS beta down when realized volatility rises, testing volatility clustering without survivor-conditioned stock selection | v1 (42 log-return sessions; 10% annual-vol target; every 5th session; 9 local trials; QDR-9 **CONFIRMATORY N=1**, 108 family trials retained ONLY as the exploratory fallback if the six structural conditions are not proved) | CODIFIED — BLOCKED, FORWARD ONLY, NO PERFORMANCE RUN | – | – | – | – | – | QDR-9 re-seal `e2f9fe4d…` (2026-08-06, before the window opened): the original 504-session/100-observation window had a **provably empty acceptance set** (max attainable DSR 0.9312 < 0.95 at 108 trials, since clearing it needed Sharpe 3.13 > the 3.00 implausibility ceiling). Now declares hypothesized annualized Sharpe **0.80** and waits for **1085 new sessions / 216 non-overlapping five-session returns** (~4.3y). Capture immutable decision-time SPUS Sharia/fund-lifecycle evidence and independent READY_TO_RUN approval first. Historical inspection is forbidden and 105.92% is not superseded. |

## Terminal evidence cards

These result cards are binding finalizations under QDR-7. `n/a — not persisted` identifies an
artifact gap; it never authorizes an inferred or fabricated value.
Field mapping (QA 2026-07-12): "expectancy/trade (net)" in rows and cards = `permutation.observedMean`
in the results/*.json artifact — verified present and matching in all four round-1 artifacts.

**Full-checklist re-runs (2026-07-13, gitSha ed51992, acceptance capabilities live):**
- `bollinger-mr-long-v2` re-run 2018-01-02→2026-07-10 seed 42: core metrics unchanged (240 trades,
  +0.19%/trade, OOS DSR 0.931, MC p95 14.1%); walkForward EARNED (yes); Sharia persisted
  UNSCREENED_EXECUTION_BLOCKED; **profitPlateau EVALUATED → FAIL** — the edge does not survive its
  a-priori parameter neighborhood (entryStdev ±0.25, VR ±0.05). The prior "near-miss" framing is
  corrected: v1's rejection codes now include an EARNED `NO_PROFIT_PLATEAU_OVERFIT` — the edge is
  parameter-fragile. Any v3 must produce a structurally different, plateau-robust rule set.
- `ts-momentum-halal-basket-v2` re-run same window/seed: core unchanged (1,334 trades, +0.11%/trade,
  OOS DSR 0.894, MC p95 37.2%); walkForward EARNED; Sharia persisted UNSCREENED_EXECUTION_BLOCKED;
  **profitPlateau EVALUATED → PASS** (robust across regimeSmaPeriod ±20, targetVolBudget ±0.001).
  Momentum-v2 is now the book's strongest candidate: remaining binding gates are exactly two —
  MC maxDD p95 37.2% > 30% breaker and OOS DSR 0.894 < 0.95 — plus Sharia verification (needs a
  real screening source). v3 remedy stays a-priori basket-level vol targeting / concurrent-position cap.

### g6b-linear-factor-wide v2 — REJECTED

- **Reason codes (binding order):** `INSUFFICIENT_SAMPLE`, `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. FULL resolves all three pre-registered binds negatively: 90 active book-month observations are below 100; OOS DSR is 0.000 versus the 0.95 gate; MC book-day p95 drawdown is 54.86% versus the unchanged 30% breaker. The 2Y plateau PASS was regime-specific — FULL center and all eight frozen neighbors have zero OOS expectancy, so the terminal plateau fails.
- **Scope/compliance:** one completed FULL run, 2018-01-02..2026-07-15, monthly decisions on a shared-cash daily book; 2,767-symbol NASDAQ wide universe, 4,481,246 resolved real YAHOO/ALPACA bars with only the 1,172,162 bars of 603 ever-selected names retained by the engine; 2 MOCK rows observed and excluded. OOS holdout 30%. Any-equities breadth is `UNSCREENED_EXECUTION_BLOCKED`; no PIT AAOIFI source was available, so AUTO_PAPER is blocked independently of merit.
- **Full vs OOS:** full = 90 active book-months, CAGR 5.71%, Sharpe 0.411, DSR 0.374, maxDD 34.37%, hit 26.11%, expectancy +0.623%/active month net, 827 raw name exits, average exposure 27.10%, turnover notional $2,190,735.14. OOS = 31 active book-month observations, CAGR 0.00%, Sharpe/DSR/maxDD/hit 0.000 and raw turnover 0 as persisted. Pure IS, Calmar, and average win/loss = n/a — not separately persisted; values are not inferred.
- **Tail/integrity:** 2,164 book days; `P(day≥+5%)=0.32%`, `P(day≤−5%)=0.32%`; MC maxDD p5/p50/p95 = 20.12/32.91/54.86%; risk of ruin 3.00% versus 5% limit; seeded sign-flip permutation p=0.096. Walk-forward, 30% OOS, data/PIT integrity, clean reproducibility, and non-implausibility pass; the sample, DSR, plateau, and drawdown gates fail.
- **Expert finding:** the 2Y evidence view's 32.75% CAGR, 120.06% OOS CAGR, and first genuine plateau did not generalize across the full regime set. The full window dilutes the edge to 5.71%, the OOS slice is flat, and tail drawdown worsens. Per the anti-period-snooping rule, the FULL verdict binds; do not promote, retune v2 against this OOS result, or use the shorter window to rescue it.
- **Reproducibility:** `g6b-linear-factor-wide@v2`; params 252/skip-21 momentum + 21-session dollar-volume proxy at 50/50 weight, top decile capped at 50, breadth floor 100, close ≥$5, proxy ≥$21M, 9 frozen trials; 15 bps/side costs and the unchanged risk/ADV/cash envelope. Seed 42; git SHA `ff73b4f`; `BacktestRun` `fff3d286-d29a-4012-b925-f0e6668ea186`; result `results/g6b-linear-factor-wide-2018-01-02-2026-07-15-wide.json`; shared engine. The earlier OOM process produced no artifact or DB row and is not evidence.

### gapper-orb v1-iex — REJECTED

- **Reason codes:** `INSUFFICIENT_SAMPLE`, `SHARIA_UNVERIFIABLE`. The run produced 0 trades, so no statistical edge can be claimed; the 20-symbol convenience universe contained no joint qualifying days. Rejection binds v1-iex + this run only; G3d may validate a new PIT micro-cap sample without rewriting this card.
- **Scope/compliance:** 2026-04-13..2026-07-10, 1-minute intraday, 20 NASDAQ symbols × 62 trading days, real Alpaca-IEX bars; 30% OOS configured. Sharia state = `UNSCREENED_FOR_EXECUTION`: long-only cash charter, but the result artifact persists no per-signal AAOIFI verdict, so it cannot enter AUTO_PAPER.
- **IS/full vs OOS:** pure IS = n/a — zero trades; full CAGR 0.00%, DSR 0.000, maxDD 0.00%, hit rate 0.00%, expectancy n/a, turnover 0, exposure n/a — not persisted. OOS = 0 trades, CAGR 0.00%, DSR 0.000, maxDD 0.00%, hit rate 0.00%; Calmar and avg win/loss = n/a — no trades. These zeros are non-informative, not evidence of safety.
- **Tail/integrity:** MC p95 maxDD 0.00%, `P(day≥+5%)=0.00%` and `P(day≤−5%)=0.00%` over 0 observed return days, ruin 0.00%, permutation p=1.000; walk-forward=false, profit plateau=false, implausible=false. Funnel cause: mcap-in-band 128 / PM≥5% 24 / day≥5% 214 / IEX volume≥350k 884 / joint 0.
- **Reproducibility:** strategy/params `gapper-orb@v1-iex`; seed 42; git SHA `b1cc65c`; `BacktestRun` id = n/a — not exposed by result artifact; result `results/gapper-orb-2026-04-13-2026-07-10-pooled.json`; data feed `alpaca-iex`.

### stocks-in-play-orb v1 — REJECTED

- **Reason codes:** `NEGATIVE_EXPECTANCY`, `DRAWDOWN_RISK_FAILURE`. Decisive merit rejection on a real 468-trade sample — sample size is NOT the issue this time.
- **Scope/compliance:** 2024-08-05..2026-07-10, 1-minute intraday, 11 deep-history liquid halal NASDAQ names (~2y real Alpaca-IEX minute bars from R2-1), 30% OOS. Sharia state = `UNSCREENED_FOR_EXECUTION` (no per-signal PIT AAOIFI verdict persisted).
- **IS/full vs OOS:** full: 468 trades, CAGR −45.70%, Sharpe −2.61, DSR 0.002, maxDD 71.43%, hit 38.89%, expectancy −0.238%/trade net. OOS: CAGR −44.84%, Sharpe −3.32, DSR 0.013. P(day≥+5%)=0.00% and P(day≤−5%)=0.00% over 5,313 observed days.
- **Tail/integrity:** MC maxDD p5/p50/p95 = 53.04/71.21/81.41%, risk of ruin 96.10%, entry-jitter permutation p=0.999, Kelly 0.0000. Checklist (as persisted): enoughTrades=true; deflatedSharpe/profitPlateau/mcMaxDD/mcRuin=false; dataQualityPitOk=false, reproducible=false (new checklist fields persisted false by the run — artifact-faithful, see repro line).
- **Expert finding:** the credible published long-side intraday result (Zarattini ORB ≈19.6%/yr on SPY/liquid movers) did not replicate under this implementation: IEX-only fills, our slippage/participation cost model, an 11-name universe (vs their broad "in play" scan), and 2024–26 tape. Five long-only intraday hypotheses have now failed honest validation — the evidence increasingly favors lower-frequency lanes (monthly cross-sectional factor, G6b) over long-only day-trading for this book.
- **Reproducibility:** `stocks-in-play-orb@v1`; seed 42; git SHA `c84d27c`; result `results/stocks-in-play-orb-2024-08-05-2026-07-10.json`; data feed `alpaca-iex`; validation lane commit `1902ada` (Codex).

### vwap-reclaim v1 — REJECTED

- **Reason codes:** `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. Sample size passed (347 trades); data/PIT integrity and clean-run reproducibility passed. Rejection is merit/compliance, not missing data.
- **Scope/compliance:** 2024-08-05..2026-07-10; 11 liquid research names; 1-minute real Alpaca-IEX; regular-session VWAP only; actual next-open fills and Decimal stop/2R math; Sharia state `UNSCREENED_EXECUTION_BLOCKED`.
- **IS/full vs OOS:** full CAGR −40.62%, Sharpe −9.95, DSR 0.000, maxDD 63.37%, hit 17.00%, 347 trades, expectancy −0.29%/trade net. OOS: 104 trades, CAGR −44.90%, Sharpe −8.72, DSR 0.000.
- **Tail/integrity:** MC maxDD p5/p50/p95 58.86/63.56/67.43%; risk of ruin 100.00% vs 5% limit; jitter permutation p=1.000; Kelly 0; `P(day≥+5%)=0.00%`, `P(day≤−5%)=0.00%` over 5,313 observed return days.
- **Verdict/next:** the long-only regular-session absorption/reclaim pattern has decisive negative expectancy on this universe/feed/cost model. Per the frozen research branch, premarket-v1.1 is not tested after a negative v1. Do not retune v1; continue R2-4 with the independently specified stop-hunt-reversal-long team.
- **Reproducibility:** `vwap-reclaim@v1`; seed 42; git SHA `50375bd`; `BacktestRun` `9d1f7a2d-d447-4281-8a9e-fae14f5fdceb`; result `results/vwap-reclaim-2024-08-05-2026-07-10.json`; data feed `alpaca-iex`.

### gapper-orb v1-iex (G3d PIT micro-cap run) — REJECTED

- **Reason codes:** `NEGATIVE_EXPECTANCY`, `DRAWDOWN_RISK_FAILURE`, `INSUFFICIENT_SAMPLE`, `SHARIA_UNVERIFIABLE`. On the first genuine micro-cap universe the screen finally fired — and the long side LOST: 76 trades, hit 31.58%, mean −4.19%/trade net, full-period equity effectively wiped. This is a merit rejection, not only a sample one.
- **Scope/compliance:** 2026-02-13..2026-07-10, 1-minute intraday, 153 real NASDAQ micro-caps on 394 PIT candidate gap days (G3d discovery: mcap-at-date 10–400M via SEC-XBRL shares × prior close, corporate-action screened), real Alpaca-IEX bars; 30% OOS configured. Sharia state = `UNSCREENED_FOR_EXECUTION` (no PIT AAOIFI verdicts for micro-caps) — execution-blocked regardless of performance.
- **IS/full vs OOS:** full: 76 trades, CAGR −100.00%, Sharpe −4.31, DSR 0.051, maxDD 98.34%, hit 31.58%, expectancy −4.19%/trade net, turnover 76×. OOS: CAGR −100.00%, Sharpe −4.64, DSR 0.180. Daily-return distribution: 0 observed days persisted (pooled per-symbol short spans — artifact gap, not evidence).
- **Tail/integrity:** MC maxDD p5/p50/p95 = 91.89/98.76/99.80%, risk of ruin 99.90%, entry-jitter permutation p=0.986 (entries indistinguishable from noise, direction negative), Kelly 0.0000 → clamped qty 0. walk-forward=false, profit plateau=false, implausible=false.
- **Expert finding (binds the hypothesis, not just the run):** long gap-continuation on micro-cap gappers is the losing side of the pattern — these names predominantly fade after the gap (dilution/offering risk, pump exhaustion); practitioner edge here is documented mostly on the short side, which the Sharia charter excludes. Any v2 must change the hypothesis (e.g. Zarattini "Stocks in Play": relative-volume ranking, trend alignment, wider stops, large/mid-cap universe) rather than retune this one.
- **Reproducibility:** strategy/params `gapper-orb@v1-iex`; seed 42; git SHA `9db80b9`; `BacktestRun` 270cabf5-a288-4aaf-a132-bcfa418aa77a; result `results/gapper-orb-2026-02-13-2026-07-10.json`; data feed `alpaca-iex`; candidate artifact `results/gapper-candidates.json` (394 symbol-days, 2,764-symbol universe, PIT mcap).

### bollinger-mr-long v1 — REJECTED

- **Reason codes:** `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. OOS CAGR turned −32.30%, OOS DSR fell to 0.283, and MC p95 maxDD reached 75.43% versus the 30% breaker; the apparent full-history edge is regime-dependent and not promotable.
- **Scope/compliance:** 2022-07-01..2026-07-10, daily cadence, declared 25-name NASDAQ-halal research universe, real Yahoo daily spine with 1,777 mock bars excluded; last 30% held out OOS. Sharia state = `DECLARED_HALAL_RESEARCH_UNIVERSE / UNSCREENED_FOR_EXECUTION`: the artifact persists no PIT AAOIFI verdict, so executable admission is blocked independently of the performance rejection.
- **IS/full vs OOS:** pure IS = n/a — the artifact persists full-history rather than a separate IS slice. Full: 151 trades, CAGR 34.23%, DSR 0.954, maxDD 52.16%, hit 62.25%, expectancy +1.104%/trade net, turnover 151; Calmar, exposure, avg win/loss = n/a — not persisted. OOS: trade count n/a — artifact repeats total count rather than split count; CAGR −32.30%, DSR 0.283, maxDD 52.16%, hit 57.78%; OOS expectancy/turnover/exposure/Calmar = n/a — not persisted.
- **Tail/integrity:** MC p95 maxDD 75.43%, `P(day≥+5%)=0.00%`, `P(day≤−5%)=0.00%`, ruin 14.70%, permutation p=0.048; walk-forward=true, OOS holdout=30%, profit plateau=false, implausible=false. A v2 must be a fresh candidate, not a v1 retune on viewed OOS data.
- **Reproducibility:** strategy/params `bollinger-mr-long@v1` (20d band, 2σ, VR≤1); seed 42; git SHA `55a1021`; `BacktestRun` id = n/a — not exposed by result artifact; result `results/bollinger-mr-long-2022-07-01-2026-07-10.json`; data feed `yahoo-daily`.

### bollinger-mr-long-v2 — REJECTED (but the diagnosed structural fixes measurably worked)

- **What v2 is (NOT a v1 retune):** a NEW candidate that keeps v1's entry/exit rule FAMILY unchanged (20d/2σ lower-band long; mid-band / ATR-stop / max-hold exits) and adds ONLY the two layers the round-1 diagnosis blamed for v1's failure: (1) **inverse-vol per-name sizing** — position weight `w = clamp(targetVolBudget/σ, 0, maxNameFraction)` on trailing-60d daily realized vol, so jumpy names (TSLA/NVDA-class) risk a smaller book slice than calm ones; the setup emits this as a `sizeFraction` HINT that the risk envelope still CLAMPS (name/gross/vol-target/ADV/cash) and can only shrink, never bypass; and (2) a **stricter regime gate** — VR ≤ 0.85 (was ≤1.0) AND close > the name's 200-day SMA (mean-revert only inside an uptrend; Chan ch.2). These were DECIDED from the diagnosis, not fitted to viewed OOS data.
- **Reason codes:** `IMPLAUSIBLE_RESULT`, `DSR_FAILURE`, `OOS_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`, `DATA_QUALITY_PIT_FAILURE`, `REPRODUCIBILITY_FAILURE`. The binding rejection is the **implausible flag**: full Sharpe **3.06 > 3** (and OOS 3.00). Per the honesty rules the flag binds — treat as overfit/artifact, do NOT explain it away or tune it under 3.
- **The fixes DID what the diagnosis predicted (do not bury this):** v1's fatal OOS collapse is GONE. OOS CAGR **+5.13%** (v1 was **−32.30%**) and nearly equals full CAGR **+5.34%** — no regime-unwind. Loss-avoidance (Klarman) targets are met for the first time in this lane: full maxDD **9.84%** (v1 52.16%), **MC maxDD p95 14.14%** vs the 30% breaker (v1 **75.43%**), risk-of-ruin **0.00%** (v1 14.70%). And the edge is now statistically real: **entry-jitter permutation p=0.001** (v1 0.048, ts-momentum 0.108) — distinguishable from random entry timing. 240 trades > the 100 floor, so this is a merit result, not a sample artifact.
- **Why it is still REJECTED (honest):** (a) the pooled-trade Sharpe 3.06 is inflated by the harness's trade-sequence √252 annualization (240 trades ≈ 28/yr, not 252) — the same convention applied to every daily setup; the inverse-vol sizing uniformized per-trade returns and pushed it past 3, tripping the implausible guard. (b) The trustworthy, NON-annualized statistic — the Deflated Sharpe — is **0.997 full but 0.931 OOS**, just under the 0.95 promotion gate ⇒ `DSR_FAILURE`. (c) `OOS_FAILURE` here is driven ONLY by the harness leaving `walkForward=false` (OOS CAGR is positive), though the daily engine is structurally walk-forward (expanding window, decide-close/fill-next-open). (d) No parameter sweep was run, so `profitPlateau=false` ⇒ `NO_PROFIT_PLATEAU_OVERFIT`. (e) Sharia + PIT + reproducibility flags are unset for a non-candidate daily run (artifact-faithful false).
- **IS/full vs OOS:** full: 240 trades, CAGR +5.34%, Sharpe 3.06 (⇒ `implausible`), DSR 0.997, maxDD 9.84%, hit 68.33%, expectancy **+0.189%/trade net**, turnover 240×. OOS (last 30% of the trade sequence, 72 trades): CAGR +5.13%, Sharpe 3.00, DSR 0.931, maxDD 6.63%, hit 70.83%; expectancy/exposure/Calmar = n/a — not persisted separately.
- **Tail/integrity:** MC maxDD p5/p50/p95 = **4.73 / 7.94 / 14.14%**, risk-of-ruin 0.00% (limit 5%), entry-jitter permutation **p=0.001** (mean/trade +0.189%), `P(day≥+5%)=0.00%` and `P(day≤−5%)=0.00%` over **49,197** observed daily returns (mean 0.00% / std 0.09%), Kelly 0.0676 → clamped qty 13.93. walk-forward flag=false (harness), OOS holdout=30%, profit-plateau=false, implausible=true.
- **Scope/compliance:** 2018-01-02..2026-07-10 (≈6 regimes: 2018 chop, 2020 crash, 2021 bull, 2022 bear, 2023–24 bull, 2025–26), daily cadence, **23 of the 25-name NASDAQ-halal universe** carried real in-window bars (2,141 real YAHOO/ALPACA bars each; MSFT/NVDA returned no real in-window rows by the harness — the documented MOCK-only exclusion), **0 MOCK bars reached the simulator** (this window/name set has none). Sharia state = `UNVERIFIED` (no PIT AAOIFI verdict persisted) ⇒ executable admission blocked independently of performance.
- **Verdict / next:** REJECTED. This is the strongest daily result to date — the diagnosis was correct and the two fixes repaired v1's OOS collapse and drawdown — but it does not clear promotion (implausible Sharpe + OOS DSR 0.931 + no plateau). The honest next step is NOT to retune v2 to dodge the implausible flag (post-hoc snooping); a v3, if pursued, must (i) fix the trade-sequence Sharpe annualization at the metrics/harness level so the flag reflects true annualized risk-adjusted return, and (ii) run a parameter-plateau sweep on the two new layers, on fresh unviewed data.
- **Reproducibility:** strategy/params `bollinger-mr-long-v2@v2` (20d/2σ band, VR≤0.85, 200-SMA uptrend, 60d realized-vol sizing / 0.4%-day budget / 20% cap); seed 42; git SHA `af89d57`; `BacktestRun` `80791a9d-2c9e-4855-9086-e789181d0002`; result `results/bollinger-mr-long-v2-2018-01-02-2026-07-10.json`; data feed `yahoo-daily`. Engine change: `simulateSetupDaily` now deploys the setup's optional `sizeFraction` hint (envelope-clamped) and scales the pooled trade return by the deployed book weight; legacy setups emit no hint ⇒ byte-identical prior results.

### bollinger-mr-long-v2 — RE-RUN (corrected measurement, QDR-7 new run) — REJECTED

- **Why a new run entry (not a rewrite):** QDR-7 forbids editing a terminal card. The card ABOVE remains the binding record of the *original* run and its `IMPLAUSIBLE_RESULT` verdict. This entry records a **parameter-identical re-run** (same 2018-01-02..2026-07-10 window, seed 42, `bollinger-mr-long-v2@v2` — strategy code untouched) after a **MEASUREMENT-only** fix to the validation harness. This is a measurement correction, NOT a retune: no strategy parameter, universe, cost, or window changed. Expectancy, trade count, Monte Carlo, permutation, drawdown, DSR, and CAGR are byte-identical to the prior run; ONLY the Sharpe-family numbers and two checklist fields move.
- **The two measurement defects fixed (harness, not strategy):**
  1. **Trade-sequence √252 annualization → time-based.** The pooled headline curve is one point per trade *exit* (a sparse trade sequence), not a per-trading-day mark-to-market series. `computeMetrics` annualized its per-period Sharpe by a fixed √252 as if the 240 trades (≈28/yr over 8.5 yrs) were consecutive daily returns — inflating Sharpe ~√(252/28)≈3×. Fix: `annualization:'calendar'` derives periods/yr from the curve's own calendar span (`observations/elapsedYears`) and annualizes by its √. Statistically defensible because, for a near-zero-mean series, the trade-sequence Sharpe·√(N/T) **equals** the √252-annualized Sharpe of the equivalent daily equity curve (zero-return non-trade days cancel between numerator and denominator; derivation + regression pin in `metrics.test.ts`). The intraday/daily ENGINE curves (genuine ~252 pts/yr) keep the fixed √252 default — they were already correct.
  2. **`dataQualityPitOk` / `reproducible` were never populated** (persisted `false` unconditionally on non-candidate runs, manufacturing two phantom rejection codes). Now wired to VERIFIED signals: `dataQualityPitOk` = the run consumed >0 bars that all passed the real-source filter (MOCK excluded + asserted) AND the engine re-ran `assertNoLookahead` on every bar (a violation aborts the run before this point); `reproducible` = deterministic no-LLM PM-surrogate path with a recorded valid seed + gitSha. Both are now honest `true` for this run, not artifact-`false`.
- **The implausible guard itself was NOT weakened** — the Sharpe>3 threshold is unchanged. We corrected the *measurement feeding* it; the corrected annualized Sharpe is genuinely below 3.
- **Corrected metrics (only these moved):** full Sharpe **3.06 → 1.02**, OOS Sharpe **3.00 → 0.98** ⇒ `implausible=false` (guard no longer trips). Checklist: `data/PIT integrity` **no → yes**, `reproducible clean run` **no → yes**. Unchanged: 240/72 trades, CAGR **+5.34% / +5.13%**, DSR **0.997 full / 0.931 OOS**, maxDD 9.84%, hit 68.33%, expectancy **+0.189%/trade net**, MC maxDD p5/p50/p95 **4.73 / 7.94 / 14.14%**, ruin 0.00%, jitter **p=0.001**, 49,197 daily obs, Kelly 0.0676→13.93. No other drift observed.
- **Corrected reason codes:** `OOS_FAILURE`, `DSR_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. Removed as artifacts: `IMPLAUSIBLE_RESULT`, `DATA_QUALITY_PIT_FAILURE`, `REPRODUCIBILITY_FAILURE`.
- **Verdict (honest, per gates):** still **REJECTED** — the remaining four codes are all REAL, not measurement artifacts: (a) `OOS_FAILURE` from the harness leaving `walkForward=false` (OOS CAGR is positive; the daily engine is structurally expanding-window, but the flag is not asserted); (b) `DSR_FAILURE` — OOS Deflated Sharpe **0.931 < 0.95** promotion gate (the trustworthy, annualization-independent statistic — this is the substantive miss); (c) `NO_PROFIT_PLATEAU_OVERFIT` — no parameter sweep was run; (d) `SHARIA_UNVERIFIABLE` — no persisted PIT AAOIFI verdict. Promotion still requires a plateau sweep on fresh unviewed data + an OOS DSR that clears 0.95; do NOT retune v2 to dodge any flag.
- **Reproducibility:** params `bollinger-mr-long-v2@v2` (unchanged); seed 42; git SHA `eb5e727`; `BacktestRun` `1c4ed82a-87b6-4957-b4fe-a897ac7ef3ad`; result `results/bollinger-mr-long-v2-2018-01-02-2026-07-10.json` (overwrote the prior run's artifact — the original run's card above is the archival record). Harness change: `computeMetrics` gains an `annualization:'fixed'|'calendar'` option (default `'fixed'`, backward-compatible); `scripts/backtest.ts` uses `'calendar'` for the pooled trade-sequenced curve and wires the two verified checklist signals. No strategy file touched.

### bollinger-mr-long-v3 — REJECTED (E2, C1 breadth)

- **Pre-registration / scope:** source `d4814b6`; v2's 20d/2σ entry, VR≤0.85 + 200-SMA regime, inverse-vol 0.4% budget/20% cap, exits, and four frozen neighbors were unchanged. C1 supplied 100 current index-provider-screened names; max six shared-cash positions. Current-holdings survivorship is disclosed.
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. Full/OOS CAGR **3.59%/7.97%**, DSR(5) **0.373/0.388**, maxDD **24.61%**, 625/214 closed trades, hit **40.51%**, mean **+0.54%/trade**, reproducible/PIT true, implausible false.
- **Tail / robustness:** 1,000 seeded paths × 2,145 book-days gave MC maxDD p5/p50/p95 **20.75/34.69/58.45%** and ruin **5.50%**; `P(day≥+5%)=0.14%`, `P(day≤−5%)=0.28%`, permutation p=0.029. The frozen plateau **PASSED** (neighbor OOS expectancy 0.848–1.157%), so breadth solved sample + plateau but not DSR/tail risk.
- **Compliance / identifiers:** C1 state `VERIFIED_COMPLIANT`, with Tier-1 `FUND_LEVEL_PURIFICATION_ONLY`; 208,567 real bars reached the engine and 124 MOCK rows were excluded. Seed 42; BacktestRun `4b2d4085-dc96-47c2-9a33-471eae80dd48`; result `results/bollinger-mr-long-v3-2018-01-02-2026-07-17-c1_verified_100.json`. The 1Y evidence view `a620f5f5-…` is non-promotable.

### ts-momentum-halal-basket v1 — REJECTED

- **Reason codes:** `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. OOS CAGR turned −45.18%, OOS DSR fell to 0.186, MC p95 maxDD reached 81.42%, ruin reached 24.20%, and permutation p=0.108 did not distinguish the edge from sign-flips.
- **Scope/compliance:** 2022-07-01..2026-07-10, daily cadence, 23 symbols present in the result from the declared 25-name NASDAQ-halal basket, real Yahoo daily spine with 585 mock GOOGL bars excluded; last 30% held out OOS. Sharia state = `DECLARED_HALAL_RESEARCH_UNIVERSE / UNSCREENED_FOR_EXECUTION`: the artifact persists no PIT AAOIFI verdict, so executable admission is blocked independently of the performance rejection.
- **IS/full vs OOS:** pure IS = n/a — the artifact persists full-history rather than a separate IS slice. Full: 231 trades, CAGR 25.16%, DSR 0.924, maxDD 70.27%, hit 41.56%, expectancy +0.718%/trade net, turnover 231; Calmar, exposure, avg win/loss = n/a — not persisted. OOS: trade count n/a — artifact repeats total count rather than split count; CAGR −45.18%, DSR 0.186, maxDD 54.36%, hit 37.68%; OOS expectancy/turnover/exposure/Calmar = n/a — not persisted.
- **Tail/integrity:** MC p95 maxDD 81.42%, `P(day≥+5%)=0.0088%` (0.01% rounded), `P(day≤−5%)=0.00%`, ruin 24.20%, permutation p=0.108; walk-forward=true, OOS holdout=30%, profit plateau=false, implausible=false. A per-name-sized/regime-conditioned v2 must start as a new candidate.
- **Reproducibility:** strategy/params `ts-momentum-halal-basket@v1` (252/63 dual momentum, EMA100 exit); seed 42; git SHA `a6f8d4b`; `BacktestRun` id = n/a — not exposed by result artifact; result `results/ts-momentum-halal-basket-2022-07-01-2026-07-10.json`; data feed `yahoo-daily`.

### ts-momentum-halal-basket-v2 — REJECTED (but the diagnosed structural fixes measurably worked)

- **What v2 is (NOT a v1 retune):** a NEW candidate that keeps v1's dual-momentum entry/exit rule FAMILY byte-for-byte (entry: 252d return > 0 AND 63d return > 0; exit: 63d ≤ 0 OR close < EMA100 — "either") and adds ONLY the two layers the round-1 diagnosis blamed for v1's failure: (1) **inverse-vol per-name sizing** — position weight `w = clamp(targetVolBudget/σ, 0, maxNameFraction)` on trailing-60d daily realized vol, emitted as a `sizeFraction` HINT the risk envelope still CLAMPS and can only shrink — the SAME proven helper (`volScaledWeight`) bollinger-mr-long-v2 uses (import-only; v2 never modified); and (2) a **market-regime kill-switch** — NEW entries are admitted only when the equal-weight, daily-rebalanced BASKET index is above its OWN 200-day SMA (Faber absolute-trend filter). Existing positions are UNAFFECTED by the gate — they exit purely by v1's rules (loss-avoidance owns the downside). Params were DECIDED a priori (sizing form/budget/cap identical to bollinger-v2; regime SMA the textbook 200d) and were NOT swept on viewed OOS data.
- **Reason codes:** `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. The binding MERIT miss is `DRAWDOWN_RISK_FAILURE`: MC maxDD **p95 37.25% > the 30% breaker** — a highly-correlated mega-cap-tech basket still carries a synchronized tail beyond the Klarman gate even after inverse-vol sizing. No implausible flag (Sharpe measured honestly under the corrected calendar harness).
- **The fixes DID what the diagnosis predicted (do not bury this):** v1's fatal OOS collapse is GONE. OOS CAGR **+16.65%** (v1 was **−45.18%**) and essentially matches full CAGR **+16.98%** — no regime unwind. Loss-avoidance targets moved the right way: MC maxDD **p95 81.42% → 37.25%**, risk-of-ruin **24.20% → 0.00%**, full maxDD **70.27% → 29.44%**. And the edge is now statistically real: **entry-jitter permutation p=0.002** (v1 was 0.108 — indistinguishable from sign-flip noise). 1,334 trades ≫ the 100 floor, so this is a merit result, not a sample artifact.
- **Why it is still REJECTED (honest):** (a) `DRAWDOWN_RISK_FAILURE` — MC p95 37.25% clears the 30% drawdown breaker; the tail was cut more than half from v1 but is not yet inside the envelope. (b) `DSR_FAILURE` — the trustworthy, annualization-independent statistic, OOS Deflated Sharpe **0.894 < 0.95** promotion gate (full DSR 0.999); the substantive risk-adjusted miss, though close. (c) `OOS_FAILURE` is driven ONLY by the harness leaving `walkForward=false`; the OOS CAGR is strongly POSITIVE (+16.65%) and the daily engine is structurally expanding-window (decide-close / fill-next-open). (d) `NO_PROFIT_PLATEAU_OVERFIT` — no parameter sweep was run (params are a-priori, not fitted). (e) `SHARIA_UNVERIFIABLE` — no persisted PIT AAOIFI verdict for a non-candidate daily run.
- **IS/full vs OOS:** pure IS = n/a — the artifact persists full-history rather than a separate IS slice. Full: 1,334 trades, CAGR **+16.98%**, Sharpe **0.90** (calendar-annualized; implausible=false), DSR **0.999**, maxDD 29.44%, hit 39.28%, expectancy **+0.112%/trade net**, turnover 1,334×; Calmar/exposure/avg win-loss = n/a — not persisted. OOS (last 30% of the trade sequence, 400 trades): CAGR **+16.65%**, Sharpe 0.78, DSR **0.894**, maxDD 22.23%, hit 40.00%; OOS expectancy/turnover/exposure/Calmar = n/a — not persisted separately. (Trend-follower profile: sub-50% hit rate carried by a few large winners — expected, not a defect.)
- **Tail/integrity:** MC maxDD p5/p50/p95 = **15.13 / 22.91 / 37.25%**, risk-of-ruin 0.00% (limit 5%), entry-jitter permutation **p=0.002** (mean/trade +0.112%), `P(day≥+5%)=0.00%` and `P(day≤−5%)=0.0020%` over **49,197** observed daily returns (mean 0.003% / std 0.25%), Kelly 0.0267 → clamped qty 5.50. walk-forward flag=false (harness), OOS holdout=30%, profit-plateau=false, implausible=false; data/PIT integrity=yes, reproducible=yes.
- **Scope/compliance:** 2018-01-02..2026-07-10 (≈6 regimes: 2018 chop, 2020 crash, 2021 bull, 2022 bear, 2023–24 bull, 2025–26), daily cadence, **23 of the 25-name NASDAQ-halal universe** carried real in-window bars (2,141 real YAHOO/ALPACA bars each; MSFT/NVDA returned no real in-window rows — the documented MOCK-only exclusion), **0 MOCK bars reached the simulator** (this window/name set has none; the regime index is built from the same 23 real series). Sharia state = `UNVERIFIED` (no PIT AAOIFI verdict persisted) ⇒ executable admission blocked independently of performance.
- **Verdict / next:** REJECTED. This is the strongest MOMENTUM result to date and the second daily lane where the diagnosis proved correct — the regime kill-switch + inverse-vol sizing repaired v1's OOS collapse, drawdown, and statistical significance. It does not clear promotion: MC p95 drawdown is still ~7pts over the breaker and OOS DSR is ~0.06 under the gate. The honest next step is NOT to retune v2 to sneak under either flag (post-hoc snooping); a v3, if pursued, must (i) reduce the correlated-tail drawdown by a mechanism decided a priori (e.g. a basket-level gross-exposure/vol-target scalar, or capping concurrent positions), and (ii) run a parameter-plateau sweep on fresh unviewed data.
- **Reproducibility:** strategy/params `ts-momentum-halal-basket-v2@v2` (252/63 dual momentum, 100-EMA/63d≤0 exit; 60d realized-vol sizing, 0.4%/day budget, 20% cap; 200d basket-index regime gate); seed 42; git SHA `35b3da2` (HEAD at run; the new `src/quant/strategies/tsMomentumHalalBasketV2.ts` + catalog entry are codified for this row, uncommitted at run time); `BacktestRun` `ab4c0da7-e4dd-47df-9cae-0395c79fbdd6`; result `results/ts-momentum-halal-basket-v2-2018-01-02-2026-07-10.json`; data feed `yahoo-daily`. New file reuses (import-only, never modifies): v1's `momentum`/`ema` math, bollinger-mr-long-v2's `volScaledWeight`/`realizedDailyVol`/`sma` sizing helper, the shared `NASDAQ_HALAL_UNIVERSE`, and the CLI `prepareUniverse` cross-name hook (builds the equal-weight basket index, read PIT-filtered ≤ asOf). No engine, metrics, risk, or existing-setup file was touched.

### ts-momentum-halal-basket-v3 — REJECTED (shared-book promotion-grade run)

- **Version / only new hypothesis:** `ts-momentum-halal-basket-v3@v3` delegates v2's 252d+63d positive-momentum entry and 63d≤0-or-EMA100 exit unchanged. Its sole new layer is the shared-book governor: trailing-60-book-day realized volatility targets 15% annualized by scaling gross exposure down to cash (never above 1×), with at most six concurrent positions. Center effective params also retain v2's 60d per-name volatility sizing (0.4% daily budget, 20% name cap) and 200d basket-regime SMA; validation trials persisted as 9.
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `SHARIA_UNVERIFIABLE`. OOS DSR(9) **0.3751 < 0.95** and seeded book-day MC p95 max drawdown **49.9067% > 30%** are independent merit failures; `UNSCREENED_EXECUTION_BLOCKED` independently prevents AUTO_PAPER. Positive OOS return and plateau robustness do not override any hard gate; no profit is promised.
- **Scope / integrity:** 2018-01-02..2026-07-10, daily `yahoo-daily`, NASDAQ long-only cash, 23 symbols, **49,243 real YAHOO/ALPACA bars**, 0 MOCK, 2,141 true shared-book NAV points, maximum six holdings, last 30% OOS. PIT/walk-forward passed (49,220 decision windows), reproducible=true, implausible=false. Pure IS = n/a — artifact persists full and OOS, not a separate IS slice.
- **Full vs OOS:** full: 1,637 trades, CAGR **5.7420%**, Sharpe 0.4510, DSR(9) **0.4138**, maxDD **29.6280%**, hit 44.673%, persisted position-trade expectancy **+11.3744% net**, turnover 1,637 count proxy / notional `13042458.754651193092` (currency unit not persisted), average exposure **67.0968%**. OOS: 614 trades, CAGR **11.8303%**, Sharpe 0.7615, DSR(9) **0.3751**, maxDD **19.5133%**, hit 53.894%, center OOS position-trade expectancy **+13.2558%**, turnover 614 count proxy; OOS notional/exposure, full/OOS Calmar, average win/loss = n/a — not persisted.
- **Tail / robustness:** 1,000 bootstrap paths × 2,140 **book-day** observations: maxDD p5/p50/p95 **17.8655/29.4576/49.9067%**, risk of ruin **1.10%**, terminal-equity p50 **155,121.41**; Kelly 0. Daily distribution: mean **0.02663%**, std **0.93746%**, `P(day≥+5%)=0%`, `P(day≤−5%)=0.04673%`. Position-trade sign-flip permutation p=**0.000**. Frozen plateau **PASS** across target vol {13.5%,15%,16.5%} × caps {5,6,7}: center OOS expectancy 13.2558%, neighbors 12.3836–13.8979%, worst degradation 6.5793%; no winner was selected and center was unchanged.
- **Sharia / reproducibility:** instrument charter is long-only cash equities in the declared research basket, but no persisted PIT AAOIFI verdict exists; state `UNSCREENED_EXECUTION_BLOCKED` / `SHARIA_UNVERIFIABLE`, so no book or allocation. Seed 42; git SHA `f9d788b`; BacktestRun `f841fa65-41e6-4932-ae65-906dd3e3bd99`; result `results/ts-momentum-halal-basket-v3-2018-01-02-2026-07-10.json`; shared engine route. Source gate: 67/67 focused and 626/626 full tests, lint/typecheck/diff-check passed.

#### QA-corrected replay addendum — FINAL shipped-source identity

- **Why the prior run is superseded:** QA found a public cache-purity/PIT defect and misleading v3 rationale after the `f9d788b` run. The historical card above is preserved verbatim for auditability, but it is not the final shipped-source replay identity. Source `d3d0071` makes replay scope engine-owned and immutable, leaves direct calls uncached, asserts PIT before cache lookup, and states in both English/Arabic rationale that v3 is REJECTED, research-only, AAOIFI-unscreened, and execution-blocked.
- **Final replay / zero drift:** one exact post-commit run exited 0 with reproducible=true and **zero metric, plateau, Sharia, or reason-code drift**: full/OOS CAGR 5.742%/11.830%, DSR(9) 0.4138/0.3751, maxDD 29.628%/19.513%, book-day MC p95 49.907%, ruin 1.10%, plateau PASS, `P(day≥+5%)=0%`, `P(day≤−5%)=0.04673%`, state `UNSCREENED_EXECUTION_BLOCKED`; ordered reasons remain `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `SHARIA_UNVERIFIABLE`. Status remains **REJECTED** with no AUTO_PAPER/AUTO_REAL permission.
- **Final identifiers / gates:** source git SHA `d3d0071`; BacktestRun `f9e5f702-253b-4a9d-bdec-d89144038ecb`; seed 42; same result path `results/ts-momentum-halal-basket-v3-2018-01-02-2026-07-10.json`. Focused 24/24, full 630/630, i18n parity 1/1, lint/typecheck/diff-check passed; frozen 9-cell heap test completed in 1.053s. These identifiers supersede `f9d788b` / `f841fa65-41e6-4932-ae65-906dd3e3bd99` for cold-start reproduction without deleting the earlier evidence.

### ts-momentum-halal-basket-v4 — REJECTED (E1, drawdown-governed C1 sleeve)

- **Pre-registration / scope:** source commit `d00c79b`; v3's 252d+63d signal, 60d/15% volatility governor, six-position cap, and frozen 3×3 plateau stayed unchanged. The only risk addition was full exposure through 8% book drawdown, linearly de-risking to cash at 20%. C1 selected the top 100 current index-provider-screened names by trailing real dollar volume; applying current holdings backward is an explicitly disclosed survivorship limitation.
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`. Full/OOS CAGR **7.00%/13.11%**, DSR(9) **0.556/0.428**, realized maxDD **18.31%/12.93%**, 1,693/554 closed execution records, hit **45.50%**, reproducible/PIT true, implausible false. Positive returns are measured simulation, not a promise.
- **Tail / robustness:** 1,000 seeded paths × 2,145 book-days gave MC maxDD p5/p50/p95 **15.55/24.93/41.49%**, ruin **0.10%**, `P(day≥+5%)=0%`, `P(day≤−5%)=0.0466%`; permutation p=0.000. Plateau **FAIL** because targetVol=16.5%/maxPositions=5 degraded OOS expectancy **66.19%** from the frozen center.
- **Compliance / evidence:** C1 snapshot state `VERIFIED_COMPLIANT` under the approved S&P index-provider-screened label; Tier-1 per-name purification remains unavailable (`FUND_LEVEL_PURIFICATION_ONLY`), never fabricated. 208,567 real YAHOO/ALPACA bars reached the shared engine; 124 MOCK rows were observed and excluded.
- **Identifiers:** seed 42; source `d00c79b`; BacktestRun `9fd3f2d5-274c-492a-b91e-a524d2c157e3`; result `results/ts-momentum-halal-basket-v4-2018-01-02-2026-07-17-c1_verified_100.json`. The 1Y evidence view (`222ba45b-…`) had 0 trades because its 251 sessions do not satisfy the frozen 252-day formation window; it is non-promotable and not the verdict.

### dual-momentum-rotation v1 — REJECTED (shared-book, continuous-cap run)

- **Version / terminal verdict:** `dual-momentum-rotation@v1`; fixed declared sleeve `SPUS,HLAL,AAPL,MSFT,NVDA,GOOGL,AMZN`; month-end 12−1M relative rank (`close[t−21]/close[t−252]−1`), strict-positive 12M absolute filter, otherwise cash; long-only, max one position. **REJECTED** in ordered QDR-7 reasons: `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. Only **11 independent closed position episodes ≪ 100**; OOS DSR(9) **0.7012 < 0.95**, the frozen plateau failed, and `UNSCREENED_EXECUTION_BLOCKED` independently bars AUTO_PAPER; no return is promised.
- **Scope / integrity:** 2018-01-02..2026-07-10, daily `yahoo-daily`, 14,108 real YAHOO/ALPACA bars, **0 MOCK**, 2,141 shared-book NAV days, exact seven symbols (SPUS 1,647; HLAL 1,756; each stock 2,141), last 30% OOS. PIT/walk-forward/data-quality/reproducibility passed; seed 42; shared route; implausible=false. Pure IS = n/a — not persisted separately.
- **Full vs OOS:** full: **11 closed position episodes**, CAGR **5.4857%**, Sharpe 0.7261, DSR(9) **0.7370**, maxDD **17.3578%**, hit **30.1402%**, qty-weighted cash-flow-equivalent expectancy **+16.6295%/episode net**, raw execution turnover 147 sells / notional `691156.86145910094705`, average exposure **13.2886%**. OOS: **3 closed episodes**, CAGR **13.8533%**, Sharpe 1.2905, DSR(9) **0.7012**, maxDD **11.7320%**, hit **53.1153%**, plateau-center OOS episode expectancy **+47.7266%**, raw turnover 76 sells. Full/OOS Calmar and average win/loss; OOS notional/exposure = n/a — not persisted. Daily NAV drives CAGR/DSR/drawdown; episode returns drive sample, permutation and plateau only.
- **Risk / tails:** continuous risk policy is fixed `maxGrossFraction=25%`, `maxOpenPositions=1`; max holdings **1**, max post-fill gross **25.0000%**, while close-to-next-open price drift peaked at **28.5546%** and was queued for deterministic next-open trimming (no same-close fill). Average gross **13.2886%**; cash stayed non-negative. Seeded 1,000-path × 2,140 **book-day** MC maxDD p5/p50/p95 **8.4622/13.2286/23.1506%**, ruin **0%**; daily mean/std **0.02246%/0.49099%**, `P(day≥+5%)=0.04673%`, `P(day≤−5%)=0%`; independent-episode sign-flip permutation p=**0.236** (not significant).
- **Robustness / compliance:** frozen 3×3 grid `{lookback 210,252,294} × {absolute threshold −1%,0,+1%}` used center + eight neighbors only; no winner selection. Plateau **FAIL** over independent episodes: center OOS expectancy **47.7266%**; 210-bar neighbors were **−0.8645% / +0.3450% / +0.3450%**, a sign flip or >99% degradation (`spiky_neighborhood`). All seven names remain AAOIFI-unscreened (`source=none`), candidate/research-only, execution blocked; ETF names do not imply certification.
- **Execution audit / reproducibility / supersession:** 12 BUY entries, **11 closed episodes**, 147 raw sells = 136 partial risk trims + 11 full exits; the still-open episode is excluded. Git SHA `0aef451`; BacktestRun `13a95084-5890-4d17-92f5-c01d913e2d5c`; result `results/dual-momentum-rotation-2018-01-02-2026-07-10.json`; validationTrials=9. Focused 30/30, full 645/645, lint/typecheck/diff-check passed. Earlier `abe70156…` (deleted data), `7fc795e8…` (entry-only cap), and `d3628d85…` / docs commit `173b92a` (trim pseudo-replication) are superseded audit records, not terminal strategy evidence.

### multi-mode-book-v1 — REJECTED (E3, allocator-combined shared book)

- **Pre-registration / construction:** source `455d8ea`; the existing allocator consumed the three parents' frozen card priors and its 40% cap produced **20% momentum / 40% mean reversion / 40% dual momentum** at seed 42. Six/five/five disjoint C1 names prevent double ownership; component rules are unchanged and rebalance on their signal cadence. Two ±5pp weight views are robustness checks only.
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. Full/OOS CAGR **11.02%/21.46%**, DSR(3) **0.888/0.729**, maxDD **22.90%**, 428/165 closed trades, hit **46.06%**, mean **+2.96%/trade**, reproducible/PIT true, implausible false. No return is promised.
- **Tail / robustness:** 1,000 seeded paths × 2,145 book-days gave MC maxDD p5/p50/p95 **17.37/27.58/46.40%**, ruin **0.50%**, `P(day≥+5%)=0.19%`, `P(day≤−5%)=0.23%`, permutation p=0.001. The frozen weight plateau **PASSED**, but the 30% tail gate did not.
- **Compliance / identifiers:** all 16 names were present in C1 and state `VERIFIED_COMPLIANT`; 34,268 real bars reached the engine and 124 MOCK rows were excluded. Seed 42; BacktestRun `8aed13a7-c560-4d92-9d18-9e5c647c2fe0`; result `results/multi-mode-book-v1-2018-01-02-2026-07-17-c1_verified_multi_mode_16.json`. The 1Y view `6c13fd25-…` had only 251 sessions versus the 252-day momentum formation window and is non-promotable.

### multi-mode-book-v2 — CANDIDATE (E6, drawdown-governed shared book)

- **Pre-registration (written and committed BEFORE any run):** v2 is `multi-mode-book-v1` (`455d8ea`) with EXACTLY ONE added axis — a book-level drawdown-responsive exposure governor attacking E3's single binding merit gate `DRAWDOWN_RISK_FAILURE` (MC p95 book DD **46.40%** vs the 30% gate). Every v1 sleeve param is inherited UNCHANGED: seed-42 allocator 20% momentum / 40% mean-reversion / 40% dual-momentum, disjoint 16-name C1 charter, component signal-cadence rebalance, 3 trials; the sleeve entry/exit/screen/signal hooks are delegated to v1 verbatim.
- **Frozen governor (a-priori, derived from the GATE ITSELF, never from any viewed equity curve):** linear exposure multiplier **1.0 at ≤10% book drawdown → 0.0 (all cash) at ≥25% book drawdown**, computed on the book's running peak-to-trough NAV drawdown, applied to NEW position sizes only; existing positions still exit by their sleeve rules and de-risking sells are always allowed. This maps exactly onto the engine's pre-existing down-only `drawdownExposureScalar` / `StrategyBookPolicy{drawdownStartFraction:0.10, drawdownCashFraction:0.25}` — v2 adds no new engine code. E1's viewed 8%→20% momentum-only governor (plateau FAIL there) motivated only WHICH axis to attack (tail risk); the threshold VALUES come from the 30% gate, not from any curve.
- **Frozen 3×3 plateau neighborhood:** {8%,10%,12%} × {20%,25%,30%} on {drawdownStartFraction × drawdownCashFraction}, center {10%,25%}, 8 off-center neighbors; robustness probe only — the chosen thresholds are NEVER updated from the sweep.
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. Full/OOS CAGR **8.47%/15.47%**, Sharpe **0.67/0.89**, DSR(3) **0.860/0.706**, actual maxDD **22.01%**, 444/165 closed trades, hit **46.15%**, mean **+3.17%/trade**, jitter p=0.000, reproducible/PIT true, implausible false. IS/OOS split = 70/30 (OOS reserve 30%). No return is promised.
- **Did the governor work? Honest read:** it attacked the correct axis and moved the tail the right way but not enough. 1,000 seeded paths × 2,145 book-days: MC maxDD p5/p50/p95 **14.68/23.45/40.06%** (v1 was 17.37/27.58/**46.40%**), ruin **0.10%** (v1 0.50%), `P(day≥+5%)=0.09%`, `P(day≤−5%)=0.05%`. Realized maxDD 22.90%→**22.01%**. The 40.06% p95 still exceeds the 30% breaker ⇒ `DRAWDOWN_RISK_FAILURE` still binds; de-risking in drawdowns also trimmed OOS CAGR 21.46%→**15.47%** and left OOS DSR **0.706** (v1 0.729) below 0.95 ⇒ `DSR_FAILURE`. The frozen 3×3 governor plateau **PASSED** (`plateau_confirmed`; every neighbor positive OOS expectancy). No parameter was tuned against any viewed OOS result.
- **Compliance / identifiers:** all 16 names present in C1, state `VERIFIED_COMPLIANT`; 34,268 real bars reached the engine, 133 MOCK rows excluded. Seed 42; gitSha `a38ed59`; BacktestRun `60079932-1655-449b-b4ba-859aacc6dba8`; result `results/multi-mode-book-v2-2018-01-02-2026-07-17-c1_verified_multi_mode_16.json`. The 1Y view was 0-trade (251 sessions < the 252-day momentum formation window) and is non-promotable, exactly as v1's.

### nvda-focus-v1 — CANDIDATE (E7, single-name focus book)

- **Pre-registration (written and committed BEFORE any run; USER DIRECTIVE 2026-07-19 "focus on one stock, verified Sharia compliant"):** universe = **{NVDA} ONLY**. Signal = **dual TIME-SERIES momentum with an absolute filter** — hold NVDA when BOTH the long leg (252d lookback, **skip-21** — the classic 12−1M window) AND the short leg (63d lookback, no skip) have strictly positive momentum; cash otherwise. Decide on bar-t close, fill next open; exit when EITHER leg drops to ≤0 (or becomes incomputable ⇒ fail-closed to cash). Position size = **100% of book cash when in** (single-name book BY DESIGN — the diversified name/vol/risk-budget envelope caps are lifted so the book can actually hold the one name; real liquidity/ADV, cash, gross, and the drawdown governor stay binding). Seed 42, 30% OOS reserve, costs 15 bps/side, max one position.
- **Frozen book-level drawdown governor (a-priori, from the E6/30%-gate family, NEVER from a viewed curve):** linear exposure **1.0 at ≤10% book drawdown → 0.0 (all cash) at ≥25%**, on running peak-to-trough NAV, via the engine's pre-existing down-only `drawdownExposureScalar` / `StrategyBookPolicy{drawdownStartFraction:0.10, drawdownCashFraction:0.25}` — no new engine code.
- **Frozen 3×3 plateau neighborhood:** {231,252,273} × {42,63,84} on {longLookbackBars × shortLookbackBars}, center {252,63}, 8 off-center neighbors; robustness probe only — the chosen params are NEVER updated from the sweep. Validation trials = 9 (the 3×3 grid count; dual-momentum multiple-testing precedent).
- **Single-name concentration note (binding):** concentrating a book in one name raises **VARIANCE, not expected return**; NVDA's own history carries >60% single-name drawdowns. A single-name daily-momentum signal will likely fire **<100 independent closed-position episodes** over 2018–2026 ⇒ `INSUFFICIENT_SAMPLE` is the expected, honest finding (episodes counted per the dual-momentum-rotation precedent), and concentration-driven `DRAWDOWN_RISK_FAILURE` is expected. The $1,000/day goal remains a **measured readout only**, never a promise.
- **Compliance route:** NVDA passes the C1 Tier-2 AAOIFI screen with FRESH SEC XBRL (debt 0.17% / cash+sec 0.92% / non-compliant income 1.07% ⇒ 107 bps purification, oldest input ~175d — `src/quant/universe/README.md`); the C1-verified fixed route records the card as `VERIFIED_COMPLIANT`.
- **Terminal verdict / ordered reasons (single FULL run 2018-01-02..2026-07-17):** **REJECTED** — `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`. 2,146 real bars, 0 MOCK; **42 independent closed-position episodes (23 OOS)** — 43 buy entries / 86 raw sell executions / 44 partial governor de-risk trims / 42 full exits audited separately. Full/OOS CAGR **15.87% / 24.41%**, Sharpe **0.86 / 0.98**, DSR **0.856 full / 0.533 OOS**, realized maxDD **23.00%**, hit-rate **29.60%** (trend book: many small losers, few big winners). IS/OOS split 70/30 (30% OOS reserve). No return promised.
- **Concentration finding (the answer the card exists to give):** one-stock focus did **not** honestly clear the daily target. 42 episodes ≪ the 100 floor ⇒ `INSUFFICIENT_SAMPLE` — a single-name daily-momentum signal simply cannot generate a statistically decisive sample, so no edge can be claimed. Concentration bit exactly where predicted: 1,000 seeded paths × 2,145 book-days give MC book-day maxDD p5/p50/p95 **18.28 / 27.84 / 44.71%** — the **44.71% p95 exceeds the 30% breaker ⇒ `DRAWDOWN_RISK_FAILURE`**, even though mean book exposure was only **27.5%** (momentum sits in cash ~⅔ of the time and the 10%→25% governor trims drawdowns; the book still hit 100% NVDA when fully in at low DD, as designed). Ruin **0.10%**, entry-jitter permutation **p=0.019** (the entry timing IS distinguishable from random), `P(day≥+5%)=0.51%` / `P(day≤−5%)=0.19%` over 2,145 days (mean 0.066% / std 1.212%), implausible false. Single-name focus raised variance, not risk-adjusted return; the $1,000/day goal stays a measured readout only.
- **Plateau (robustness, not tuning):** frozen 3×3 {231,252,273}×{42,63,84} on {longLookbackBars × shortLookbackBars}, center {252,63}. **FAIL — `no_positive_center_edge`**: the center's mean OOS *episode* return is **−0.94%** (the +24.41% OOS NAV CAGR is carried by a handful of big winners at 29.6% hit-rate, so the per-episode OOS mean is negative), so plateau evaluates SPIKY ⇒ `NO_PROFIT_PLATEAU_OVERFIT`. Neighbors ranged −1.33% … +3.27%; no param was ever updated from the sweep. `OOS_FAILURE` did NOT fire (walk-forward earned, OOS reserve 30%, OOS CAGR positive).
- **Compliance / identifiers:** NVDA present in C1, state `VERIFIED_COMPLIANT`; reproducible clean run + PIT integrity both true. Seed 42; gitSha `14a4501`; BacktestRun `08aa5aab-f7ea-4b3c-93dd-215a4a568481`; result `results/nvda-focus-v1-2018-01-02-2026-07-17-c1_verified_nvda.json`; dataFeed yahoo-daily. The 1Y view was 0-trade (251 sessions < the 252-day formation window) and is non-promotable. No retune, no admission — this is the terminal verdict for single-stock focus.

### bagholder-bounce v1 — REJECTED (E4, extreme-gap exhaustion)

- **Pre-registration:** source `cecc9d4`; gap-down ≥20%, further opening flush ≥5%, then the first bar with both a higher low and a close above its predecessor's high, restricted to 09:45–11:00 ET. Stop is the immutable flush low, target 2R, long-only, inverse-vol hint beneath the unchanged participation/risk/cash envelope, EOD flat.
- **Why distinct / terminal verdict:** unlike the seven prior rejected intraday parents, v1 required both discontinuous repricing and sequential seller exhaustion. **REJECTED** — `INSUFFICIENT_SAMPLE`, `OOS_FAILURE`, `DSR_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`: zero qualifying trades, so every return/risk statistic is non-informative and no edge exists to promote.
- **Evidence / limits:** the fixed 11-name sleeve supplied **247,565 captured REAL Alpaca-IEX minute bars**, 62 trading days/name and 682 symbol-days; the provider database cap prevented retaining deeper minutes, so the FULL preset is a terminal pipeline verdict over the available April–July 2026 capture, not an eight-year intraday claim. No MOCK rows, parameter changes, or repeat FULL runs.
- **Identifiers / compliance:** seed 42; DSR full/OOS 0.000/0.000; BacktestRun `b98e0145-0ba5-4ac9-bd49-44424af8cb37`; result `results/bagholder-bounce-2018-01-02-2026-07-17.json`; `UNSCREENED_EXECUTION_BLOCKED`, never eligible for execution.

### time-of-day v1 — REJECTED (E5, frozen A/B timing overlay)

- **Pre-registration:** params source `75c2399`; A requires a ≥1% opening drop then first higher-low/high reclaim at 09:45–10:00, B freezes 09:30–10:29 and requires ≥1% first-hour return then the first high/VWAP breakout at 10:30–11:00. Both are long-only, structural-stop/2R, inverse-vol hints below the unchanged envelope, and EOD flat. These clock windows are constraints, not a time sweep.
- **Terminal verdict / ordered reasons:** **REJECTED** — `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`. 262/78 full/OOS trades, mean **−0.23%/trade**, hit **34.35%**, full/OOS CAGR **−7.08%/−30.28%**, DSR **0.020/0.448**, actual maxDD **50.51%**. Timing did not rescue the lane.
- **Tail / robustness:** 1,000 seeded trade paths gave maxDD p5/p50/p95 **31.34/51.16/65.45%**, ruin **47.00%**, permutation p=0.992, and no declared profit plateau. The one post-diagnostic code change (`6fde71d`) only memoized the existing ET conversion by immutable Date object; focused branch decisions stayed green and no parameter/rule changed.
- **Evidence / identifiers:** 247,565 captured REAL Alpaca-IEX minute bars, 62 trading days/name / 682 symbol-days, seed 42; BacktestRun `53430eca-132e-48a0-aa77-54f75e64dede`; result `results/time-of-day-2018-01-02-2026-07-17.json`. FULL preset is limited to the available April–July 2026 minute capture; `UNSCREENED_EXECUTION_BLOCKED`, never executable.

### coint-statarb-long-leg v1 — REJECTED

- **Reason codes:** `INSUFFICIENT_SAMPLE`, `SHARIA_UNVERIFIABLE`. The run produced 9 trades (≪ the 100-trade floor), so no statistical edge can be claimed and every downstream metric is non-informative. Rejection binds v1 + this run only; a v2 (looser gate / deeper universe) is a NEW candidate, never a retune of v1 on viewed data.
- **Root cause (not edge — sample):** the halal daily spine has only ~10 names with ≥252d of shared REAL history (AAPL, ADBE, AMZN, COST, META, NFLX, PEP, QCOM, SBUX, TSLA; MSFT/NVDA are MOCK-only ⇒ excluded, GOOGL sparse at 130 real bars). ~13 further names carry only 102 real bars (< the 252d formation window) ⇒ 0 trades. Cross that thin universe with a strict Engle–Granger 5% ADF gate (t ≤ −3.34) and a k=2σ dislocation trigger and joint qualifying events are rare: only ADBE(1), AMZN(4), META(1), NFLX(2), QCOM(1) ever fired.
- **History NOT deepened (honest tooling limit):** `src/quant/data/ingest.ts` is a forward-incremental updater — for any symbol that already has a latest bar it computes `days = elapsedDays + OVERLAP`, so it cannot backfill history for the 102-bar names (already current to 2026-07-10). Deepening the formation window via the ALLOWED keyless-Yahoo path was therefore not achievable with existing machinery (no data-code changes are in scope). Before = after; see the bar-count table in the session dispatch. No MOCK bar reached the simulator (585 GOOGL MOCK bars excluded in-path).
- **Scope/compliance:** 2022-07-01..2026-07-10, daily cadence, 23 NASDAQ-halal names present (of the declared 25; MSFT/NVDA absent — no real bars), real Yahoo daily spine; last 30% held out OOS. Sharia state = `DECLARED_HALAL_RESEARCH_UNIVERSE / UNSCREENED_FOR_EXECUTION`: long-only cash charter (short leg vetoed and never traded), but the artifact persists no PIT AAOIFI verdict, so executable admission is blocked independently of the sample rejection.
- **IS/full vs OOS (all non-informative at n=9):** pure IS = n/a — the artifact persists full-history, not a separate IS slice. Full: 9 trades, CAGR 4.78%, Sharpe 5.05 (⇒ `implausible`=true, an artifact of a 9-trade curve, NOT an edge), DSR 0.823, maxDD 9.89%, hit 55.56%, mean +2.09%/trade net, turnover 9; Calmar/exposure/avg win-loss = n/a — not persisted. OOS: trade count n/a — artifact repeats total count; CAGR −5.07%, Sharpe −40.52, DSR 0.005.
- **Tail/integrity:** MC maxDD p5/p50/p95 = 3.92/10.18/20.52% (within the 30% breaker — but a 9-trade bootstrap is non-informative), risk-of-ruin 0.00%, entry-jitter permutation p=0.206 (edge indistinguishable from random entry timing), `P(day≥+5%)=0.00%` and `P(day≤−5%)=0.00%` over 11,306 observed daily returns (mean 0.00% / std 0.03%), Kelly 0.0719 → clamped qty 14.81; walk-forward=false, profit-plateau=false, implausible=true. These figures document the run; they are not evidence of safety.
- **Reproducibility:** strategy/params `coint-statarb-long-leg@v1` (252d formation, k=2, EG-5% ADF≤−3.34, mean-reversion exit); seed 42; git SHA `0c181d6`; `BacktestRun` id `2be31abd-aea0-489a-9144-8f85aa1fb647`; result `results/coint-statarb-long-leg-2022-07-01-2026-07-10.json`; data feed `yahoo-daily`.

## Deferred — data-gated (Tier D)

| id | missing data | note |
|---|---|---|
| l2-hidden-bid-scalp | Level-2 order book feed | no free source; revisit if feed acquired |
| options-flow (gamma/max-pain/OI) | options chain + OI feed | free sources unreliable; park |
| dark-pool-footprints | ATS/dark-pool prints | paid feeds only; park |
| merger-arb | corporate-events feed + shorting (Sharia) | doubly gated; park |

## Excluded — Sharia (Tier S)

Short-side setups (broken-parabolic short, fake-halt rug, AH pump fade, earnings-rip fade, FOMC fade,
merger-arb short) are EXCLUDED from execution (no shorting). Long mirrors kept where they exist:
earnings/FOMC panic-flush → long bounce (folded into bagholder-bounce / time-of-day). Educational
content may teach them flagged per DR-5.

### multi-mode-book-v3 — REJECTED (R4-E8, sukuk-ballast substitution)

- **Pre-registration / scope (BEFORE the run, commit `d562dc1`):** v3 = multi-mode-book-v2 (`a38ed59`) with EVERY sleeve, allocator, and governor parameter inherited UNCHANGED and ZERO new tunable parameters. The single structural substitution: whenever the book holds idle capital — allocator residual cash, absolute-filter cash states, or governor-de-risked capital — it parks in the SPSK sukuk ETF instead of flat cash (fill next open, same 15 bps/side cost, same envelope/ADV caps). SPSK is SOLD FIRST to fund equity entries (equity exits always run first); before SPSK's first real bar (2019-12-31) idle capital fail-closes to cash. Implemented as a policy-gated engine mechanic (`StrategyBookPolicy.idleBallastSymbol`) — byte-identical when unset (all 32 existing engine tests green), ballast excluded from the drawdown governor's risk exposure and never governor-trimmed, ballast fills kept out of `fills`/`tradeRecords`. Frozen 3×3 governor plateau {8,10,12}×{20,25,30} inherited verbatim; 3 trials; seed 42; 30% OOS; FULL 2018-01-02→2026-07-17.
- **Terminal verdict / ordered reasons:** **REJECTED** — `OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`. The hypothesis is FALSIFIED on BOTH pre-registered binding axes.
- **v2 → v3 comparison on the two binding axes (the whole point of this run):**
  - **DSR axis (carry was meant to replace the governor's cash drag):** WORSE, not better. OOS DSR **0.706 → 0.001**; full DSR 0.860 → 0.150; full CAGR **15.47% → −1.08%**; OOS CAGR **+15.47% → −7.75%**. Root cause: the MarketBar spine stores RAW (unadjusted) YAHOO closes, so SPSK's coupon/distribution carry — the entire economic rationale for a sukuk ballast — is NOT in the price series; the raw close FELL 20.10 → 17.92 over the window. Parking idle/de-risked capital in a price-only, coupon-stripped, downward-drifting instrument DRAGGED the book below the 0% that plain cash would have preserved.
  - **DRAWDOWN axis (low equity correlation was meant to cut the tail):** WORSE, not better. MC book-day p95 **40.06% → 50.65%**; realized maxDD **22.01% → 39.22%**. Root cause: v2's governor de-risked into CASH (zero drawdown contribution); v3's governor de-risks into SPSK, which carried its OWN 2022 rate-shock drawdown correlated with the equity selloff — the low-correlation assumption failed exactly when it was needed, so the "ballast" ADDED tail risk instead of dampening it.
- **Full vs OOS:** full = 204 closed equity trades, CAGR −1.08%, Sharpe −0.06, DSR 0.150, maxDD 39.22%, hit 43.22%, expectancy **+3.43%/equity trade net**, average book exposure 81.14% (incl. ballast), turnover notional $22,541,956 (ballast round-trips: 1,901 ballast fills). OOS = 0 closed equity episodes in the last 30% (the OOS book sat heavily in de-risked SPSK ballast, so its −7.75% CAGR / −1.42 Sharpe / 0.001 DSR is the ballast's own price decline). The equity sleeves themselves were NOT the failure — entry-jitter permutation p=0.004 and +3.43%/trade show a real sleeve edge; the ballast destroyed the book.
- **Tail / robustness:** 2,145 book days; `P(day≥+5%)=0.00%`, `P(day≤−5%)=0.05%`; MC book-day maxDD p5/p50/p95 = 16.34 / 29.57 / 50.65%; risk of ruin 2.70% (limit 5%). The frozen 3×3 governor plateau FAILED (SPIKY): the {start*}×{cash20} column turned OOS expectancy negative (−2.70% / −2.12% / −0.57%) — tighter cash-out thresholds force earlier parking into the falling ballast.
- **Compliance / identifiers:** the 16 equity sleeve names keep C1 Tier-2 `VERIFIED_COMPLIANT` (`FUND_LEVEL_PURIFICATION_ONLY`); 35,980 real YAHOO/ALPACA bars reached the engine, 0 MOCK. SPSK carries the SEPARATE ballast disclosure, recorded VERBATIM from the i18n-fintech instrument ruling (R4-E8), no unqualified compliance claim:
  - **RULING:** "SPSK admissible as idle-capital ballast, paper-trading research ONLY (conditional). Tier-1 fund-level treatment, reasonCode FUND_LEVEL_PURIFICATION_ONLY; NEVER label 'AAOIFI-compliant' outright — AAOIFI governs sukuk structuring, not this ETF's certification. Required disclosure: RUSHD follows the AAOIFI-aligned (Gulf) reading of secondary sukuk trading; some Dow Jones Sukuk Index constituents may carry murabaha-heavy tails outside the strictest AAOIFI Standard No. 17 view (bay' al-dayn divergence vs the Malaysian school — recorded, not silently resolved). Pre-real-money conditions: SP Funds Sharia board certificate verification (AAOIFI Std 17 scope), CMA gate, live purification disclosure, user-facing divergence note."
  - **Card label EN:** "Sukuk (SPSK) — index-provider Sharia-screened (Dow Jones Sukuk methodology, AAOIFI-aligned); fund-level purification only; paper-trading research asset, not a cash/interest substitute."
  - **Card label AR:** "صكوك (SPSK) — مُفحوصة شرعياً من مزوّد المؤشر (منهجية مؤشر داو جونز للصكوك، متوافقة مع معايير الأيوفي)؛ التطهير على مستوى الصندوق فقط؛ أصل بحثي للتداول التجريبي (ورقي)، وليس بديلاً نقدياً أو ربوياً."
- **Data limitation (recorded, not hidden):** the raw-close/no-coupon SPSK price series structurally under-measures sukuk carry. A future adjusted-close (total-return) SPSK spine could re-open the DSR axis, but that would be a NEW candidate on new data, never a retune of this run. Even so, the DRAWDOWN-axis failure (2022 sukuk–equity co-movement) is a real economic finding independent of the coupon-data gap.
- **Reproducibility:** `multi-mode-book-v3@v3`; params = v2's a-priori governor {10%,25%} + 20/40/40 allocator, SPSK ballast, 3 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; git SHA `d562dc1`; `BacktestRun` `367faa48-6c8c-4155-8de7-febba2201fd6`; result `results/multi-mode-book-v3-2018-01-02-2026-07-17-c1_verified_multi_mode_16_spsk_ballast.json`; shared engine. The 1Y evidence view `779b8c21-cb1d-4e22-82e0-b81939bace42` is non-promotable.

### halal-risk-parity-core v1 — REJECTED (R-RP1, first correlation/volatility-aware weighting mechanism tested)

- **Pre-registration / scope (BEFORE the run, commit `6e5a095`):** a structurally NEW mechanism class versus every prior T2 setup — NO directional/timing signal at all. Monthly rebalance; weight_i ∝ 1/σ_i (252-trading-day trailing sample stdev of daily returns), normalized to sum 1 across rankable names, water-filling capped at 20%/name; universe is the C1-verified top-40-by-dollar-volume sleeve (`buildVerifiedUniverse` → `selectDollarVolumeSleeve`, maxNames=40); breadth floor 15 names else cash. Frozen 3×3 plateau grid {lookbackDays 231/252/273} × {perNameCap 0.15/0.20/0.25}, 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. Params were never altered after any diagnostic; the sibling `halal-markowitz-core` (mean-variance tangency weighting) is a separate, independently-built dispatch — no coordination.
- **Terminal verdict / ordered reasons:** **REJECTED** — `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. The mechanism did NOT clear promotion, but it PARTIALLY validated the a-priori hypothesis on the exact axis it was built to fix.
- **Did breadth + vol-aware sizing fix the concentration/drawdown failure that killed every prior setup?** Partially. MC book-day maxDD p95 is **45.90%** — still above the 30% breaker (binding failure), but well below the 37-59% range every prior 1-6-position concentrated setup produced on this same data/cost model (e.g. ts-momentum-halal-basket-v4 41.49%, dual-momentum-rotation 23.15% — note dual-momentum is single-name-at-a-time so not a fair breadth comparison; bollinger-mr-long-v3 58.45%, multi-mode-book-v1 46.40%). Average book exposure was high (87.24%) with 20%+ names typically held per rebalance (turnover notional $1,898,250), i.e. genuine multi-name diversification was achieved as designed — it reduced but did not eliminate tail risk, since inverse-vol weighting is still long-only equity beta with no hedge.
- **Full vs OOS:** full = 90 active book-months (1,631 raw closed-name executions / 3,414 fills), CAGR **24.64%**, Sharpe 1.13, DSR **0.958**, maxDD (realized) 31.89%, hit 48.62%, expectancy +2.270%/trade net (permutation observed mean), turnover 1,631. OOS = 32 active book-months, CAGR **39.62%**, Sharpe 1.75, DSR **0.898**, maxDD (realized) 23.36%, hit 57.39%, turnover 576. Historical-comparison series (trade-sequenced curve, 2021-10-05→2026-07-17, no dividends): strategy 100→**302.09**, SPY 100→171.62, SPUS 100→192.40 — the model materially outperformed BOTH benchmarks over the window it actually traded, but this raw-return comparison is descriptive only and does not override the OOS-sample/DSR/drawdown gates below.
- **Tail/integrity:** 2,145 book days; `P(day≥+5%)=0.37%`, `P(day≤−5%)=0.37%`; MC book-day maxDD p5/p50/p95 = 19.68/28.79/**45.90%**; risk of ruin 0.20% (within the 5% limit); seeded sign-flip permutation p=**0.000**. Walk-forward EARNED (81,934 PIT-guarded decision windows), 30% OOS holdout, data/PIT integrity, and clean reproducibility all PASS; profit-plateau **PASSED** (`plateau_confirmed`: all 8 frozen neighbors same-sign, max degradation 8.8%, well within bound) — the edge is NOT parameter-fragile, unlike most prior rejections in this ledger. The binding misses are sample size (90 < 100 book-months) and OOS DSR (0.898 < 0.95); full DSR (0.958) itself clears the bar.
- **Expert finding:** this is the strongest evidence yet that concentration, not signal quality, was the dominant driver of the prior 20 setups' tail-drawdown failures — a PURE sizing/diversification mechanism with zero directional edge produced a materially better MC tail (45.90% vs 37-59%) and a clean, non-spiky plateau. It still falls short of the 30% breaker and the 100-observation floor, so it is REJECTED, not "close" — no retune. A future candidate that COMBINES this weighting mechanism with a genuine directional signal (rather than testing weighting in isolation) is the indicated next step, as a NEW setup, never a version bump of this file.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (Tier-1 SPUS / Tier-2 AAOIFI-XBRL screened 40-name sleeve, `FUND_LEVEL_PURIFICATION_ONLY` / index-provider-screened per name); 81,974 resolved real YAHOO/ALPACA bars, 26 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch.
- **Reproducibility:** `halal-risk-parity-core@v1`; params lookback 252d / cap 20% / floor 15 / maxNames 40, 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; git SHA `6e5a095`; `BacktestRun` `214b6a94-9437-4a18-bf9d-efe42652d197`; result `results/halal-risk-parity-core-2018-01-02-2026-07-17-c1_verified_40.json`; shared engine. No retune/admission; no second FULL run performed.

### halal-markowitz-core v1 — REJECTED

- **Pre-registration / hypothesis (BEFORE the run, commit `9e2eafe`):** the FIRST structurally new weighting mechanism tried in this lab — every prior multi-name T2 setup is a single-signal timing bet (momentum rank, Bollinger mean-reversion, dual-momentum rotation, allocator blends) holding 1–6 simultaneous positions, and the dominant recurring binding failure across ALL of them is `DRAWDOWN_RISK_FAILURE` (MC p95 book-day drawdown 37–59%). This setup asks: does Markowitz mean-variance (tangency) weighting over a genuinely diversified 20–40-name C1-verified halal sleeve tame that tail? Monthly rebalance at month-end close, filled next-open; C1 `buildVerifiedUniverse` + `selectDollarVolumeSleeve(maxNames=40)`; rankable = ≥252 trailing trading days of continuous history; breadth floor 15 names else cash; weighting via `markowitzFrontier()` (reused verbatim from `src/quant/portfolio/frontier.ts`, seed 42, 10,000 long-only Dirichlet-sampled portfolios, risk-free rate 0) — the TANGENCY candidate's weights, clipped (never redistributed) at a 20% per-name cap. Frozen 3×3 plateau {lookbackDays: 231/252/273} × {perNameCap: 0.15/0.20/0.25}, center = {252, 0.20}, 9 trials. Shared risk-envelope engine unchanged throughout (name/gross/ADV/cash clamps stay fully binding). C1-verified sleeve resolved to 40 large/mega-cap NASDAQ tech-and-growth-heavy names (MU, NVDA, SNDK, AAPL, TSLA, AMD, MSFT, GOOGL, AVGO, MRVL, AMAT, ORCL, LRCX, STX, KLAC, IBM, LLY, LITE, GEV, GLW, QCOM, CSCO, PANW, TXN, XOM, JNJ, CRWD, CRM, NOW, ABBV, ADI, VRT, COHR, ANET, TER, PEP, ISRG, PG, MPWR, HD).
- **Terminal verdict / ordered reasons:** **REJECTED** — `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. The core hypothesis is FALSIFIED: MC p95 book-day drawdown (50.78%) is not merely above the 30% breaker, it is WORSE than several of the concentrated single-signal setups this mechanism was designed to beat (bollinger-mr-long-v3 58.45%, ts-momentum-v3 49.91%, ts-momentum-v4 41.49%, multi-mode-book-v1/v2 46.40%/40.06% — halal-markowitz-core lands squarely inside that same failed band, not below it).
- **Why the hypothesis failed:** the C1-verified top-40-by-dollar-volume sleeve is dominated by large-cap NASDAQ tech/growth/semiconductor names — exactly the names with the HIGHEST pairwise correlation and the LARGEST shared beta to systematic drawdowns (2022 rate-shock, tech selloffs). Markowitz tangency weighting optimizes for idiosyncratic (stock-specific) diversification within the covariance matrix it is given, but it cannot diversify away a dominant common factor that the entire investable sleeve shares. Correlation-aware weighting genuinely differs from equal-weight or momentum-rank (the frozen plateau sweep — a first for a multi-name T2 setup in this lab — PASSED, proving the mechanism itself is parameter-robust, not spurious), but robustness to its OWN parameters is not the same as robustness to systematic market risk. The remedy this run rules out is "diversify by covariance across more names within the same style/sector concentration"; a genuine fix would need either explicit factor/beta-neutralization, a broader style-diversified sleeve (value/defensive names alongside growth), or a portfolio-level volatility/drawdown governor layered on top (not tested here — out of scope for a v1 mechanism test).
- **Full vs OOS:** full = 90 active book-months, CAGR 31.02%, Sharpe 1.18, DSR 0.970, maxDD 33.99%, hit-rate 47.65%, expectancy +2.84%/active month net (permutation.observedMean), 3,414 fills / 1,644 raw name-exits, average book exposure 86.82%, gross exposure cap audit 1.00 (envelope binding correctly, never breached). OOS = 32 active book-months, CAGR 56.57%, Sharpe 1.75, DSR 0.899 (binding miss — just under the 0.95 gate). Pure IS and Calmar are not separately persisted.
- **Tail/integrity:** 2,145 book days; `P(day≥+5%)=0.56%`, `P(day≤−5%)=0.70%`; MC book-day maxDD p5/p50/p95 = 23.10/33.03/50.78%; risk of ruin 0.20% (limit 5%, passes); seeded sign-flip permutation p=0.000. Walk-forward EARNED (81,934 PIT-guarded decision windows), 30% OOS holdout, data/PIT integrity, and clean reproducibility all pass; profit-plateau PASSED (`plateau_confirmed`, all 8 frozen neighbors same-sign, max degradation 16.2% — well within bound). The sample (90<100 active months — a structural ceiling of the FULL period's 2018-01-02 start plus a 252-day trailing-lookback warm-up, not a wiring defect) and DSR/drawdown merit gates are the binding failures.
- **Sharia:** C1 `VERIFIED_COMPLIANT` (all 40 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL screened); long-only cash, no ballast, no leverage, no short.
- **Reproducibility:** `halal-markowitz-core@v1`; params = 252d trailing lookback, 20% per-name cap, 15-name breadth floor, seed 42, 10,000 portfolios, 9 frozen trials; 15 bps/side costs and the unchanged risk/ADV/cash envelope. Seed 42; git SHA `9e2eafe`; `BacktestRun` `354b784e-a43f-4694-9292-acf792927eaa`; result `results/halal-markowitz-core-2018-01-02-2026-07-17-c1_verified_40.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) produced 0 fills — the 1-calendar-year window (≈251 trading days) is exactly one day short of the frozen 252-day lookback, so no month ever cleared the rankable floor within that short window; this is a calendar-arithmetic property of the fixed lookback, not a crash or a wiring defect (engine resolved the 40-name C1 sleeve, processed 10,080 bars, and produced a clean non-terminal report card with zero errors).

### halal-momentum-risk-parity-core v1 — REJECTED (R6, first momentum-selection + risk-parity-sizing combination tested)

- **Pre-registration / hypothesis (BEFORE the run, commit `fe224b9`):** COMBINES two mechanisms already independently validated this session over the C1 Sharia-verified halal universe at real breadth. Selection: dual-momentum (`dualMomentumRotation`'s exact proven formula/params — lookbackBars=252, skipRecentBars=21, absoluteThreshold=0 — reused via the imported `dualMomentumMetrics` helper, never a different definition); at each month-end, from names with absolute momentum > 0, select the top `selectionFraction=0.5` (top half) by relative-momentum rank. Sizing: `inverseVolatilityWeights`/`capAndRedistribute` (imported UNCHANGED from `halal-risk-parity-core`) over ONLY the selected survivors' 252d trailing daily-return σ, 20% per-name cap. Universe: C1-verified top-60-by-dollar-volume sleeve (wider than both single-mechanism siblings' 40, since this setup ranks-then-selects a subset). Breadth floor 15 SELECTED names else cash. Frozen 3×3 plateau {lookbackBars 231/252/273} × {selectionFraction 0.4/0.5/0.6}, perNameCap fixed at 0.20 (not re-swept), 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. The a-priori hypothesis: layering the proven directional momentum signal on top of the proven best-yet (45.90% MC p95) risk-parity weighting, at genuine breadth (unlike dual-momentum-rotation's single-name sizing), would clear BOTH the sample-size gate and push the drawdown/DSR gates further than either single-mechanism sibling — because the absolute-momentum filter is a textbook tail-risk mitigant (de-risks toward cash on a broad trend reversal) that pure diversification alone lacks. Params were never altered after the (expected, structurally 0-fill) 1Y diagnostic.
- **Terminal verdict / ordered reasons:** **REJECTED** — `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`.
- **Did combining the proven signal with the proven sizing beat both single-mechanism siblings, as hypothesized?** No — it is WORSE than both on the exact two axes it was designed to fix. MC book-day maxDD p95 is **60.18%**, above both `halal-risk-parity-core`'s 45.90% and `halal-markowitz-core`'s 50.78% — the momentum overlay did NOT act as the hypothesized tail-risk mitigant; it made the tail materially worse. OOS DSR **0.753** is also below both siblings (0.898, 0.899). The likely mechanism: dual-momentum selection systematically concentrates the book into whichever names are CURRENTLY trending together — in this window, a correlated semiconductor/AI-rally cluster (top-P&L names: NVDA, LITE, MU, GLW, STX, CIEN, KLAC, AMD) — and correlated momentum clusters are exactly the names that crash together in a momentum reversal (the well-documented "momentum crash" phenomenon), actively working AGAINST the idiosyncratic-diversification benefit the risk-parity sizing otherwise provides. The absolute-momentum cash filter did not offset this: average book exposure stayed high (74.84%), so the de-risk-to-cash mechanism did not trigger fast/hard enough to prevent the tail blowup.
- **Full vs OOS:** full = 89 active book-months (32 OOS; 1,072 raw closed-name executions, 2,073 fills), CAGR **21.46%**, Sharpe 0.87, DSR **0.836**, realized maxDD 34.13%, hit-rate 42.28%, expectancy +2.107%/trade net (permutation observed mean), average book exposure 74.84%, turnover notional $7,303,336. OOS = 32 active book-months, CAGR **51.17%**, Sharpe 1.41, DSR **0.753** (binding miss, well short of 0.95 — WORSE than both siblings' ~0.90), realized maxDD 30.48%, hit-rate 56.92%. Historical-comparison series (trade-sequenced curve, 2021-10-05→2026-07-17, no dividends): strategy 100→**376.50**, SPY 100→171.62, SPUS 100→192.40 — the raw-return curve beat both benchmarks by an even wider margin than either sibling, which is itself a flag: high raw returns riding a concentrated momentum cluster are exactly what produces both the DSR shortfall and the drawdown-axis failure below; this descriptive comparison does not override the rigor gates.
- **Tail/integrity:** 2,145 book days; `P(day≥+5%)=0.61%`, `P(day≤−5%)=1.03%`; MC book-day maxDD p5/p50/p95 = 26.31/38.35/**60.18%**; risk of ruin 1.80% (within the 5% limit); seeded sign-flip permutation p=**0.002**. Walk-forward EARNED (123,475 PIT-guarded decision windows), 30% OOS holdout, data/PIT integrity, and clean reproducibility all PASS; profit-plateau **PASSED** (`plateau_confirmed`, all 8 frozen neighbors same-sign, max observed degradation 21.8% — well within the 50% bound), proving the edge is not parameter-fragile even though it is economically the wrong edge for this book's tail-risk goal. The binding misses are sample size (89 < 100 active book-months, one short of both siblings' 90 — the same structural FULL-period-start-plus-warmup ceiling, not a defect) and OOS DSR (0.753 < 0.95); drawdown is the decisive new finding.
- **Expert finding:** the hypothesis is cleanly FALSIFIED, and the falsification is informative: momentum selection and risk-parity sizing are not simply additive/complementary mechanisms — the selection step actively creates the correlation structure (a trending cluster) that the sizing step's own-volatility-only weighting is blind to (by design; it was built correlation-BLIND, per `halal-risk-parity-core`'s a-priori spec). Combining a signal that concentrates correlation with a sizer that cannot see correlation reproduces, and here worsens, the exact systematic-beta tail-risk failure `halal-markowitz-core` also hit via a different route (dominant common factor exposure). A genuine next step would need EITHER a covariance-aware sizer applied AFTER momentum selection (testing whether Markowitz-style correlation-awareness fixes what pure inverse-vol cannot on a momentum-selected subset) OR an explicit sector/factor-exposure cap layered on top of selection — both are NEW setups, never a version bump of this file.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (all 60 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 123,535 resolved real YAHOO/ALPACA bars, 32 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch.
- **Reproducibility:** `halal-momentum-risk-parity-core@v1`; params lookbackBars 252 / skipRecentBars 21 / absoluteThreshold 0 / selectionFraction 0.5 / perNameCap 20% (fixed) / floor 15 / maxNames 60, 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; git SHA `fe224b9`; `BacktestRun` `146d4ed7-306f-4999-9943-f81f03eb1334`; result `results/halal-momentum-risk-parity-core-2018-01-02-2026-07-17-c1_verified_60.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) produced 0 fills — identical calendar-arithmetic artifact to both siblings (1-calendar-year window ≈251 trading days, one day short of the frozen 252-day lookback); not a wiring defect. No retune/admission; no second FULL run performed.

### halal-sector-capped-risk-parity-core v1 — REJECTED (R7, ISOLATED sector-exposure-cap A/B vs the halal-risk-parity-core baseline)

- **Pre-registration / hypothesis (BEFORE the run, commit `70c2fa6`):** an ISOLATED controlled test of ONE new variable (a sector-exposure cap) against the already-known `halal-risk-parity-core` v1 baseline (REJECTED, MC p95 45.90%, OOS DSR 0.898). Every other dimension — the C1-verified top-40-by-dollar-volume sleeve, 252d lookback, 20% per-name cap, and the inverse-vol weighting base (weight_i ∝ 1/σ_i) — is held IDENTICAL to that sibling, so any change in the terminal numbers is attributable to the sector cap alone. Weighting pipeline: (1) raw normalized inverse-vol weights via the imported UNCHANGED `inverseVolatilityWeights`; (2) a NEW `capSectorExposure` function (water-filling at the sector-aggregate level, modeled on `capAndRedistribute`'s own algorithm) caps any sector's summed weight at `sectorCap=0.30`, redistributing the excess proportionally to other sectors, iterated to convergence; (3) THEN the existing UNCHANGED `capAndRedistribute` (20% per-name cap) — order matters, sector cap first. Sector classification is a static, honestly-sourced GICS-style reference table (real public company sector data, compiled 2026-07-20) covering the documented 40-name sleeve; `configureUniverse` fails fast if any resolved symbol is unclassified. A-priori hypothesis: capping any single sector at 30% forces the book off its uncapped concentration in Semiconductors/Software/Tech Hardware into less-correlated sectors, reducing MC p95 maxDD below 45.90% and/or lifting OOS DSR above 0.898 — an empirical bet, not a certainty, explicitly flagged as possibly no better if the tech-heavy universe simply has no genuine diversification to buy. Frozen 3×3 plateau {sectorCap 0.25/0.30/0.35} × {lookbackDays 231/252/273}, perNameCap fixed at 0.20 (not re-swept), 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. Params were never altered after the (expected, structurally 0-fill) 1Y diagnostic.
- **Terminal verdict / ordered reasons:** **REJECTED** — `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`.
- **Did the isolated sector cap improve, worsen, or not change MC p95 drawdown and OOS DSR vs the halal-risk-parity-core baseline (45.90% / 0.898)?** It improved BOTH, but only marginally, and neither cleared its gate. MC book-day p95 maxDD is **44.95%** vs the baseline's 45.90% — a **−0.95 percentage point** improvement (directionally confirms the hypothesis: forcing sector spread genuinely reduced the tail slightly) but still far above the 30% breaker. OOS DSR is **0.900** vs the baseline's 0.898 — a **+0.002** improvement, effectively unchanged and still well short of the 0.95 gate. Full DSR is **0.958**, identical to the baseline to three decimals. Active book-months are **90** (identical to the baseline's 90), OOS **32** (identical) — the sector cap did not change the sample-size ceiling, as expected (it only reshapes weights within already-ranked months, never changes which months clear the breadth floor). The likely mechanism: the C1-verified 40-name sleeve resolves to ~17/40 Semiconductor-classified names (per the documented sleeve), so the 30% cap DOES bind materially every rebalance, but the sectors it redistributes into (Software, Tech Hardware, Health Care, Consumer Discretionary) still share meaningful systematic beta with the capped sector in broad market drawdowns (2022) — sector labels are a coarser proxy for "uncorrelated" than true covariance, so the cap buys some but not much genuine diversification.
- **Full vs OOS:** full = 90 active book-months (1,632 raw closed-name executions, 3,417 fills), CAGR **24.22%** (vs baseline 24.64%), Sharpe 1.13, DSR **0.958**, realized maxDD 31.93% (vs baseline 31.89%), hit-rate 48.53%, expectancy +2.232%/trade net (permutation observed mean), turnover 1,632. OOS = 32 active book-months, CAGR **39.26%** (vs baseline 39.62%), Sharpe 1.76, DSR **0.900** (binding miss, essentially tied with baseline's 0.898), realized maxDD 23.35%, hit-rate 57.39%. Historical-comparison series (trade-sequenced curve, 2021-10-05→2026-07-17, no dividends): strategy 100→**300.49** (vs baseline's 302.09 — essentially identical, slightly lower), SPY 100→171.62, SPUS 100→192.40 — both this setup and its baseline beat both benchmarks by a similar wide margin on this descriptive measure; the sector cap did not materially change the raw-return curve, consistent with it being a risk-shaping (not return-shaping) mechanism as designed.
- **Tail/integrity:** 2,145 book days; `P(day≥+5%)=0.37%`, `P(day≤−5%)=0.37%` (identical to baseline); MC book-day maxDD p5/p50/p95 = 19.51/28.33/**44.95%**; risk of ruin 0.20% (within the 5% limit); seeded sign-flip permutation p=**0.000**. Walk-forward EARNED, 30% OOS holdout, data/PIT integrity, and clean reproducibility all PASS; profit-plateau **PASSED** (`plateau_confirmed`, all 8 frozen neighbors same-sign, max observed degradation **6.3%** — the tightest/most robust plateau of any T2 setup in this ledger, tighter than the baseline's own 8.8%). The binding misses are identical in kind to the baseline: sample size (90 < 100 active book-months, the same structural FULL-period-start-plus-warmup ceiling) and OOS DSR (0.900 < 0.95); MC p95 drawdown (44.95%) remains above the 30% breaker.
- **Expert finding:** the isolated A/B is a clean, informative NEGATIVE result — the sector cap is a real, structurally different lever (it constrains WHAT is held, unlike all three prior reweighting-only mechanisms) and it DID move both target metrics in the hypothesized direction, unlike `halal-momentum-risk-parity-core` which moved both in the WRONG direction. But the magnitude is small (−0.95pp MC p95, +0.002 OOS DSR) — nowhere near enough to change the terminal verdict. This confirms the session's cumulative diagnosis more precisely: GICS-style sector labels are too coarse a proxy for the true correlation structure driving this universe's systematic tail risk; a genuinely different diversification source (real covariance-aware weighting on top of a sector-diversified selection, a broader style mix including non-tech factor tilts, or actual cross-market breadth via a second exchange) would be needed to move the needle materially — none of which is a version bump of this file. Real TASI data remains unavailable in this DB (0 MarketBar rows, market='TASI'), so cross-market diversification stays out of scope without a separate data-ingestion project.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (all 40 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 81,974 resolved real YAHOO/ALPACA bars, 32 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch.
- **Reproducibility:** `halal-sector-capped-risk-parity-core@v1`; params lookbackDays 252 / sectorCap 30% / perNameCap 20% (fixed) / floor 15 / maxNames 40, 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; git SHA `70c2fa6`; `BacktestRun` `9dfa79cd-5498-4379-86d0-b3c46e0621ef`; result `results/halal-sector-capped-risk-parity-core-2018-01-02-2026-07-17-c1_verified_40.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) produced 0 fills — identical calendar-arithmetic artifact to all three T2 siblings (1-calendar-year window ≈251 trading days, one day short of the frozen 252-day lookback); not a wiring defect. No retune/admission; no second FULL run performed.

### halal-sector-capped-risk-parity-wide v1 — REJECTED (breadth follow-up to R7, LAST test in this in-universe research line)

- **Pre-registration / hypothesis (BEFORE the run, commit `79ad4c3`):** the SAME `halal-sector-capped-risk-parity-core` mechanism, structurally UNCHANGED — `capSectorExposure` and `sectorCappedInverseVolatilityWeights` imported verbatim, never reimplemented, applied over identical 252d lookback / 30% sector cap / 20% per-name cap / 15-name breadth floor — with exactly ONE variable changed: the C1-verified sleeve widened from the narrow sibling's 40-name isolated-A/B sleeve to the QDR-8 ceiling of `maxNames=100` (`selectDollarVolumeSleeve`), close to half of every real symbol in this DB (228 distinct NASDAQ symbols). Direct motivation: the narrow sibling's own diagnosis that its 40-name sleeve resolved to 17/40 (42.5%) Semiconductors, leaving the 30% cap almost no genuine sector alternative to redistribute into, which the narrow A/B's card flagged as the likely reason its effect was real but tiny (MC p95 44.95% vs the `halal-risk-parity-core` baseline's 45.90%, −0.95pp; OOS DSR 0.900 vs 0.898, +0.002). A-priori hypothesis: at 100 names the same cap would have real Health Care/Industrials/Consumer Staples/Materials/Energy/Real Estate/Utilities constituents to redistribute into, and should therefore MEANINGFULLY beat the narrow A/B's marginal effect on MC p95 and/or OOS DSR — an empirical bet, explicitly flagged as possibly not materializing if the real top-100-by-dollar-volume NASDAQ resolution still skews mega-cap tech, or if a noisier 100-name volatility estimate independently worsens the tail. A NEW, independently-built GICS-style `SYMBOL_SECTOR` table (never importing/editing the narrow sibling's private table) was required to cover the wider resolution; the 1Y diagnostic surfaced exactly one gap (`FIX`, Comfort Systems USA — Industrials/Construction & Engineering), added honestly per the acceptance criteria's authoritative-gap-list instruction (commit `48d360e`), not a hypothesis retune. Frozen 3×3 plateau {sectorCap 0.25/0.30/0.35} × {lookbackDays 231/252/273}, 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17.
- **Terminal verdict / ordered reasons:** **REJECTED** — `INSUFFICIENT_SAMPLE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. (A first FULL attempt, executed against an uncommitted `FIX` classification fix, was correctly flagged `REPRODUCIBILITY_FAILURE` by `isReproducibleRun`'s dirty-quant-tree check — BacktestRun `0917b051-722b-4adc-91be-821c363de539` — and is superseded by the clean-tree run below, identical numbers, run once the fix was committed.)
- **Did widening from 40 to 100 names meaningfully scale the sector cap's effect vs the narrow A/B (44.95% MC p95 / 0.900 OOS DSR)?** Yes on drawdown, no on DSR — a genuinely mixed, informative result. MC book-day p95 maxDD is **42.26%**, a **−2.69 percentage point** improvement over the narrow sibling (44.95%) and **−3.64pp** vs the `halal-risk-parity-core` baseline (45.90%) — roughly **3× the magnitude** of the narrow A/B's own −0.95pp effect, confirming the core diagnosis: real sector breadth genuinely gives the cap more room to buy diversification. But OOS DSR is **0.802**, WORSE than both the narrow sibling (0.900) and the baseline (0.898) — a **−0.098pp** and **−0.096pp** regression respectively — and full DSR also slipped to **0.912** (vs 0.958 for both siblings). CAGR fell in step (full **18.20%** vs narrow 24.22%/baseline 24.64%; OOS **23.18%** vs narrow 39.26%/baseline 39.62%) — the wider, more genuinely diversified book gives up return along with concentration risk, an expected diversification trade-off, but here the DSR (risk-adjusted, multiple-testing-corrected) measure moved the WRONG way, not just the raw return. Active book-months are **90** (identical to both single-mechanism siblings, OOS 32) — sleeve breadth does not change the sample-size ceiling, as expected (a structural function of the FULL-period-start-plus-warmup calendar, not the mechanism).
- **Full vs OOS:** full = 90 active book-months (4,119 raw closed-name executions, 8,716 fills), CAGR **18.20%**, Sharpe 1.00, DSR **0.912**, realized maxDD 32.62%, hit-rate 48.62%, expectancy +1.713%/trade net (permutation observed mean), turnover 4,119. OOS = 32 active book-months, CAGR **23.18%**, Sharpe 1.47, DSR **0.802** (binding miss), realized maxDD 17.81%, hit-rate 57.39%. Historical-comparison series (trade-sequenced curve, 2021-10-05→2026-07-17, no dividends): strategy 100→**218.48** (well below both siblings' ≈300–302) vs SPY 100→171.62, SPUS 100→192.40 — this setup still beats both benchmarks on this descriptive measure, but by a visibly narrower margin than either single-mechanism sibling, consistent with the return give-up from genuine diversification.
- **Tail/integrity:** 2,145 book days; `P(day≥+5%)=0.33%`, `P(day≤−5%)=0.28%`; MC book-day maxDD p5/p50/p95 = 17.24/26.06/**42.26%**; risk of ruin 0.10% (within the 5% limit); seeded sign-flip permutation p=**0.000**. Walk-forward EARNED (208,535 PIT-guarded decision windows), 30% OOS holdout, data/PIT integrity, and clean reproducibility all PASS on the (second, clean-tree) run; profit-plateau **PASSED** (`plateau_confirmed`, all 8 frozen neighbors same-sign, max observed degradation **13.5%** — looser than the narrow sibling's 6.3% but still well within bound). The binding misses: sample size (90 < 100 active book-months, the same structural ceiling every T2 sibling hits), full/OOS DSR (0.912/0.802 < 0.95), and MC p95 drawdown (42.26% > 30% breaker).
- **5-mechanism session comparison (all REJECTED; MC p95 maxDD / OOS DSR):**

  | mechanism | MC p95 maxDD | OOS DSR | verdict |
  |---|---|---|---|
  | `halal-markowitz-core` (R5, covariance-aware Markowitz, 40 names) | 50.78% | 0.899 | REJECTED |
  | `halal-risk-parity-core` (R-RP1, inverse-vol risk parity, 40 names — baseline) | 45.90% | 0.898 | REJECTED |
  | `halal-momentum-risk-parity-core` (R6, momentum-selection + risk-parity, 60 names) | 60.18% | 0.753 | REJECTED |
  | `halal-sector-capped-risk-parity-core` (R7, sector cap, 40 names — narrow A/B) | 44.95% | 0.900 | REJECTED |
  | `halal-sector-capped-risk-parity-wide` (this dispatch, sector cap, 100 names) | **42.26%** | 0.802 | REJECTED |

  Best-yet MC p95 tail drawdown of any T2 mechanism this session (42.26%, beating even the narrow sector-capped sibling), but the worst OOS DSR of the three risk-parity-based mechanisms (only `halal-momentum-risk-parity-core`'s momentum-driven concentration is worse). No mechanism in this line has cleared the 30% MC p95 breaker or the 0.95 DSR gate.
- **Expert finding:** breadth is a real, structurally different lever from the sector cap itself — it measurably amplifies the cap's tail-risk benefit (3× the narrow A/B's effect on MC p95, the strongest single-axis result in this whole research line) — but it is NOT a free win: the same widening that bought tail-risk reduction cost risk-adjusted return (OOS DSR fell, not rose). The likely mechanism: real sector diversification genuinely reduces the WORST-case tail (fewer names crash together), which is exactly what MC p95 measures, but it also dilutes the concentrated mega-cap-tech names that drove this universe's strong OOS Sharpe in the narrower sleeves, and a 100-name inverse-vol estimate is noisier per-name than a 40-name one — both plausible, non-exclusive explanations, and this dispatch cannot fully separate them without a further ablation (out of scope: per the dispatch brief, this is the LAST planned test in the in-universe reweighting/selection research line). Per the mandate's honesty requirement: this is a genuinely mixed result, reported as measured — the sector-cap-at-breadth hypothesis is confirmed on drawdown and falsified on risk-adjusted return, and neither axis clears its gate regardless. Combined with all five session mechanisms failing the same core gates (sample size, DSR, MC p95), the cumulative finding stands: no within-NASDAQ-halal-universe reweighting or selection scheme (Markowitz, risk parity, momentum-selection, sector cap, sector cap at breadth) has cleared QDR-6 promotion. A genuinely different diversification source — real cross-market breadth (TASI, still 0 MarketBar rows in this DB) or a fundamentally different asset class — is the indicated next lever, not a further variant of this mechanism family.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (all 100 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 208,635 resolved real YAHOO/ALPACA bars, 42 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch.
- **Reproducibility:** `halal-sector-capped-risk-parity-wide@v1`; params lookbackDays 252 / sectorCap 30% / perNameCap 20% (fixed) / floor 15 / maxNames 100, 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; git SHA `48d360e`; `BacktestRun` `1e12d743-7253-40c3-834e-fb5a32362bc3`; result `results/halal-sector-capped-risk-parity-wide-2018-01-02-2026-07-17-c1_verified_100.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) produced 0 fills — identical calendar-arithmetic artifact to all four siblings (1-calendar-year window ≈251 trading days, one day short of the frozen 252-day lookback); surfaced the one real sector-table gap (`FIX`), not a wiring defect. No retune/admission; the superseded dirty-tree FULL attempt (`0917b051-722b-4adc-91be-821c363de539`) is documented above for transparency but is not the authoritative run.

### halal-concentrated-momentum-core v1 — REJECTED (P1, USER-DIRECTED CAGR-focused pivot after 5/5 diversification-line REJECTs)

- **Pre-registration / pivot hypothesis (BEFORE the run, commit `aee92d1`):** every one of the five prior in-session tests (halal-markowitz-core, halal-risk-parity-core, halal-momentum-risk-parity-core, halal-sector-capped-risk-parity-core, halal-sector-capped-risk-parity-wide) chased diversification to survive QDR-6 — all five REJECTED, none cleared MC p95<30% or OOS DSR≥0.95. The user explicitly redirected this session's objective (NASDAQ-only; TASI still has 0 real MarketBar rows): maximize REALIZED CAGR for an actively-traded halal book, explicitly accepting that the honestly-measured risk profile may NOT clear QDR-6 — Sharia compliance is the ONE constraint held completely non-negotiable and unchanged; the gate machinery itself (`reportCard.ts`, `monteCarlo.ts`, `walkForward.ts`, `profitPlateau.ts`, `envelope.ts`) is untouched. This setup deliberately tests the OPPOSITE end of the spectrum from all five priors: WEEKLY rebalance (not monthly), a FASTER 126d/skip5 momentum lookback (not the family's 252/21 — a 252d signal barely moves week to week, which would make weekly cadence pointless), and a FIXED top-10 COUNT selection (not a fraction of the pool, unlike `halal-momentum-risk-parity-core`'s top-50%-of-60) sized via the SAME `inverseVolatilityWeights`/`capAndRedistribute` mechanism (imported UNCHANGED), per-name cap raised to 25% (vs the diversified siblings' 20%, appropriate for a ≤10-name book). Cash floor of 3 eligible names (below which the whole book holds cash) — justified because under a 25% cap, 3 is the smallest count where the capped-sizing step still does real differentiating work rather than being a fig leaf. A-priori prediction: materially higher CAGR than any of the five priors (including markowitz's 56.57% OOS ceiling), at the deliberate, ACCEPTED cost of measurably worse tail risk — likely resembling or exceeding `halal-momentum-risk-parity-core`'s 60.18% MC p95 / 0.753 DSR, since this setup concentrates MORE than that sibling. Frozen 3×3 plateau {lookbackDays 105/126/147} × {topN 8/10/12}, 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. Params were never altered after the 1Y diagnostic (which ran cleanly, non-terminal, 132 trades, plateau-confirmed evidence view only).
- **Observation-unit precedent used (and why):** this setup rebalances WEEKLY, so the monthly siblings' `activeMonthlyBookReturnRecords` collapsing (`MONTHLY_BOOK_OBSERVATION_SETUP_IDS`) does not apply and this setup was deliberately NOT added to that set — inventing a parallel weekly-collapse mechanism was out of scope for this dispatch. Instead this setup follows the SAME fallback precedent as `bollinger-mr-long-v3` and `ts-momentum-halal-basket-v3` (both in `SHARED_BOOK_SETUP_IDS` but NOT in `MONTHLY_BOOK_OBSERVATION_SETUP_IDS`): the independent inference/sample unit is the raw pooled per-name TRADE (open→close position episode), not a monthly-collapsed book return, while risk (bootstrap/MC) inputs still use the true shared book-day NAV curve. This gave a materially LARGER, decisive sample (2,317 full-period trades / 726 OOS) than any monthly-book sibling's ~90 active months, clearing `INSUFFICIENT_SAMPLE` outright for the first time in this research line.
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. Sample size, walk-forward, OOS holdout, profit-plateau, data/PIT integrity, MC risk-of-ruin, and reproducibility all PASS.
- **Did the pivot maximize CAGR at the deliberate, expected cost of worse tail risk, as hypothesized?** Yes, decisively, on BOTH predicted axes. OOS CAGR is **69.66%** — the HIGHEST OOS CAGR of all six T2 mechanisms tested this session, beating even `halal-markowitz-core`'s previous ceiling of 56.57%. Full CAGR is **33.84%**, Sharpe 1.05 full / 1.42 OOS, hit-rate 52.45% (materially higher than any prior T2 sibling's ~48-58%, consistent with a faster, more responsive weekly signal). But MC book-day p95 maxDD is **64.68%** — the WORST tail drawdown of any T2 mechanism tested this session, worse even than `halal-momentum-risk-parity-core`'s 60.18% (the previous worst, itself already diagnosed as momentum-concentration-driven tail risk). OOS DSR is **0.750**, essentially tied with that same sibling's 0.753 for the worst risk-adjusted, multiple-testing-corrected result this session. The mechanism is exactly as diagnosed by that prior result: concentrating into a FIXED top-10 (tighter than the sibling's ~30-name top-half) at a FASTER 126d signal — while sized by the same correlation-BLIND inverse-vol weighting — pushes further into the same momentum-cluster tail-risk failure mode, and does so more, not less, because the selection is more concentrated. Full DSR of **0.929** is the second-highest full-period DSR of any T2 setup this session (after `halal-markowitz-core`'s 0.970), showing the mechanism is NOT noisy or spurious — plateau **PASSED cleanly** (`plateau_confirmed`, all 8 frozen neighbors same-sign OOS expectancy, ranging 27.70%-36.92% — a genuine plateau, not a spike), so this is a real, parameter-stable edge, deliberately trading tail risk for CAGR, not overfitting.
- **Full vs OOS:** full = 2,317 trades, CAGR **33.84%**, Sharpe 1.05, DSR **0.929**, realized maxDD 34.96%, hit-rate 52.45%, expectancy +17.26%/trade net (permutation observed mean, seeded sign-flip p=**0.000**). OOS = 726 trades, CAGR **69.66%**, Sharpe 1.42, DSR **0.750** (binding miss). MC book-day maxDD p5/p50/p95 = 29.85%/44.09%/**64.68%** (limit 30%); risk of ruin **1.90%** (well within the 5% limit — the book does not blow up, it merely draws down deeply); Kelly fraction 0.0086 → clamped qty 1.0248.
- **6-mechanism session comparison (CAGR-focused pivot vs all five diversification-line priors; OOS CAGR / MC p95 maxDD / OOS DSR):**

  | mechanism | OOS CAGR | MC p95 maxDD | OOS DSR | verdict |
  |---|---|---|---|---|
  | `halal-markowitz-core` (R5, covariance-aware Markowitz, 40 names, monthly) | 56.57% | 50.78% | 0.899 | REJECTED |
  | `halal-risk-parity-core` (R-RP1, inverse-vol risk parity, 40 names, monthly — best tail-risk of the priors) | 39.62% | 45.90% | 0.898 | REJECTED |
  | `halal-momentum-risk-parity-core` (R6, momentum-selection + risk-parity, 60 names, monthly) | 51.17% | 60.18% | 0.753 | REJECTED |
  | `halal-sector-capped-risk-parity-core` (R7, sector cap, 40 names, monthly) | 39.26% | 44.95% | 0.900 | REJECTED |
  | `halal-sector-capped-risk-parity-wide` (sector cap, 100 names, monthly — best tail-risk of the priors) | 23.18% | 42.26% | 0.802 | REJECTED |
  | `halal-concentrated-momentum-core` (this dispatch — CAGR pivot, top-10, WEEKLY) | **69.66%** | **64.68%** | 0.750 | REJECTED |

  This dispatch has the HIGHEST OOS CAGR and the WORST MC p95 maxDD of all six mechanisms tested this session — the two extremes of the exact tradeoff the user's pivot deliberately targeted.
- **Under a HYPOTHETICAL relaxed risk tolerance (informational reading ONLY — this does NOT alter the actual QDR-6 gate or the REJECTED verdict above; the mechanically-computed status uses the SAME unmodified 0.95 DSR / 30% MC-p95 thresholds as every other setup in this lab):** if the promotion bar were instead OOS DSR≥0.70 and MC p95<55%, this setup's OOS DSR (0.750) WOULD clear that relaxed bar, but its MC p95 (64.68%) would NOT — even under a materially loosened risk tolerance, the realized tail drawdown is still too deep. A user genuinely willing to accept ~65% worst-case (p95) drawdown paths in exchange for a ~70% OOS CAGR could reasonably paper-trade this as a high-conviction, high-volatility satellite sleeve (P7-style, never AUTO_REAL) — but that is an explicit risk-tolerance choice for the user to make outside this lab's gate, not something this report card grants.
- **Expert finding:** the pivot hypothesis is cleanly CONFIRMED, and cleanly informative: concentration and cadence, not sizing sophistication, are the actual CAGR lever in this universe — a faster, more concentrated momentum book realizes materially higher realized returns than any diversification-first mechanism tried this session, exactly as the covariance-aware `halal-markowitz-core` (highest-CAGR prior) already hinted. But the cost is exactly what `halal-momentum-risk-parity-core` already diagnosed and this dispatch now confirms at a MORE extreme setting: momentum selection concentrates the book into correlated clusters that the (deliberately reused, correlation-BLIND) inverse-vol sizing cannot see or fix, and tightening that concentration further (fixed top-10 vs top-half-of-60, weekly vs monthly) pushes tail risk further in the same direction, not a new direction. Per the mandate's honesty requirement, this is reported exactly as measured: the highest-CAGR, worst-tail-risk mechanism of the session, REJECTED by the SAME unmodified gate as all five priors, with an explicit, clearly-labeled informational note translating the number for a risk-tolerant reading. A genuine next step toward the user's CAGR objective, if pursued, would need a correlation-AWARE sizer (Markowitz-style) applied AFTER this concentrated weekly selection — a NEW setup, never a version bump of this file.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (60 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 123,535 resolved real YAHOO/ALPACA bars, 42 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch. Sharia compliance is unchanged and unaffected by the CAGR-focused pivot — it was never in scope to relax.
- **Reproducibility:** `halal-concentrated-momentum-core@v1`; params lookbackDays 126 / skipRecentDays 5 / absoluteThreshold 0 / topN 10 / cashFloor 3 (fixed) / perNameCap 25% / maxNames 60, 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; git SHA `aee92d1`; `BacktestRun` `01772bc9-7988-4f90-b7e9-3ff0ad5063d2`; result `results/halal-concentrated-momentum-core-2018-01-02-2026-07-17-c1_verified_60.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) ran cleanly (132 trades, plateau-confirmed evidence view, DIAGNOSTIC_NON_TERMINAL, no persistence) and changed zero params. No retune/admission; one FULL run only.

### halal-managed-momentum-core v1 — REJECTED (P2, PRE-REGISTERED down-only tail-risk attack on halal-concentrated-momentum-core's own diagnosed flaw)

- **Pre-registration / hypothesis (params committed `50c7cf9` BEFORE the clean FULL run):** `halal-concentrated-momentum-core`'s own header diagnoses its 64.68% MC p95 tail as coming from being FULLY INVESTED in a correlated momentum cluster exactly when a market-wide crash hits every held name at once. This setup's SELECTION and SIZING are BYTE-IDENTICAL to that engine (weekly ISO-week cadence, 126d/skip5 relative momentum among absolute-positive survivors, fixed top-10, `inverseVolatilityWeights`/`capAndRedistribute` at 25% per-name cap, 3-name cash floor — the concentrated engine's file is untouched; its exported pure helpers are reused verbatim). The ONLY change is three DOWN-ONLY, literature-standard momentum-crash mitigants stacked on top, none of which can ever inflate a return: (1) **volatility targeting** (Barroso & Santa-Clara 2015 — vol-managed momentum roughly doubles raw momentum's Sharpe; engine-native `realizedVolLookback=60`/`targetAnnualVol=20%`, higher than the diversified siblings' 15% since this is a deliberately concentrated CAGR-focused book); (2) a **drawdown governor** (engine-native, full exposure through 15% book drawdown, linear to cash at 35%); (3) a **trend/regime filter** (Daniel & Moskowitz 2016 "Momentum Crashes" / Faber 2007 — reusing `ts-momentum-halal-basket-v2`'s proven equal-weight-index/200d-SMA logic import-only: below its own SMA, the whole book holds cash that week, sidestepping exactly the correlated-cluster-crash mechanism the concentrated engine's own diagnosis blames). A-priori prediction: materially lower MC p95 drawdown at the cost of SOME OOS CAGR give-back (down-only governors can only trim, never add, return); the empirical question was the RATE of that trade. Frozen 3×3 plateau {targetAnnualVol 0.15/0.20/0.25} × {regimeSmaPeriod 150/200/250}, 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. No parameter was retuned after the 1Y diagnostic (which ran cleanly, non-terminal, 144 trades, evidence view only).
- **Implementation note (engineering choice, not a design deviation):** the regime's equal-weight index is built from `UniversePrepareInput.dailyBarsBySymbol` (always fully populated) rather than `closesBySymbol` (which the shared engine's memory-bound two-pass `tradableBookSymbols` discovery flow legally passes empty on its first, symbol-union pass) — using `closesBySymbol` would have made the discovery pass see an always-"insufficient" regime and union in zero tradable names, silently starving the real run. Both fields carry byte-identical real close data; only the input field differs.
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`. Walk-forward, OOS holdout, sample size, MC risk-of-ruin, data/PIT integrity, and clean-tree reproducibility all PASS. (An initial FULL run executed before the pre-registration commit landed was correctly flagged `REPRODUCIBILITY_FAILURE` by `isReproducibleRun`'s dirty-quant-tree check — BacktestRun `c04bfc2b-0817-48e6-b20f-ff3fd47b1dd3` — and is superseded by the clean-tree run below; the params were byte-identical, so every metric reproduced exactly and only the reproducibility flag cleared.)
- **Did the three down-only governors cut the 64.68% drawdown while keeping most of the 69.66% OOS CAGR, as hypothesized?** Partially — a real effect, in the right direction on both axes, but NOT decisive. MC book-day p95 maxDD fell from **64.68% to 50.70%** (−13.98 percentage points, a 21.6% relative reduction) — a genuine tail-risk improvement, but nowhere near the diversification family's 42.26% floor (`halal-sector-capped-risk-parity-wide`). OOS CAGR fell from **69.66% to 44.45%** (−25.21 percentage points, a 36% relative reduction) — retaining roughly 64% of the baseline's OOS CAGR. OOS DSR is **0.733**, essentially unchanged from the baseline's 0.750 (the governors did not improve risk-adjusted quality — they only trimmed raw exposure). The average book exposure fell to **61.3%** (`sharedBook.averageExposure`), confirming the governors are genuinely binding, not inert. Critically, this result is now DOMINATED on every axis by the pre-existing `halal-markowitz-core` diversification result (56.57% OOS CAGR / 50.78% MC p95 / 0.899 OOS DSR) — a correlation-AWARE covariance sizer, tested five setups earlier in this same research line, already achieves a materially better CAGR/drawdown/DSR combination than stacking down-only volatility/drawdown/regime governors on top of a correlation-BLIND concentrated momentum book.
- **Full vs OOS:** full = 5,675 trades, CAGR **20.14%**, Sharpe 0.92, DSR **0.868**, realized maxDD 23.51%, hit-rate 42.84%, expectancy +25.35%/trade net (permutation observed mean, seeded sign-flip p=**0.000**). OOS = 2,334 trades, CAGR **44.45%**, Sharpe 1.39, DSR **0.733** (binding miss), hit-rate 52.57%. MC book-day maxDD p5/p50/p95 = 21.84%/33.55%/**50.70%** (limit 30%); risk of ruin **0.50%** (well within the 5% limit); Kelly fraction 0 → clamped qty 0 (the seeded bootstrap declines to size this book at all under its own criterion, a stronger negative signal than the concentrated baseline's positive-but-tiny 0.0086 Kelly fraction).
- **Profit plateau — near-miss, not a spike:** `NO_PROFIT_PLATEAU_OVERFIT`, but the underlying sweep is closer to robust than the label suggests: ALL 8 frozen neighbors are same-sign positive OOS expectancy (center 0.5475); 7 of 8 stay within the 50% degradation bound (targetAnnualVol=0.20|regimeSmaPeriod=150: −17.9%; =0.25|150: −16.1%; =0.20|250: −23.7%; =0.25|250: −40.8%; =0.15|150: −42.7%; =0.15|250: −46.8%; =0.25|200: −38.6%); exactly ONE neighbor (targetAnnualVol=0.15, regimeSmaPeriod=200 — the tightest vol target crossed with the frozen center's own regime period) degrades **59.7%**, past the bound, flagging the whole sweep SPIKY. A tighter 15%-vol-target book is evidently more sensitive to the exact regime-SMA period than the 20-25% settings.
- **Historical-comparison series (trade-sequenced curve, 2021-10-05→2026-07-17, no dividends):** strategy 100→**340.32** vs SPY 100→171.62, SPUS 100→192.40 — this setup still clears both benchmarks by a wide margin on this descriptive measure, consistent with its still-high raw CAGR even after the governors trim exposure.
- **6th-mechanism-family comparison (all this session's T2 mechanisms; OOS CAGR / MC p95 maxDD / OOS DSR):**

  | mechanism | OOS CAGR | MC p95 maxDD | OOS DSR | verdict |
  |---|---|---|---|---|
  | `halal-risk-parity-core` (inverse-vol risk parity, 40 names, monthly — best-tail-risk diversified prior) | 39.62% | 45.90% | 0.898 | REJECTED |
  | `halal-markowitz-core` (covariance-aware Markowitz, 40 names, monthly — best all-round diversified prior) | 56.57% | 50.78% | 0.899 | REJECTED |
  | `halal-momentum-risk-parity-core` (momentum-selection + risk-parity, 60 names, monthly) | 51.17% | 60.18% | 0.753 | REJECTED |
  | `halal-sector-capped-risk-parity-wide` (sector cap, 100 names, monthly — best MC p95 of the session) | 23.18% | **42.26%** | 0.802 | REJECTED |
  | `halal-concentrated-momentum-core` (top-10, WEEKLY, no governors — CAGR-pivot baseline) | **69.66%** | 64.68% | 0.750 | REJECTED |
  | `halal-managed-momentum-core` (this dispatch — same top-10 WEEKLY book + 3 down-only governors) | 44.45% | 50.70% | 0.733 | REJECTED |

  The governors moved this setup roughly to the MIDPOINT of the concentrated-vs-diversified spectrum on MC p95 (50.70%, between the concentrated baseline's 64.68% and the diversified floor's 42.26%) while giving up MORE than half the concentrated baseline's CAGR premium over the diversified ceiling — worse on CAGR, MC p95, AND DSR than `halal-markowitz-core` simultaneously.
- **Relaxed-tolerance reading (informational ONLY — does NOT alter the REJECTED verdict, which uses the SAME unmodified 0.95 DSR / 30% MC-p95 thresholds as every other setup in this lab):** for a user who explicitly stated they would accept a risk profile that misses strict QDR-6 provided drawdown falls materially while most CAGR survives, this result is a genuine, honest partial win, not the decisive one hoped for: a real ~14pp cut in worst-case tail drawdown, at a cost of ~25pp of OOS CAGR — but the resulting risk/return combination does not exceed what a simpler, already-tested covariance-aware sizer (`halal-markowitz-core`) achieves on every one of the same three axes. A risk-tolerant user weighing this setup against `halal-concentrated-momentum-core` gets a materially safer book (50.70% vs 64.68% MC p95) for a materially lower return (44.45% vs 69.66% OOS CAGR); weighing it against `halal-markowitz-core` instead, there is no case for preferring this setup on the honest numbers.
- **Expert finding:** the three down-only governors work exactly as designed — mechanically, average exposure fell to 61.3%, MC p95 fell 13.98pp, and every governor is provably down-only by construction (`drawdownExposureScalar`/`strategyBookExposureScalar`, portfolioEngine.ts, unchanged) — but stacking mitigants onto a correlation-BLIND selection/sizing mechanism does not reach the risk/return frontier a correlation-AWARE mechanism (Markowitz tangency weighting) already occupies in this same universe. The regime filter (Daniel & Moskowitz's most-cited mitigant) alone cannot fully substitute for genuine cross-sectional diversification: it de-risks the WHOLE book to cash in a broad downtrend, but does nothing about the correlated concentration WITHIN a single bull-regime top-10 selection, which is where a large share of the concentrated baseline's tail risk plausibly still lives. Per the mandate's honesty requirement: this is reported exactly as measured — a real, positive, pre-registered, down-only risk reduction that does not clear the strict gate and does not dominate a simpler prior mechanism already in this ledger. A genuinely more promising next lever (out of scope here) would be a correlation-AWARE sizer applied AFTER weekly momentum selection, not further down-only overlays on inverse-vol sizing.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (60 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 123,535 resolved real YAHOO/ALPACA bars, 50 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch. Sharia compliance is unchanged and unaffected by the tail-risk-mitigation redesign — it was never in scope to relax.
- **Reproducibility:** `halal-managed-momentum-core@v1`; params lookbackDays 126 / skipRecentDays 5 / absoluteThreshold 0 / topN 10 / cashFloor 3 (fixed) / perNameCap 25% / maxNames 60 / realizedVolLookback 60 (fixed) / targetAnnualVol 20% / drawdownStartFraction 15% (fixed) / drawdownCashFraction 35% (fixed) / regimeSmaPeriod 200, 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; gitSha `50c7cf9` (params committed BEFORE this run; `isReproducibleRun` clean-tree check PASSED — `reproducible clean run: yes`); `BacktestRun` `ddda4001-9305-41c9-b93c-a34102f84563`; result `results/halal-managed-momentum-core-2018-01-02-2026-07-17-c1_verified_60.json`; shared engine. The superseded pre-commit dirty-tree run (`c04bfc2b-0817-48e6-b20f-ff3fd47b1dd3`) produced byte-identical metrics and is retained above for transparency, not as the authoritative run. 1Y diagnostic (`--period 1Y`, non-terminal) ran cleanly (144 trades, evidence view only, DIAGNOSTIC_NON_TERMINAL, no persistence) and changed zero params. No retune/admission; one authoritative FULL run only.

### halal-momentum-markowitz-core v1 — REJECTED (P3, PRE-REGISTERED synthesis of momentum SELECTION + Markowitz covariance-aware SIZING)

- **Pre-registration / hypothesis (params committed `47bc0d9` BEFORE the FULL run):** all seven prior in-session mechanisms used momentum SELECTION with a correlation-BLIND SIZER (inverse-vol — `halal-momentum-risk-parity-core`, `halal-concentrated-momentum-core`, `halal-managed-momentum-core`), OR covariance-aware SIZING with NO directional selection (`halal-markowitz-core`, `halal-risk-parity-core`, both sector-capped variants) — never both together. This setup combines them: WEEKLY ISO-week cadence, `dualMomentumMetrics`/`selectTopNByMomentum` (both imported UNCHANGED from `halalConcentratedMomentumCore`/`dualMomentumRotation`) select the fixed top-20 by 126d/skip5 relative momentum among absolute-positive survivors of the C1-verified 60-name sleeve (topN widened from the concentrated engine's 10 to 20, and cashFloor widened from 3 to 10, a-priori, because a Markowitz covariance estimate needs materially more breadth to be well-conditioned than a single-name inverse-vol ratio does); the selected subset's trailing 126d returns then feed `markowitzFrontier()` (imported UNCHANGED from `../portfolio/frontier`, byte-identical study params — seed 42, 10,000 portfolios, riskFreeRate 0 — to `halal-markowitz-core`), and `.tangency.weights` are capped at 25% per name via `capAndRedistribute` (imported UNCHANGED from `halalRiskParityCore`). A-priori prediction: retain MORE OOS CAGR than the diversified-monthly setups (39.62–56.57%) because the sizer starts from a momentum-tilted subset, while landing BELOW the concentrated baseline's 64.68% MC p95 because the covariance sizer explicitly down-weights the correlated cluster the baseline's own header diagnosed — ideally beating `halal-markowitz-core`'s 56.57%/50.78%/0.899 on at least the CAGR axis. Frozen 3×3 plateau {topN 15/20/25} × {lookbackDays 105/126/147}, 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. No parameter was retuned after the 1Y diagnostic (which ran cleanly, non-terminal, 278 trades, evidence view only).
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`. Walk-forward, OOS holdout (30.00%), sample size (4,499≫100), MC risk-of-ruin, profit plateau, data/PIT integrity, and clean-tree reproducibility all PASS.
- **Did the Markowitz sizer retain most of the concentrated engine's CAGR while taming its tail, as hypothesized?** Partially, and the honest reading is mixed rather than decisive. OOS CAGR is **52.95%**, retaining **76.0%** of the concentrated baseline's 69.66% (52.95/69.66) — a real, meaningful CAGR retention, confirming the a-priori expectation on that half. But MC book-day p95 maxDD fell only from **64.68% to 60.51%** (−4.17 percentage points, a 6.4% relative reduction) — a genuine move in the predicted direction, but far short of both the diversified family's 42.26–50.78% range and the a-priori hope of a decisive tail reduction. OOS DSR is **0.746**, essentially unchanged from the concentrated baseline's 0.750 (−0.004) — the covariance sizer did not improve risk-adjusted quality any more than inverse-vol sizing did. Critically, **this result does NOT beat `halal-markowitz-core` on any of the three target axes**: CAGR 52.95% vs 56.57% (**−3.62pp**), MC p95 60.51% vs 50.78% (**+9.73pp, worse**), OOS DSR 0.746 vs 0.899 (**−0.153, worse**) — the plain covariance-aware monthly sizer over the WHOLE 40-name sleeve, with no momentum pre-selection at all, remains the single best-all-round mechanism this entire research line has produced. The synthesis moved the frontier point in the predicted direction relative to its own baseline, but not far enough to establish a new, superior corner.
- **Full vs OOS:** full = 4,499 trades, CAGR **25.65%**, Sharpe 0.95, DSR **0.884**, realized maxDD 40.06%, hit-rate 51.42%, expectancy +1.548%/trade net (permutation observed mean, seeded sign-flip p=**0.000**). OOS = 1,464 trades, CAGR **52.95%**, Sharpe 1.39, DSR **0.746** (binding miss), hit-rate 55.99%. MC book-day maxDD p5/p50/p95 = 26.91%/39.81%/**60.51%** (limit 30%, binding miss); risk of ruin **1.80%** (well within the 5% limit); Kelly fraction 0.0019 → clamped qty 0.2266 (the seeded bootstrap sizes this book modestly, ahead of the concentrated baseline's near-zero Kelly and `halal-managed-momentum-core`'s zero-clamped Kelly). Average book exposure **92.0%**.
- **Profit plateau — PASSED cleanly:** `plateau_confirmed` — all 8 frozen neighbors are same-sign positive OOS expectancy (topN=15|lookbackDays∈{105,126,147}: 3.24%/3.67%/5.16%; topN=20|lookbackDays∈{105,147}: 2.89%/2.69%; topN=25|lookbackDays∈{105,126,147}: 2.51%/2.29%/2.38%), a tight 2.29–5.16% range with no spiky degradation — a genuinely robust sweep, unlike `halal-managed-momentum-core`'s SPIKY neighborhood at the same 9-trial scale.
- **Historical-comparison series (weekly-sequenced curve, 2021-10-05→2026-07-17, no dividends):** strategy 100→**400.68** vs SPY 100→171.62, SPUS 100→192.40 — this setup clears both benchmarks by a wide margin on this descriptive measure, consistent with its still-high raw CAGR.
- **7th-mechanism-family comparison (all this session's T2 mechanisms; OOS CAGR / MC p95 maxDD / OOS DSR):**

  | mechanism | OOS CAGR | MC p95 maxDD | OOS DSR | verdict |
  |---|---|---|---|---|
  | `halal-risk-parity-core` (inverse-vol risk parity, 40 names, monthly — best-tail-risk diversified prior) | 39.62% | 45.90% | 0.898 | REJECTED |
  | `halal-markowitz-core` (covariance-aware Markowitz, 40 names, monthly — best all-round diversified prior) | 56.57% | 50.78% | **0.899** | REJECTED |
  | `halal-momentum-risk-parity-core` (momentum-selection + risk-parity, 60 names, monthly) | 51.17% | 60.18% | 0.753 | REJECTED |
  | `halal-sector-capped-risk-parity-wide` (sector cap, 100 names, monthly — best MC p95 of the session) | 23.18% | **42.26%** | 0.802 | REJECTED |
  | `halal-concentrated-momentum-core` (top-10, WEEKLY, no governors — CAGR-pivot baseline) | **69.66%** | 64.68% | 0.750 | REJECTED |
  | `halal-managed-momentum-core` (same top-10 WEEKLY book + 3 down-only governors) | 44.45% | 50.70% | 0.733 | REJECTED |
  | `halal-momentum-markowitz-core` (this dispatch — top-20 WEEKLY momentum + Markowitz sizing) | 52.95% | 60.51% | 0.746 | REJECTED |

  This synthesis lands BETWEEN the concentrated baseline and the diversified-monthly family on CAGR (52.95%, below the concentrated 69.66% but above every diversified setup except `halal-markowitz-core`'s 56.57%) and on MC p95 (60.51%, below the concentrated baseline's 64.68% but above every diversified setup) — a genuinely NEW point on the mapped frontier, exactly as hypothesized structurally, but it is Pareto-DOMINATED by `halal-markowitz-core` on every axis simultaneously, so it does not represent an improvement over the best mechanism already in this ledger.
- **Relaxed-tolerance reading (informational ONLY — does NOT alter the REJECTED verdict, which uses the SAME unmodified 0.95 DSR / 30% MC-p95 thresholds as every other setup in this lab):** for a user who explicitly stated they would accept a risk profile that misses strict QDR-6 in exchange for maximum realized CAGR, this setup is a legitimate, honestly-measured candidate — 52.95% OOS CAGR with a materially better (`plateau_confirmed`) parameter-robustness profile than either weekly momentum sibling, and a modestly lower tail (60.51% vs the concentrated baseline's 64.68%) for a real CAGR cost (52.95% vs 69.66%). But a risk-tolerant user comparing it directly against `halal-markowitz-core` gets a WORSE outcome on every single axis (lower CAGR, higher tail risk, lower DSR) — there is no honest case for preferring this setup over the plain Markowitz sizer, even under a relaxed tolerance.
- **Expert finding:** the synthesis is STRUCTURALLY confirmed (momentum selection retains real CAGR premium; Markowitz sizing moves the tail in the predicted direction and delivers the most robust plateau of the three momentum-family setups) but ECONOMICALLY falsified as an improvement — applying a covariance-aware sizer AFTER momentum pre-selection does not beat applying the SAME sizer to the whole diversified sleeve with no selection at all. The most plausible explanation: the momentum-selected top-20 subset is, BY CONSTRUCTION, a MORE correlated pool than the full 40-60 name sleeve (momentum selection concentrates into a correlated winning cluster — the exact mechanism `halal-momentum-risk-parity-core` and `halal-concentrated-momentum-core` both diagnosed) — so the covariance sizer has genuinely less diversification material to work with post-selection than it does over the unfiltered sleeve, even though it still does real, measurable, directionally-correct work (the plateau and the modest tail improvement prove that). This closes the in-session momentum-selection research line: three variants tried (pure inverse-vol, inverse-vol + down-only governors, Markowitz), and `halal-markowitz-core`'s plain covariance-aware sizing over the undirected sleeve remains the single best mechanism produced this session on every axis QDR-6 measures.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (60 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 123,535 resolved real YAHOO/ALPACA bars, 50 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch. Sharia compliance is unchanged and unaffected by the sizing-mechanism synthesis — it was never in scope to relax.
- **Reproducibility:** `halal-momentum-markowitz-core@v1`; params lookbackDays 126 / skipRecentDays 5 / absoluteThreshold 0 / topN 20 / cashFloor 10 (fixed) / perNameCap 25% / maxNames 60 (fixed) / markowitz seed 42 (fixed) / portfolios 10,000 (fixed) / riskFreeRate 0 (fixed) / tradingDaysPerYear 252 (fixed), 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; gitSha `47bc0d9` (params committed BEFORE this run; `isReproducibleRun` clean-tree check PASSED — `reproducible clean run: yes`); `BacktestRun` `8869bef7-0be0-46f6-8338-ec54e487f01e`; result `results/halal-momentum-markowitz-core-2018-01-02-2026-07-17-c1_verified_60.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) ran cleanly (278 trades, evidence view only, DIAGNOSTIC_NON_TERMINAL, no persistence) and changed zero params. No retune/admission; one authoritative FULL run only.

### halal-trend-rider-core v1 — REJECTED (P4, FIRST let-winners-run entry/exit-mode setup this session)

- **Pre-registration / hypothesis (BEFORE the run, commit `2e3a61d`):** all eight prior in-session setups (`halal-markowitz-core`, `halal-risk-parity-core`, `halal-momentum-risk-parity-core`, `halal-sector-capped-risk-parity-core`, `halal-sector-capped-risk-parity-wide`, `halal-concentrated-momentum-core`, `halal-managed-momentum-core`, `halal-momentum-markowitz-core` — ALL REJECTED) used the engine's `targetWeight` rebalance-to-target mode, which SYSTEMATICALLY TRIMS WINNERS every rebalance and additionally caps per-name exposure (20-25%) — the user correctly observed both mechanisms cap upside during the 2022-2026 NASDAQ tech/AI bull (NVDA ~10x'd) regardless of sizing sophistication. This setup OMITS `targetWeight` entirely (portfolioEngine.ts:1027-1064: no `targetWeight` hook ⇒ a held position not matching `exit()` is left completely untouched, never trimmed). Weekly ISO-week top-`maxOpenSlots`(10) entry by 126d/skip5 relative momentum among absolute-positive survivors (`dualMomentumMetrics`/`selectTopNByMomentum` imported UNCHANGED), 10% NAV sizing at entry only, never resized again. Exit checked DAILY on either a trailing stop (`evaluateStops` imported UNCHANGED, `trailingStopPct=0.20`/`staticStopPct=0.15`, wider than stops.ts's 0.10/0.08 defaults) with `peakClose` tracked INCREMENTALLY per (symbol, full param set) — not recomputed from the truncated `decisionHistoryBars` window, since a let-winners-run position can outlive that window by years — or 126d relative momentum turning ≤0. Concurrency structurally capped at `maxOpenPositions=maxOpenSlots` via the UNCHANGED risk envelope (risk/envelope.ts:106), never strategy-side bookkeeping. A-priori prediction: materially higher CAGR than `halal-concentrated-momentum-core`'s 69.66% OOS ceiling (the best of the eight priors), at a DELIBERATELY ACCEPTED, honestly-reported cost of comparable-or-worse tail risk — the trailing stop is this design's only, imperfect, risk control. Frozen 3×3 plateau {trailingStopPct 0.15/0.20/0.25} × {maxOpenSlots 8/10/12}, 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. Params were never altered after the 1Y diagnostic (which ran cleanly, non-terminal, 25 trades, evidence view only).
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`. Walk-forward, OOS holdout (30.00%), sample size (232≥100), MC risk-of-ruin, data/PIT integrity, and clean-tree reproducibility all PASS.
- **Did letting winners run beat the concentrated baseline's 69.66% OOS CAGR, as hypothesized? No — decisively falsified.** OOS CAGR is **45.44%**, **24.22 percentage points (−34.8% relative) BELOW** `halal-concentrated-momentum-core`'s 69.66% — never trimming winners produced a LOWER realized OOS CAGR than a mechanism that rebalances and trims every single week. MC book-day p95 maxDD is **62.04%**, only marginally better than the concentrated baseline's 64.68% (−2.64pp, a 4.1% relative improvement) — nowhere near the 30% breaker or the diversified family's 42-51% range. OOS DSR is **0.630**, the WORST of all nine mechanisms tested this session (below `halal-managed-momentum-core`'s previous-worst 0.733). Full CAGR **25.65%** and full DSR **0.875** are both respectable in isolation but the OOS collapse on DSR is decisive.
- **Full vs OOS:** full = 232 trades, CAGR **25.65%**, Sharpe 0.94, DSR **0.875**, realized maxDD 31.46%, hit-rate 52.77%. OOS = 85 trades, CAGR **45.44%**, Sharpe 1.20, DSR **0.630** (binding miss). MC book-day maxDD p5/p50/p95 = 27.67%/40.70%/**62.04%** (limit 30%, binding miss); risk of ruin **1.40%** (well within the 5% limit); entry-jitter permutation p=**0.000** (mean/trade +11.84%); Kelly fraction 0.0049 → clamped qty 0.5786.
- **Profit plateau — SPIKY:** 3 of 8 frozen neighbors (all at the tighter `trailingStopPct=0.15`, every `maxOpenSlots` value) degrade past the bound (OOS expectancy 5.21%/6.04%/5.65% vs the wider-stop neighbors' 17.50-25.65%) — a genuinely sensitive parameter surface: a tighter trailing stop whipsaws this let-winners-run design out of real winners before they compound, which is exactly the failure mode the header's honest caveat anticipated (a 20% trailing stop cannot avoid every whipsaw either, and the sweep proves the exact sensitivity).
- **9-mechanism session comparison (ALL mechanisms tested this session; OOS CAGR / MC p95 maxDD / OOS DSR):**

  | mechanism | OOS CAGR | MC p95 maxDD | OOS DSR | verdict |
  |---|---|---|---|---|
  | `halal-risk-parity-core` (inverse-vol risk parity, 40 names, monthly — best-tail-risk diversified prior) | 39.62% | 45.90% | 0.898 | REJECTED |
  | `halal-markowitz-core` (covariance-aware Markowitz, 40 names, monthly — best all-round diversified prior) | 56.57% | 50.78% | **0.899** | REJECTED |
  | `halal-momentum-risk-parity-core` (momentum-selection + risk-parity, 60 names, monthly) | 51.17% | 60.18% | 0.753 | REJECTED |
  | `halal-sector-capped-risk-parity-core` (sector cap, 40 names, monthly) | 39.26% | 44.95% | 0.900 | REJECTED |
  | `halal-sector-capped-risk-parity-wide` (sector cap, 100 names, monthly — best MC p95 of the session) | 23.18% | **42.26%** | 0.802 | REJECTED |
  | `halal-concentrated-momentum-core` (top-10, WEEKLY REBALANCE that trims winners — highest OOS CAGR of the priors) | **69.66%** | 64.68% | 0.750 | REJECTED |
  | `halal-managed-momentum-core` (same top-10 WEEKLY book + 3 down-only governors) | 44.45% | 50.70% | 0.733 | REJECTED |
  | `halal-momentum-markowitz-core` (top-20 WEEKLY momentum + Markowitz sizing) | 52.95% | 60.51% | 0.746 | REJECTED |
  | `halal-trend-rider-core` (this dispatch — entry/exit LET-WINNERS-RUN, never trims, top-10 slots) | 45.44% | 62.04% | **0.630** | REJECTED |

  This dispatch does NOT achieve the highest OOS CAGR of the session (it ranks 5th of 9) and has the WORST OOS DSR of the session, while its MC p95 is only marginally better than the concentrated-rebalance baseline it was designed to outperform.
- **Relaxed-tolerance reading (informational ONLY — does NOT alter the REJECTED verdict, which uses the SAME unmodified 0.95 DSR / 30% MC-p95 thresholds as every other setup in this lab):** a risk-tolerant user hoping "never trim a winner" would beat the concentrated-rebalance baseline gets a WORSE outcome on CAGR (45.44% vs 69.66%) AND on DSR (0.630 vs 0.750) for an only marginally better MC p95 (62.04% vs 64.68%) — there is no honest case for preferring this setup over `halal-concentrated-momentum-core` even under a relaxed tolerance, and none at all versus `halal-markowitz-core` (worse on every axis).
- **Expert finding — the intuitive hypothesis is FALSIFIED, and the mechanism is diagnosable, not noise:** letting winners run costs this book PORTFOLIO VELOCITY, not just tail risk. `halal-concentrated-momentum-core` re-ranks and ROTATES its entire top-10 EVERY WEEK, continuously replacing decelerating names with whatever is CURRENTLY hottest — capturing sequential exposure to MANY different winners across the bull, even though it trims each one's individual share. `halal-trend-rider-core` only frees a slot when a genuine stop or momentum breakdown fires, so capital gets "parked" in decent-but-decelerating positions for extended periods (232 full-period trades vs the concentrated baseline's 2,317 — roughly 10× fewer), unable to rotate into fresher momentum leaders, and each held position rides more of its own eventual give-back before the (deliberately wide, 15-20%) stops bind. The net effect is FEWER total compounding opportunities captured, not more — never-trimming a winner does not automatically translate into higher realized CAGR when it trades off against reduced rotation velocity in a fixed-slot book. This closes the direct "let winners run" hypothesis as tested: a genuinely different next lever (out of scope here) would need BOTH no-trim AND a rotation mechanism that reclaims a slot from a decelerating-but-still-marginally-positive-momentum name in favor of a stronger new entrant — which this file deliberately did not build, since that would reintroduce a form of trimming.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (60 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 123,535 resolved real YAHOO/ALPACA bars, 63 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch. Sharia compliance is unchanged and unaffected by the let-winners-run redesign — only diversification/weight discipline was relaxed, per the user's explicit authorization, never Sharia.
- **Reproducibility:** `halal-trend-rider-core@v1`; params lookbackDays 126 / skipRecentDays 5 / absoluteThreshold 0 / maxOpenSlots 10 / sizeFraction 10% / staticStopPct 15% (fixed) / trailingStopPct 20% / maxNames 60 (fixed), 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; gitSha `2e3a61d` (params committed BEFORE this run; `isReproducibleRun` clean-tree check PASSED — `reproducible clean run: yes`); `BacktestRun` `c698c65a-db73-486d-9ff5-6570c5bd8cc6`; result `results/halal-trend-rider-core-2018-01-02-2026-07-17-c1_verified_60.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) ran cleanly (25 trades, evidence view only, DIAGNOSTIC_NON_TERMINAL, no persistence) and changed zero params. No retune/admission; one authoritative FULL run only.

### halal-fast-momentum-core v1 — REJECTED (P5, ISOLATED velocity+concentration extreme testing the trend-rider REJECT's rotation-velocity finding)

- **Pre-registration / hypothesis (BEFORE the run, commit `000dea1`):** `halal-trend-rider-core`'s REJECT (the ninth setup this session) diagnosed the concentrated engine's 69.66% OOS CAGR as coming from ROTATION VELOCITY — re-ranking its entire top-10 EVERY WEEK, continuously replacing decelerating names with whatever is CURRENTLY hottest (2,317 trades) — not from position longevity (never-trimming instead produced 45.44%, 232 trades, WORSE). This setup is an ISOLATED change from `halal-concentrated-momentum-core`'s file: identical weekly ISO-week rebalance, identical `dualMomentumMetrics`/`selectTopNByMomentum`/`inverseVolatilityWeights`/`capAndRedistribute` (all imported UNCHANGED), identical C1-verified 60-name sleeve and 25% per-name cap. ONLY three params change: lookbackDays 126→63 (a FASTER signal — half the baseline lookback), skipRecentDays 5→2 (keeping the skip/lookback ratio ≈ the baseline's), topN 10→5 (TIGHTER concentration — half the baseline count, so each weekly rotation reallocates a larger share of the book). cashFloor scaled proportionally 3→2 (baseline's floor/topN ratio 0.30 × topN=5 ≈ 1.5, rounded up to 2; also the smallest count where inverse-vol sizing still differentiates between names at all). A-priori prediction: pushing rotation velocity and per-rotation impact to their sensible maximum COULD beat the 69.66% OOS CAGR ceiling if the trend-rider finding generalizes — explicitly NOT guaranteed, since a faster (63d) signal is also more prone to noise/whipsaw than the proven 126d window; the real number was reported regardless of direction. Frozen 3×3 plateau {lookbackDays 42/63/84} × {topN 4/5/6}, 9 trials, seed 42, 30% OOS, FULL 2018-01-02→2026-07-17. Params were never altered after the 1Y diagnostic (which ran cleanly, non-terminal, 97 trades, evidence view only).
- **Terminal verdict / ordered reasons:** **REJECTED** — `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`. Walk-forward, OOS holdout (30.00%), sample size (1,390≥100), MC risk-of-ruin (4.10%≤5%), data/PIT integrity, and clean-tree reproducibility all PASS.
- **Did faster+tighter momentum beat the concentrated baseline's 69.66% OOS CAGR, as hypothesized? Yes — decisively confirmed, and at a real, honestly-measured drawdown cost.** OOS CAGR is **105.92%**, **+36.26 percentage points (+52.1% relative) ABOVE** `halal-concentrated-momentum-core`'s 69.66% — the single highest OOS CAGR of all ten mechanisms tested this session. MC book-day p95 maxDD is **68.40%**, **+3.72 percentage points (+5.8% relative) WORSE** than the concentrated baseline's 64.68% — also the single worst MC p95 of all ten mechanisms this session, exceeding even `halal-momentum-risk-parity-core`'s prior-worst 60.18%. OOS DSR is **0.874**, notably BETTER than every momentum-selection/rotation sibling this session (`halal-concentrated-momentum-core` 0.750, `halal-momentum-markowitz-core` 0.746, `halal-momentum-risk-parity-core` 0.753, `halal-managed-momentum-core` 0.733, `halal-trend-rider-core` 0.630) — still short of the 0.95 gate, but the risk-ADJUSTED quality of the OOS return stream genuinely improved even as raw tail drawdown worsened, because the OOS Sharpe (1.73) is meaningfully higher than the baseline's own OOS Sharpe. Full CAGR **36.99%** and full DSR **0.928** are both the best full-period figures of the concentrated-momentum family.
- **Full vs OOS:** full = 1,390 trades, CAGR **36.99%**, Sharpe 1.05, DSR **0.928**, realized maxDD 41.42%, hit-rate 53.89%. OOS = 410 trades, CAGR **105.92%**, Sharpe 1.73, DSR **0.874** (binding miss, but the best OOS DSR of any momentum-selection setup this session). MC book-day maxDD p5/p50/p95 = 32.63%/47.92%/**68.40%** (limit 30%, binding miss — the worst of the session); risk of ruin **4.10%** (within the 5% limit, but close to it); entry-jitter permutation p=**0.000** (mean/trade +12.76%); Kelly fraction 0.0160 → clamped qty 1.899.
- **Profit plateau — SPIKY:** 2 of 8 frozen neighbors (`lookbackDays=42` at `topN=5` and `topN=6`) degrade past the bound (OOS expectancy 12.69%/12.76% vs the center's 27.16% and the other six neighbors' 14.82–39.997%) — the fastest lookback in the grid (42d, roughly two-thirds of the chosen 63d) is where the signal becomes genuinely noisy, exactly the honest a-priori caveat that a faster signal is more whipsaw-prone. The chosen center (63d) and the slower 84d neighbors all held up, but the neighborhood as a whole is not uniformly robust ⇒ `NO_PROFIT_PLATEAU_OVERFIT`.
- **10-mechanism session comparison (ALL mechanisms tested this session; OOS CAGR / MC p95 maxDD / OOS DSR):**

  | mechanism | OOS CAGR | MC p95 maxDD | OOS DSR | verdict |
  |---|---|---|---|---|
  | `halal-risk-parity-core` (inverse-vol risk parity, 40 names, monthly — best-tail-risk diversified prior) | 39.62% | 45.90% | 0.898 | REJECTED |
  | `halal-markowitz-core` (covariance-aware Markowitz, 40 names, monthly — best all-round diversified prior) | 56.57% | 50.78% | **0.899** | REJECTED |
  | `halal-momentum-risk-parity-core` (momentum-selection + risk-parity, 60 names, monthly) | 51.17% | 60.18% | 0.753 | REJECTED |
  | `halal-sector-capped-risk-parity-core` (sector cap, 40 names, monthly) | 39.26% | 44.95% | 0.900 | REJECTED |
  | `halal-sector-capped-risk-parity-wide` (sector cap, 100 names, monthly — best MC p95 of the session) | 23.18% | **42.26%** | 0.802 | REJECTED |
  | `halal-concentrated-momentum-core` (top-10, WEEKLY, 126d/skip5 — prior CAGR ceiling) | 69.66% | 64.68% | 0.750 | REJECTED |
  | `halal-managed-momentum-core` (same top-10 WEEKLY book + 3 down-only governors) | 44.45% | 50.70% | 0.733 | REJECTED |
  | `halal-momentum-markowitz-core` (top-20 WEEKLY momentum + Markowitz sizing) | 52.95% | 60.51% | 0.746 | REJECTED |
  | `halal-trend-rider-core` (entry/exit LET-WINNERS-RUN, never trims, top-10 slots) | 45.44% | 62.04% | 0.630 | REJECTED |
  | `halal-fast-momentum-core` (this dispatch — top-5, WEEKLY, 63d/skip2 — max velocity + max concentration) | **105.92%** | **68.40%** | 0.874 | REJECTED |

  This dispatch achieves the HIGHEST OOS CAGR of the session (rank 1 of 10) and the WORST MC p95 maxDD of the session (rank 10 of 10, i.e. the largest), while posting the second-best OOS DSR of the six momentum-selection/rotation mechanisms (behind only the diversification-first, no-selection setups) — a real, decisive, honestly-measured CAGR/tail-risk trade at the aggressive extreme.
- **Relaxed-tolerance reading (informational ONLY — does NOT alter the REJECTED verdict, which uses the SAME unmodified 0.95 DSR / 30% MC-p95 thresholds as every other setup in this lab):** for the user who explicitly stated they want maximum CAGR and accept a risk profile that misses strict QDR-6, this is the single best result of the session on the stated objective — OOS CAGR 105.92% at MC p95 68.40% is a genuinely better CAGR/risk trade than `halal-concentrated-momentum-core`'s 69.66%/64.68% on the CAGR axis (52% more return for only 5.8% more tail drawdown, a favorable trade-off in relative terms) and its OOS DSR (0.874) is materially better too — a risk-tolerant reader focused purely on maximizing realized CAGR while retaining the best DSR available in the momentum family would prefer this setup over the concentrated baseline on every axis except raw MC p95. It remains below `halal-markowitz-core`'s DSR (0.899) and MC p95 (50.78%), but far exceeds its CAGR (56.57%) — the two setups represent genuinely different points on the CAGR/risk frontier, not a strict dominance either way.
- **Expert finding — the rotation-velocity hypothesis is CONFIRMED, and the mechanism scales as predicted, with a real cost:** faster momentum (63d/skip2 vs 126d/skip5) reacts to fresher price information and rotates the book's top-5 into whatever is currently accelerating with roughly the same weekly cadence but a shorter memory, while tighter concentration (top-5 vs top-10) means each rotation event moves a LARGER fraction of the book into the names currently ranked highest. Both effects point the same direction — MORE exposure to the freshest winners, MORE often — and the OOS CAGR result (105.92%, the session's best) confirms this compounds into real additional realized return, not noise (permutation p=0.000). The cost is exactly what was predicted a priori and not hidden: a materially worse MC p95 tail (68.40%, the session's worst) because fewer, larger positions concentrated in a faster-turning momentum cluster amplify a market-wide reversal more than the already-concentrated 126d/top-10 baseline does. The 42d plateau neighbor's degradation is the clearest evidence the faster-signal-is-noisier caveat was correct at the margin — 63d held up, but pushing even faster (42d) breaks down. This closes the velocity+concentration extreme of the momentum family: `halal-fast-momentum-core` is the highest-CAGR, highest-risk end of the spectrum this session explored, genuinely different from (not simply worse than) `halal-markowitz-core`'s highest-DSR, lowest-risk end.
- **Compliance:** C1 `VERIFIED_COMPLIANT` (60 sleeve names Tier-1 SPUS / Tier-2 AAOIFI-XBRL, `FUND_LEVEL_PURIFICATION_ONLY` index-provider-screened per name); 123,535 resolved real YAHOO/ALPACA bars, 63 MOCK excluded. Paper-trading/simulated only; no execution wiring in this dispatch. Sharia compliance is unchanged and unaffected by the velocity/concentration redesign — only the return/tail-risk profile was pushed to its aggressive extreme, per the user's explicit authorization, never Sharia.
- **Reproducibility:** `halal-fast-momentum-core@v1`; params lookbackDays 63 / skipRecentDays 2 / absoluteThreshold 0 / topN 5 / cashFloor 2 (fixed) / perNameCap 25% / maxNames 60, 9 frozen trials; 15 bps/side + unchanged risk/ADV/cash envelope. Seed 42; git SHA `000dea1` (params committed BEFORE this run; `isReproducibleRun` clean-tree check PASSED — `reproducible clean run: yes`); `BacktestRun` `218716ac-f692-4f0f-91f3-81045a159a62`; result `results/halal-fast-momentum-core-2018-01-02-2026-07-17-c1_verified_60.json`; shared engine. 1Y diagnostic (`--period 1Y`, non-terminal) ran cleanly (97 trades, evidence view only, DIAGNOSTIC_NON_TERMINAL, no persistence) and changed zero params. No retune/admission; one authoritative FULL run only.

## Post-pause research lanes (G6 — need user go)

| id | lane | first rung (lazy-dev) |
|---|---|---|
| rl-ppo-baseline | G6a deep-RL | single-agent PPO on G3 gym env; identical gates; QDR-3 Python escape needs architect |
| xsec-factor-linear | G6b cross-sectional factor | LINEAR momentum+turnover quantile ranks (Quantformer's inputs) before any transformer; monthly cadence |

## Research anchors (calibration — beating these by 10× = implausible flag, not celebration)

- Zarattini/Aziz/Barbon SPY ORB (Swiss Finance Inst. 24-97): ≈19.6%/yr, Sharpe 1.33–2.4 — best published intraday momentum.
- VWAP band reversion (QuantConnect): 61–71% win rate, ≈1.4:1 RR.
- Quantformer (arXiv 2404.00424, CREDIBLE): 17–25%/yr, Sharpe 0.9–1.0, alpha ≈0.16, 0.3% costs, 4,601 A-shares; monthly rebalance beat weekly AND daily; long-only by construction.
- DRL survey (Pricope 2021): most published RL traders unprofitable in realistic settings; best case ~20–60% ANNUAL; backtest ≠ live.
- MAPPO-SLSTM paper (WARNING, not target): Sharpe 4.0 with zero transaction costs on ~1,070 records, incomplete walk-forward — exceeds our implausible flag.
- OHLCV-signal falsification literature exists (arXiv 2605.04004).
- Klarman doctrine: loss avoidance first — steady 16%/yr × 10y beats 20%/yr × 9y then one −15% year. MC maxDD p95 gate enforces this.
- bollinger-mr-long v1 (G2 daily, measured 2026-07 via `npm run backtest -- --setup bollinger-mr-long --from 2022-07-01 --to 2026-07-10`, seed 42, dataFeed=yahoo-daily; results/bollinger-mr-long-2022-07-01-2026-07-10.json): 25-name NASDAQ-halal universe, real MarketBar spine, **1,777 MOCK bars excluded in-path** (GOOGL 585 / MSFT 514 / NVDA 678). 151 trades (11 full-history names carry it; the 14 recent-backfill names have ≤102 bars and near-zero trades). The persisted full-history summary flatters (CAGR 34.2%, Sharpe 2.20, DSR 0.954, hit 62%, +1.10%/trade net; pure IS was not separately persisted) but **OOS (last 30% of trades) collapses: CAGR −32.3%, Sharpe −1.37, DSR 0.283**. MC maxDD p95 = 75.4% (≫ 30% breaker), risk-of-ruin 14.7%, entry-jitter p=0.048. Terminal status **REJECTED** (`OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`): classic regime-dependent mean-reversion that works in the 2023–24 bull tape and unwinds OOS; loss-avoidance (Klarman) gate fails hard. Do NOT re-tune v1 (data-snooping) — a v2 with a stricter stationarity gate + per-name (not pooled-independent) sizing is a NEW ledger candidate. New daily-cadence engine path: `simulateSetupDaily`/`filterRealDailyBars` in backtest/engine.ts (holds across days; same decide-close/fill-next-open + cost model as `simulate`).
- ts-momentum-halal-basket v1 (G2 daily, measured 2026-07 via `npm run backtest -- --setup ts-momentum-halal-basket --from 2022-07-01 --to 2026-07-10`, seed 42, dataFeed=yahoo-daily; results/ts-momentum-halal-basket-2022-07-01-2026-07-10.json): 25-name NASDAQ-halal basket, real MarketBar spine, **585 MOCK bars excluded in-path** (GOOGL only). 231 trades (11 full-history names carry it — the 12 recent-backfill names have ≤130 bars, < the 252d momentum window, so 0 trades). Dual absolute-momentum entry (252d>0 AND 63d>0), exit on 63d≤0 OR close<EMA100. The persisted full-history summary looks plausible (CAGR 25.2%, Sharpe 1.30, DSR 0.924, mean +0.72%/trade net; pure IS was not separately persisted) but **OOS (last 30% of trades) collapses: CAGR −45.2%, Sharpe −1.85, DSR 0.186**; hit-rate only 41.6% (trend-follower: few big wins, many small losses). MC maxDD p5/p50/p95 = 36.6/57.8/**81.4%** (≫ 30% breaker), risk-of-ruin 24.2%, **entry-jitter permutation p=0.108 — the edge is NOT distinguishable from random sign-flips**. Terminal status **REJECTED** (`OOS_FAILURE`, `DSR_FAILURE`, `DRAWDOWN_RISK_FAILURE`, `NO_PROFIT_PLATEAU_OVERFIT`, `SHARIA_UNVERIFIABLE`): momentum rides the 2023–24 mega-cap-tech bull then unwinds OOS; the pooled-independent full-cash-per-name sizing amplifies drawdown. Do NOT re-tune v1. A v2 that sizes per-name off shared equity and adds a market-regime gate is a NEW candidate.
- coint-statarb-long-leg v1 (G2 daily pairs, measured 2026-07 via `npm run backtest -- --setup coint-statarb-long-leg --from 2022-07-01 --to 2026-07-10`, seed 42, dataFeed=yahoo-daily; results/coint-statarb-long-leg-2022-07-01-2026-07-10.json): Engle–Granger two-step re-run POINT-IN-TIME every bar (walk-forward re-formation, formation strictly precedes the trade — no look-ahead in pair selection OR hedge ratio); OLS β + ADF-lite unit-root gate (t ≤ −3.34, MacKinnon EG 2-var 5%) + k=2σ entry, mean-reversion/cointegration-break/ATR/time exits; long the undervalued leg ONLY (short leg Sharia-vetoed). Cross-name partner series fed via the new generic `prepareUniverse` hook (types.ts + CLI daily path) into a PIT-filtered pair book — the single-symbol engine is untouched. **585 GOOGL MOCK bars excluded in-path.** Only **9 trades** (ADBE 1 / AMZN 4 / META 1 / NFLX 2 / QCOM 1) — the halal spine has just ~10 names with ≥252d shared REAL history; the strict cointegration gate + 2σ trigger rarely coincide. Full curve looks hot (CAGR 4.78%, **Sharpe 5.05 ⇒ implausible flag**, DSR 0.823) but at n=9 this is an ARTIFACT, not an edge; OOS DSR 0.005; permutation p=0.206. Terminal status **REJECTED** (`INSUFFICIENT_SAMPLE`, `SHARIA_UNVERIFIABLE`). History could NOT be deepened: `ingest.ts` only extends forward from the latest bar, so it cannot backfill the 102-bar names' formation window with existing machinery. v2 (looser ADF / lower k / broader deep-history universe) is a NEW candidate, never a v1 retune. `BacktestRun` 2be31abd-aea0-489a-9144-8f85aa1fb647.
- gapper-orb IEX calibration (G3b, measured 2026-07 via scripts/calibrate-iex-ratio.ts): the Alpaca IEX feed prints a MEDIAN 3.28% of consolidated daily volume (1,240 symbol-days, range 0.59–14.97%), so v1's 10M consolidated cumVolume screen ⇒ v1-iex minCumVolume = 350k. Volume leg only; mcap band + gap thresholds unchanged. The full 90d/20-symbol IEX backtest (per-day batched, per-symbol streamed; results/gapper-orb-2026-04-13-2026-07-10-pooled.json) yielded 0 qualifying setups. Terminal status **REJECTED** (`INSUFFICIENT_SAMPLE`, `SHARIA_UNVERIFIABLE`). Funnel (screen legs, symbol-days): mcap∈band 128 / PM≥5% 24 / day≥5% 214 / vol≥350k 884, JOINT 0. Root cause is universe, not edge: 16/20 backfill names never enter the 10–400M band and the 4 that do (SNDL/CHPT/FCEL/DNA) have ~0 PM≥5% days with near-zero IEX premarket volume. A real PIT micro-cap gapper universe (G3d) may start a fresh validation run.
