Use the `quant-experiment-governance` skill and run exactly one Rushd Quant experiment through this DAG:

`quant-research-director (read-only hypothesis) → quant-strategist (implementation + tests + optional non-terminal diagnostic) → pre-registration commit → quant-validation-auditor (READY_TO_RUN) → orchestrator executes one FULL run → quant-validation-auditor (terminal audit) → STRATEGY_LAB/JOURNAL terminal commit`.

The user's request follows this command. Do not combine candidates, relax QDR-6/QDR-7 thresholds, or let the proposing/implementing agent validate its own terminal evidence.
